import { describe, it } from "vitest";

describe("group visibility", () => {
	it.todo("private group hides feed from non-members");
	it.todo("private group visible in discovery");
	it.todo("invite token generation");
	it.todo("invite acceptance adds member");
	it.todo("invite by username sends notification");
});
