import { describe, it } from "vitest";

describe("createGroupAction", () => {
	it.todo("creates group with valid data");
	it.todo("generates unique slug from group name");
	it.todo("rejects empty name");
	it.todo("rejects name longer than 80 chars");
	it.todo("sets creator as admin member");
	it.todo("sets visibility correctly (public/private)");
});
