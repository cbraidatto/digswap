import { describe, it } from "vitest";

describe("createReviewAction/getReviewsForRelease", () => {
	it.todo("creates review with rating 1-5");
	it.todo("rejects rating 0 or 6");
	it.todo("creates review as group post");
	it.todo("pressing-specific review stores details");
	it.todo("general release review");
	it.todo("query reviews by releaseId");
	it.todo("one review per user per release (upsert)");
});
