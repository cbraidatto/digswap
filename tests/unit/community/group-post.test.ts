import { describe, it } from "vitest";

describe("createPostAction", () => {
	it.todo("creates post with text content");
	it.todo("creates post with linked record (releaseId)");
	it.todo("rejects empty content");
	it.todo("requires group membership");
	it.todo("logs activity_feed event");
});
