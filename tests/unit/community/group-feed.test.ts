import { describe, it } from "vitest";

describe("getGroupPosts", () => {
	it.todo("returns posts paginated by cursor");
	it.todo("returns posts newest first");
	it.todo("group_post visible in personal feed only if follower AND member");
	it.todo("group_post in global feed respects group membership");
});
