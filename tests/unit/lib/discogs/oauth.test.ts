import { describe, test, expect } from "vitest";
import type {} from "tests/__mocks__/discogs";

describe("Discogs OAuth helpers", () => {
	test.todo("getRequestToken returns token and authorizeUrl");

	test.todo("getAccessToken exchanges verifier for access token");

	test.todo("stores tokens in Vault via admin client");
});
