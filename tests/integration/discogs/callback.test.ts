import { describe, test, expect } from "vitest";
import type {} from "tests/__mocks__/discogs";

describe("Discogs OAuth callback route", () => {
	test.todo("exchanges verifier for access token");

	test.todo("stores tokens and updates profile");

	test.todo("creates import job and redirects to /import-progress");
});
