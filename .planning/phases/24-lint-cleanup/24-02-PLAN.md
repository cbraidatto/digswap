---
phase: 24-lint-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/app/(protected)/(explore)/explorar/_components/advanced-search-filters.tsx
  - apps/web/src/app/(protected)/(explore)/explorar/_components/genre-filter.tsx
  - apps/web/src/app/(protected)/(explore)/explorar/_components/search-section.tsx
  - apps/web/src/app/(protected)/(explore)/explorar/_components/record-search.tsx
  - apps/web/src/app/(protected)/(explore)/explorar/_components/browse-grid.tsx
  - apps/web/src/app/(protected)/(explore)/explorar/_components/suggested-section.tsx
  - apps/web/src/app/(protected)/(explore)/explorar/_components/trending-section.tsx
  - apps/web/src/app/(protected)/(explore)/explorar/loading.tsx
  - apps/web/src/app/(protected)/(explore)/explorar/crates/page.tsx
  - apps/web/src/app/(protected)/(feed)/feed/page.tsx
  - apps/web/src/app/(protected)/(feed)/feed/loading.tsx
  - apps/web/src/app/(protected)/(feed)/feed/_components/feed-showcase.tsx
  - apps/web/src/app/(protected)/(profile)/perfil/_components/badge-row.tsx
  - apps/web/src/app/(protected)/(profile)/perfil/_components/collection-heatmap.tsx
  - apps/web/src/app/(protected)/(profile)/perfil/_components/about-tab.tsx
  - apps/web/src/app/(protected)/(profile)/perfil/[username]/compare/page.tsx
  - apps/web/src/app/(protected)/(community)/comunidade/loading.tsx
  - apps/web/src/app/(protected)/(community)/comunidade/_components/group-card.tsx
  - apps/web/src/app/(protected)/(community)/comunidade/[slug]/_components/group-post-feed.tsx
  - apps/web/src/app/(protected)/(community)/comunidade/[slug]/_components/group-composer.tsx
autonomous: true
requirements: [QUAL-01]

must_haves:
  truths:
    - "biome lint reports zero a11y errors for noLabelWithoutControl"
    - "biome lint reports zero noStaticElementInteractions errors"
    - "biome lint reports zero useKeyWithClickEvents errors"
    - "biome lint reports zero noImplicitAnyLet errors"
    - "biome lint reports zero useAriaPropsSupportedByRole errors"
    - "Loading skeleton arrays use biome-ignore for noArrayIndexKey (static lists)"
    - "Semantic elements replace generic div/span where role attributes were used"
  artifacts:
    - path: "apps/web/src/app/(protected)/(explore)/explorar/_components/advanced-search-filters.tsx"
      provides: "Fixed label-input associations"
      contains: "htmlFor"
    - path: "apps/web/src/app/(protected)/(explore)/explorar/crates/page.tsx"
      provides: "Keyboard-accessible crate cards"
      contains: "onKeyDown"
  key_links: []
---

<objective>
Fix all non-auto-fixable lint errors: a11y violations, suspicious code patterns, and incorrect ARIA usage.

Purpose: Eliminate all remaining biome lint errors that cannot be auto-fixed. Loading skeleton arrays get biome-ignore comments (index-as-key is safe for static lists). Semantic elements replace role-annotated divs.
Output: All a11y lint errors resolved, all suspicious code errors resolved.
</objective>

<execution_context>
@C:\Users\INTEL\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\INTEL\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@biome.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix a11y lint errors</name>
  <files>
    apps/web/src/app/(protected)/(explore)/explorar/_components/advanced-search-filters.tsx
    apps/web/src/app/(protected)/(explore)/explorar/_components/genre-filter.tsx
    apps/web/src/app/(protected)/(explore)/explorar/_components/search-section.tsx
    apps/web/src/app/(protected)/(explore)/explorar/_components/record-search.tsx
    apps/web/src/app/(protected)/(explore)/explorar/crates/page.tsx
    apps/web/src/app/(protected)/(profile)/perfil/_components/badge-row.tsx
    apps/web/src/app/(protected)/(profile)/perfil/[username]/compare/page.tsx
    apps/web/src/app/(protected)/(feed)/feed/_components/feed-showcase.tsx
    apps/web/src/app/(protected)/(community)/comunidade/_components/group-card.tsx
    apps/web/src/app/(protected)/(community)/comunidade/[slug]/_components/group-post-feed.tsx
    apps/web/src/app/(protected)/(community)/comunidade/[slug]/_components/group-composer.tsx
  </files>
  <read_first>
    - apps/web/src/app/(protected)/(explore)/explorar/_components/advanced-search-filters.tsx
    - apps/web/src/app/(protected)/(explore)/explorar/crates/page.tsx
    - apps/web/src/app/(protected)/(profile)/perfil/_components/badge-row.tsx
    - apps/web/src/app/(protected)/(feed)/feed/_components/feed-showcase.tsx
  </read_first>
  <action>
    Fix each a11y lint error by category:

    **noLabelWithoutControl (7 errors in advanced-search-filters.tsx):**
    Each `<label>` at lines ~242, 256, 270, 288, 306, 320, 336 is not associated with a form control. For each label:
    - Add an `id` attribute to the corresponding input/select element
    - Add `htmlFor="{id}"` to the label element
    - Use descriptive IDs like `filter-genre`, `filter-decade`, `filter-country`, etc.

    **useSemanticElements (11 errors across multiple files):**
    Replace `<div role="...">` and `<span role="...">` with the correct semantic HTML elements:
    - `<div role="list">` -> `<ul>` or `<ol>`
    - `<div role="listitem">` -> `<li>`
    - `<div role="navigation">` -> `<nav>`
    - `<div role="banner">` -> `<header>`
    - `<div role="main">` -> `<main>`
    - `<div role="complementary">` -> `<aside>`
    - `<span role="img">` -> keep span but add biome-ignore if no better element exists
    Files: genre-filter.tsx (lines ~29, 42), search-section.tsx (lines ~55, 67, 71), record-search.tsx (line ~54), compare/page.tsx (line ~44), group-card.tsx (line ~65), group-post-feed.tsx (lines ~68, 70), group-composer.tsx (line ~128)
    Remove the `role` attribute after converting to semantic element.

    **noStaticElementInteractions (2 errors):**
    - feed-showcase.tsx line ~114: Add `role="button"` and `tabIndex={0}` and `onKeyDown={(e) => { if (e.key === 'Enter' || e.key === ' ') { e.preventDefault(); /* same handler as onClick */ } }}`
    - crates/page.tsx line ~97: Same pattern -- add role="button", tabIndex={0}, and onKeyDown handler

    **useKeyWithClickEvents (1 error in crates/page.tsx line ~97):**
    Already addressed by the noStaticElementInteractions fix above -- adding onKeyDown resolves both.

    **useAriaPropsSupportedByRole (1 error in badge-row.tsx line ~13):**
    Read the component to understand the element and its role. If using `aria-label` on an element whose role doesn't support it, either:
    - Change to a role that supports aria-label (e.g., `role="img"` supports it)
    - Or use `title` attribute instead
    - Or wrap in a `<span>` with appropriate role
  </action>
  <verify>
    <automated>cd apps/web && npx biome lint ../web/src/ --max-diagnostics=200 2>&1 | grep -E "noLabelWithoutControl|noStaticElementInteractions|useKeyWithClickEvents|useAriaPropsSupportedByRole|useSemanticElements" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - Zero noLabelWithoutControl errors
    - Zero noStaticElementInteractions errors
    - Zero useKeyWithClickEvents errors
    - Zero useAriaPropsSupportedByRole errors
    - Zero useSemanticElements errors (or reduced to only justified biome-ignore cases)
    - All interactive elements have keyboard support (onKeyDown + role + tabIndex)
  </acceptance_criteria>
  <done>All a11y lint errors resolved. Labels associated with controls. Semantic elements used. Interactive elements keyboard-accessible.</done>
</task>

<task type="auto">
  <name>Task 2: Fix remaining lint errors and add biome-ignore for safe patterns</name>
  <files>
    apps/web/src/app/(protected)/(feed)/feed/page.tsx
    apps/web/src/app/(protected)/(feed)/feed/loading.tsx
    apps/web/src/app/(protected)/(explore)/explorar/loading.tsx
    apps/web/src/app/(protected)/(explore)/explorar/_components/browse-grid.tsx
    apps/web/src/app/(protected)/(explore)/explorar/_components/suggested-section.tsx
    apps/web/src/app/(protected)/(explore)/explorar/_components/trending-section.tsx
    apps/web/src/app/(protected)/(explore)/explorar/crates/page.tsx
    apps/web/src/app/(protected)/(profile)/perfil/_components/collection-heatmap.tsx
    apps/web/src/app/(protected)/(profile)/perfil/_components/about-tab.tsx
    apps/web/src/app/(protected)/(community)/comunidade/loading.tsx
  </files>
  <read_first>
    - apps/web/src/app/(protected)/(feed)/feed/page.tsx (line ~44 for noImplicitAnyLet)
    - apps/web/src/app/(protected)/(feed)/feed/loading.tsx (lines ~17, 27 for noArrayIndexKey)
    - apps/web/src/app/(protected)/(explore)/explorar/loading.tsx (line ~28)
  </read_first>
  <action>
    **noImplicitAnyLet (1 error in feed/page.tsx line ~44):**
    Add an explicit type annotation to the `let` variable. Read the surrounding code to determine the correct type. Example: `let result` -> `let result: FeedResult | null = null` (use the actual type from context).

    **noArrayIndexKey (10 errors -- all loading skeletons and static arrays):**
    For each occurrence, add a `// biome-ignore lint/suspicious/noArrayIndexKey: static skeleton list` comment on the line above the JSX element using the array index as key. These are all safe because:
    - Loading skeletons are static lists that never reorder
    - The arrays are created inline with `Array.from()` or similar patterns
    Files and approximate lines:
    - feed/loading.tsx: lines ~17, ~27
    - explorar/loading.tsx: line ~28
    - browse-grid.tsx: line ~66
    - suggested-section.tsx: line ~44
    - trending-section.tsx: line ~40
    - crates/page.tsx: line ~64
    - collection-heatmap.tsx: line ~44
    - about-tab.tsx: line ~109
    - comunidade/loading.tsx: line ~15

    After all fixes, run `npx biome check apps/web/src/ --max-diagnostics=500` and confirm that all errors are resolved. Remaining issues should only be warnings (noNonNullAssertion, noExplicitAny, noUnusedFunctionParameters) which are style recommendations, not blocking errors.
  </action>
  <verify>
    <automated>cd apps/web && npx biome lint ../web/src/ --max-diagnostics=200 2>&1 | grep -E "noImplicitAnyLet|noArrayIndexKey|noCommentText" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - Zero noImplicitAnyLet errors
    - Zero noArrayIndexKey errors (all suppressed with biome-ignore comments)
    - Zero noCommentText errors (fixed by Plan 01 auto-fix or already resolved)
    - Only style warnings remain (noNonNullAssertion, noExplicitAny, noUnusedFunctionParameters -- these are warnings, not errors)
  </acceptance_criteria>
  <done>All blocking lint errors resolved. Only style warnings remain. QUAL-01 satisfied.</done>
</task>

</tasks>

<verification>
1. `npx biome check apps/web/src/` reports zero errors (warnings acceptable)
2. All a11y violations resolved
3. All noArrayIndexKey instances have biome-ignore comments with justification
4. feed/page.tsx has explicit type annotation
</verification>

<success_criteria>
- biome check reports zero errors across apps/web/src/
- All a11y lint rules pass
- All suspicious code patterns resolved
- biome-ignore comments include justification for each suppression
</success_criteria>

<output>
After completion, create `.planning/phases/24-lint-cleanup/24-02-SUMMARY.md`
</output>
