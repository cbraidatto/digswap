---
phase: 24-lint-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitattributes
  - apps/web/src/**/*.ts
  - apps/web/src/**/*.tsx
autonomous: true
requirements: [QUAL-01]

must_haves:
  truths:
    - "All source files in apps/web/src use LF line endings"
    - "biome format reports zero CRLF errors"
    - "biome check --fix resolves all auto-fixable import sorting and lint issues"
  artifacts:
    - path: ".gitattributes"
      provides: "Git line ending normalization config"
      contains: "* text=auto eol=lf"
  key_links:
    - from: ".gitattributes"
      to: "git checkout/commit behavior"
      via: "eol=lf forces LF on checkout"
      pattern: "eol=lf"
---

<objective>
Normalize all line endings to LF and auto-fix formatting, import sorting, and auto-fixable lint issues across the entire web app source.

Purpose: QUAL-01 requires zero CRLF formatting errors. This plan creates the .gitattributes guard and runs bulk auto-fixes.
Output: All 96 CRLF format errors eliminated, all 13 organize errors fixed, all auto-fixable lint issues resolved.
</objective>

<execution_context>
@C:\Users\INTEL\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\INTEL\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@biome.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create .gitattributes and normalize line endings</name>
  <files>.gitattributes</files>
  <read_first>
    - biome.json (confirm formatter config)
  </read_first>
  <action>
    1. Create `.gitattributes` at the repo root with:
       ```
       * text=auto eol=lf
       *.png binary
       *.jpg binary
       *.jpeg binary
       *.gif binary
       *.ico binary
       *.woff binary
       *.woff2 binary
       ```
       This forces LF on all text files going forward, preventing CRLF from Windows git config (core.autocrlf=true).

    2. Run `git add --renormalize .` to re-normalize all tracked files to LF in the index. This converts existing CRLF files to LF without changing working tree content until next checkout.

    3. Run `biome format --write apps/web/src/` to convert all CRLF line endings in source files to LF immediately.

    4. Run `biome check --fix apps/web/src/` to fix all auto-fixable issues:
       - Import sorting (13 organize errors)
       - Auto-fixable lint: noUnusedImports (5), noGlobalIsNan (3), noCommentText (4), noUnusedVariables (some)

    5. Verify with `biome format apps/web/src/ 2>&1 | tail -5` -- should show zero format errors.
  </action>
  <verify>
    <automated>cd apps/web && npx biome format ../web/src/ 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - .gitattributes exists with `* text=auto eol=lf`
    - `biome format apps/web/src/` reports zero format errors (no CRLF diffs)
    - `biome check --fix` has resolved all auto-fixable organize and lint issues
    - Import sorting errors eliminated
  </acceptance_criteria>
  <done>All 96 CRLF format errors and 13 organize errors eliminated. Auto-fixable lint issues resolved. .gitattributes prevents future CRLF.</done>
</task>

<task type="auto">
  <name>Task 2: Verify remaining error count</name>
  <files></files>
  <read_first></read_first>
  <action>
    Run `biome check apps/web/src/ --max-diagnostics=500 2>&1` and count remaining errors.

    After Task 1, the remaining issues should ONLY be non-auto-fixable lint rules (a11y, suspicious, style warnings). These are addressed in Plan 02.

    Record the exact remaining count for Plan 02 reference.
  </action>
  <verify>
    <automated>cd apps/web && npx biome check ../web/src/ --max-diagnostics=500 2>&1 | grep -c "━━" || true</automated>
  </verify>
  <acceptance_criteria>
    - Format errors: 0
    - Organize errors: 0
    - Remaining issues are only non-auto-fixable lint rules
  </acceptance_criteria>
  <done>Remaining error count documented. All auto-fixable issues confirmed resolved.</done>
</task>

</tasks>

<verification>
1. `biome format apps/web/src/` exits with 0 errors
2. `biome check --fix apps/web/src/` reports no more fixable issues
3. `.gitattributes` exists at repo root with LF enforcement
</verification>

<success_criteria>
- Zero CRLF-related format errors in biome check output
- Zero import organize errors
- .gitattributes prevents future CRLF contamination
</success_criteria>

<output>
After completion, create `.planning/phases/24-lint-cleanup/24-01-SUMMARY.md`
</output>
