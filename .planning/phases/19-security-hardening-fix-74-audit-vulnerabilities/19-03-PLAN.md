---
phase: 19-security-hardening-fix-74-audit-vulnerabilities
plan: 03
type: execute
wave: 2
depends_on: [19-01, 19-02]
files_modified:
  - .planning/phases/19-security-hardening-fix-74-audit-vulnerabilities/19-03-SUMMARY.md
autonomous: true
requirements: [SEC-AUDIT-05, SEC-AUDIT-06]
must_haves:
  truths:
    - "All 8 Drizzle schema files match the production migrations (no policy drift)"
    - "Full test suite (vitest + tsc) passes with all 563+ tests green"
    - "No TypeScript compilation errors across the entire monorepo"
  artifacts:
    - path: ".planning/phases/19-security-hardening-fix-74-audit-vulnerabilities/19-03-SUMMARY.md"
      provides: "Schema alignment results table and audit closure checklist"
      contains: "Schema Alignment Spot-Check Results"
  key_links:
    - from: "apps/web/src/lib/db/schema/*.ts"
      to: "supabase/migrations/2026041*.sql"
      via: "Column definitions and RLS policy alignment"
      pattern: "pgTable|pgPolicy"
---

<objective>
Verify all security hardening changes and confirm schema alignment with production migrations.

Purpose: This is the final gate for Phase 19. The Drizzle schema spot-check confirms the Codex patch (commit 525a307) correctly aligned all 8 schema files with the two security migrations. The full test suite gate ensures all 563+ tests pass after the Plan 01 and Plan 02 changes. No new production code is written — this plan only verifies and documents results.

Output: SUMMARY file with schema alignment results table and complete audit closure checklist.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-security-hardening-fix-74-audit-vulnerabilities/19-CONTEXT.md
@.planning/phases/19-security-hardening-fix-74-audit-vulnerabilities/19-01-SUMMARY.md
@.planning/phases/19-security-hardening-fix-74-audit-vulnerabilities/19-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Spot-check Drizzle schema alignment and produce results artifact</name>
  <files>.planning/phases/19-security-hardening-fix-74-audit-vulnerabilities/19-03-schema-check.md</files>
  <read_first>
    - supabase/migrations/20260411_security_audit_fixes.sql (full file — Phase 1 security fixes)
    - supabase/migrations/20260412_security_audit_phase2.sql (full file — Phase 2 security fixes)
    - apps/web/src/lib/db/schema/collections.ts (full file)
    - apps/web/src/lib/db/schema/social.ts (full file)
    - apps/web/src/lib/db/schema/groups.ts (full file)
    - apps/web/src/lib/db/schema/group-invites.ts (full file)
    - apps/web/src/lib/db/schema/wantlist.ts (full file)
    - apps/web/src/lib/db/schema/notifications.ts (full file)
    - apps/web/src/lib/db/schema/listening-logs.ts (full file)
    - apps/web/src/lib/db/schema/trades.ts (full file)
  </read_first>
  <action>
This is a verification task that produces a concrete output artifact.

For each of the 8 Drizzle schema files, verify alignment with the production migrations:

**Verification checklist (check each item and record PASS/FAIL):**

1. **collections.ts** — Verify any RLS policy changes from 20260411/20260412 are reflected in the Drizzle schema's pgPolicy definitions. Look for: SELECT/INSERT/UPDATE/DELETE policies matching the migration SQL.

2. **social.ts** — Check for follows/activity_feed table RLS alignment.

3. **groups.ts** — Check for community_groups table RLS alignment, especially any SECURITY DEFINER function changes.

4. **group-invites.ts** — Check for group_invites table RLS alignment.

5. **wantlist.ts** — Check wantlist_items RLS from 20260412 migration (specifically the new INSERT/UPDATE/DELETE policies).

6. **notifications.ts** — Check notifications INSERT block from 20260412 migration (should block direct INSERT, only server actions create notifications).

7. **listening-logs.ts** — Check listening_logs dual policy fix from 20260412 migration.

8. **trades.ts** — Check trade_requests UPDATE policy removal from 20260411 migration, plus acquire_trade_lease terminal check from 20260412.

For each file, the Drizzle pgPolicy definitions should match the SQL in the migrations. If any are missing or misaligned, record the specific discrepancy.

**Write the results to `.planning/phases/19-security-hardening-fix-74-audit-vulnerabilities/19-03-schema-check.md`:**

```markdown
# Schema Alignment Spot-Check Results

| Schema File | Migration | Status | Notes |
|-------------|-----------|--------|-------|
| collections.ts | 20260411 | PASS/FAIL | details |
| social.ts | 20260411 | PASS/FAIL | details |
| groups.ts | 20260411 | PASS/FAIL | details |
| group-invites.ts | 20260412 | PASS/FAIL | details |
| wantlist.ts | 20260412 | PASS/FAIL | details |
| notifications.ts | 20260412 | PASS/FAIL | details |
| listening-logs.ts | 20260412 | PASS/FAIL | details |
| trades.ts | 20260411+12 | PASS/FAIL | details |
```

If any FAIL: create a follow-up fix in the schema file (update pgPolicy to match migration SQL). Do NOT create new migrations — only update Drizzle schema definitions.
  </action>
  <verify>
    <automated>cd "C:\Users\INTEL\Desktop\Get Shit DOne" && test -f .planning/phases/19-security-hardening-fix-74-audit-vulnerabilities/19-03-schema-check.md && grep -c "PASS\|FAIL" .planning/phases/19-security-hardening-fix-74-audit-vulnerabilities/19-03-schema-check.md</automated>
  </verify>
  <acceptance_criteria>
    - .planning/phases/19-security-hardening-fix-74-audit-vulnerabilities/19-03-schema-check.md exists
    - The file contains a results table with PASS/FAIL for all 8 schema files
    - Any FAIL items have been fixed (Drizzle schema updated to match migration)
  </acceptance_criteria>
  <done>All 8 Drizzle schema files verified against production security migrations. Results documented in schema-check.md artifact.</done>
</task>

<task type="auto">
  <name>Task 2: Run full test suite and TypeScript compilation gate</name>
  <files></files>
  <read_first>
    - .planning/phases/19-security-hardening-fix-74-audit-vulnerabilities/19-01-SUMMARY.md (confirm Plan 01 completed)
    - .planning/phases/19-security-hardening-fix-74-audit-vulnerabilities/19-02-SUMMARY.md (confirm Plan 02 completed)
  </read_first>
  <action>
Run the full test suite and TypeScript compilation as a final gate.

**Step 1: TypeScript compilation check (both apps)**

```bash
cd apps/web && npx tsc --noEmit
cd apps/desktop && npx tsc --noEmit
```

Both must pass with 0 errors. If errors exist from Plan 01/02 changes, fix them.

**Step 2: Full vitest suite (apps/web)**

```bash
cd apps/web && npx vitest run 2>&1 | tail -20
```

All 563+ tests must pass. Record the exact count.

If any tests fail:
- Read the failure output
- Determine if the failure is from Plan 01/02 changes (fix required) or pre-existing (document as pre-existing)
- Fix any failures caused by this phase's changes

**Step 3: Desktop TypeScript gate**

Note: `apps/desktop` has no vitest configuration (no vitest in devDependencies, no vitest.config.ts, no test script in package.json). There is one test file (`apps/desktop/tests/peer-handshake.test.ts`) but it cannot currently be run. The TypeScript compilation in Step 1 is the primary gate for desktop code correctness.

```bash
cd apps/desktop && npx tsc --noEmit 2>&1 | tail -20
```

Confirm 0 errors. This verifies Plan 01's utilityProcess migration and Plan 02's throttle additions compile cleanly.

**Step 4: Verify security audit closure checklist**

Confirm each of the 7 remaining items is resolved:

1. BridgeWindow nodeIntegration:true -> FIXED (Plan 01: utilityProcess migration)
2. Hash fallback when DB is null -> FIXED (Plan 02 Task 1: null rejection)
3. Trade RPC rate limits -> FIXED (Plan 02 Task 2: in-memory throttle)
4. Handoff token TTL mismatch -> FIXED (Plan 02 Task 3: 30_000ms alignment in both files)
5. Drizzle schema spot-check -> VERIFIED (Plan 03 Task 1)
6. Full test suite gate -> VERIFIED (this task)
7. (67 previously fixed items from Codex patch + security migrations) -> ALREADY FIXED

Record the final audit closure status in the SUMMARY.
  </action>
  <verify>
    <automated>cd "C:\Users\INTEL\Desktop\Get Shit DOne\apps\web" && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - npx tsc --noEmit passes in apps/web with 0 errors
    - npx tsc --noEmit passes in apps/desktop with 0 errors
    - npx vitest run in apps/web shows all tests passing (563+ tests, 0 failures)
    - SUMMARY documents the complete audit closure checklist with all 7 items marked resolved
  </acceptance_criteria>
  <done>Full test suite passes. TypeScript compiles cleanly in both apps. All 74 audit vulnerabilities accounted for (67 previously fixed + 7 fixed/verified in this phase).</done>
</task>

</tasks>

<verification>
1. `cd apps/web && npx tsc --noEmit` exits 0
2. `cd apps/desktop && npx tsc --noEmit` exits 0
3. `cd apps/web && npx vitest run` shows 0 failures
4. Schema spot-check artifact exists with all PASS results
</verification>

<success_criteria>
- TypeScript compilation passes across the monorepo (both apps/web and apps/desktop)
- All 563+ tests pass in apps/web
- Desktop TypeScript compiles cleanly (no vitest available — tsc is the gate)
- All 8 Drizzle schema files verified against production migrations
- Complete audit closure checklist documented (74/74 vulnerabilities resolved)
</success_criteria>

<output>
After completion, create `.planning/phases/19-security-hardening-fix-74-audit-vulnerabilities/19-03-SUMMARY.md`
</output>
