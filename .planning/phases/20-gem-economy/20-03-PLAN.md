---
phase: 20-gem-economy
plan: 03
type: execute
wave: 2
depends_on:
  - 20-01
  - 20-02
files_modified:
  - apps/web/src/app/(protected)/(profile)/perfil/_components/collection-card.tsx
  - apps/web/src/app/(protected)/(feed)/feed/_components/feed-card.tsx
  - apps/web/src/app/(protected)/(feed)/feed/_components/group-feed-card.tsx
  - apps/web/src/app/(protected)/(explore)/explorar/_components/record-search-card.tsx
  - apps/web/src/app/(protected)/(explore)/explorar/_components/browse-grid.tsx
  - apps/web/src/app/(protected)/(explore)/explorar/_components/trending-section.tsx
  - apps/web/src/app/(protected)/(explore)/explorar/_components/suggested-section.tsx
  - apps/web/src/app/(protected)/(profile)/perfil/_components/wantlist-match-section.tsx
  - apps/web/src/app/(protected)/(profile)/perfil/_components/wantlist-card.tsx
  - apps/web/src/app/release/[discogsId]/_components/release-hero.tsx
  - apps/web/src/app/release/[discogsId]/_components/similar-section.tsx
  - apps/web/src/app/(protected)/(profile)/perfil/[username]/compare/page.tsx
  - apps/web/src/lib/wantlist/radar-queries.ts
  - apps/web/src/app/(protected)/(explore)/explorar/_components/leaderboard-row.tsx
  - apps/web/src/app/(protected)/(profile)/perfil/_components/rank-card.tsx
  - apps/web/src/app/(protected)/(profile)/perfil/_components/about-tab.tsx
  - apps/web/src/app/(protected)/(profile)/perfil/page.tsx
  - apps/web/src/components/shell/notification-row.tsx
autonomous: true
requirements:
  - GEM-01
  - GEM-06

must_haves:
  truths:
    - "No file imports RarityPill — all replaced with GemBadge"
    - "No file calls getRarityTier() — all replaced with getGemTier()"
    - "Leaderboard row shows 'gems' suffix instead of 'pts'"
    - "RankCard shows 'Gem Score' label instead of 'Rarity'"
    - "Profile page passes gemScore to AboutTab and RankCard"
    - "NotificationRow handles gem_tier_change type"
  artifacts:
    - path: "apps/web/src/app/(protected)/(profile)/perfil/_components/rank-card.tsx"
      provides: "RankCard with gemScore prop"
      contains: "Gem Score"
    - path: "apps/web/src/app/(protected)/(explore)/explorar/_components/leaderboard-row.tsx"
      provides: "Leaderboard row with gems suffix"
      contains: "gems"
    - path: "apps/web/src/components/shell/notification-row.tsx"
      provides: "NotificationRow with gem_tier_change case"
      contains: "gem_tier_change"
  key_links:
    - from: "all consumer files"
      to: "apps/web/src/components/ui/gem-badge.tsx"
      via: "import { GemBadge }"
      pattern: "import.*GemBadge.*from.*gem-badge"
    - from: "apps/web/src/app/(protected)/(profile)/perfil/page.tsx"
      to: "apps/web/src/lib/gamification/queries.ts"
      via: "getUserRanking returns gemScore"
      pattern: "ranking\\.gemScore"
---

<objective>
Replace all RarityPill imports with GemBadge, update all getRarityTier calls to getGemTier, update RankCard to show Gem Score, update leaderboard row format, update profile page data flow, and add gem_tier_change notification type.

Purpose: This is the comprehensive UI sweep that makes gem badges visible across every surface. Without this, users still see the old rarity pills.
Output: All 13+ consumer files updated from rarity to gem system, leaderboard shows gems, profile shows gem score.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/20-gem-economy/20-CONTEXT.md
@.planning/phases/20-gem-economy/20-RESEARCH.md
@.planning/phases/20-gem-economy/20-UI-SPEC.md
@.planning/phases/20-gem-economy/20-01-SUMMARY.md
@.planning/phases/20-gem-economy/20-02-SUMMARY.md

<interfaces>
<!-- From Plan 01: gem-badge.tsx -->
```typescript
import { GemBadge } from "@/components/ui/gem-badge";
// Props: score: number | null | undefined, showScore?: boolean, className?: string
```

<!-- From Plan 02: updated UserRanking -->
```typescript
export interface UserRanking {
  gemScore: number;
  contributionScore: number;
  globalRank: number | null;
  title: string | null;
}
```

<!-- From Plan 01: getGemTier -->
```typescript
import { getGemTier, type GemTier } from "@/lib/gems/constants";
// getGemTier(score: number | null): GemTier | null
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace RarityPill with GemBadge across all 11 consumer files</name>
  <files>
    apps/web/src/app/(protected)/(profile)/perfil/_components/collection-card.tsx
    apps/web/src/app/(protected)/(feed)/feed/_components/feed-card.tsx
    apps/web/src/app/(protected)/(feed)/feed/_components/group-feed-card.tsx
    apps/web/src/app/(protected)/(explore)/explorar/_components/record-search-card.tsx
    apps/web/src/app/(protected)/(explore)/explorar/_components/browse-grid.tsx
    apps/web/src/app/(protected)/(explore)/explorar/_components/trending-section.tsx
    apps/web/src/app/(protected)/(explore)/explorar/_components/suggested-section.tsx
    apps/web/src/app/(protected)/(profile)/perfil/_components/wantlist-match-section.tsx
    apps/web/src/app/(protected)/(profile)/perfil/_components/wantlist-card.tsx
    apps/web/src/app/release/[discogsId]/_components/release-hero.tsx
    apps/web/src/app/release/[discogsId]/_components/similar-section.tsx
  </files>
  <read_first>
    apps/web/src/app/(protected)/(profile)/perfil/_components/collection-card.tsx
    apps/web/src/app/(protected)/(feed)/feed/_components/feed-card.tsx
    apps/web/src/app/(protected)/(feed)/feed/_components/group-feed-card.tsx
    apps/web/src/app/(protected)/(explore)/explorar/_components/record-search-card.tsx
    apps/web/src/app/(protected)/(explore)/explorar/_components/browse-grid.tsx
    apps/web/src/app/(protected)/(explore)/explorar/_components/trending-section.tsx
    apps/web/src/app/(protected)/(explore)/explorar/_components/suggested-section.tsx
    apps/web/src/app/(protected)/(profile)/perfil/_components/wantlist-match-section.tsx
    apps/web/src/app/(protected)/(profile)/perfil/_components/wantlist-card.tsx
    apps/web/src/app/release/[discogsId]/_components/release-hero.tsx
    apps/web/src/app/release/[discogsId]/_components/similar-section.tsx
    apps/web/src/components/ui/gem-badge.tsx
  </read_first>
  <action>
    For each of the 11 files, perform the same mechanical replacement:

    1. **Change import:** Replace `import { RarityPill } from "@/components/ui/rarity-pill"` with `import { GemBadge } from "@/components/ui/gem-badge"`
    2. **Change JSX:** Replace `<RarityPill score={...} />` with `<GemBadge score={...} />`
    3. **Remove getRarityTier imports if present:** If the file imports `getRarityTier` from `@/lib/collection/rarity`, replace with `getGemTier` from `@/lib/gems/constants` where used. If `getRarityTier` was only used for the pill component, remove the import entirely.
    4. **Preserve all existing props:** The `score` prop should pass the same value (rarity_score / rarityScore). The GemBadge accepts the same numeric score.
    5. **Note on showScore:** RarityPill defaults `showScore=true`, GemBadge defaults `showScore=false`. Where RarityPill was used with no explicit showScore prop (meaning true), explicitly pass `showScore={true}` to GemBadge to preserve behavior. Where RarityPill had `showScore={false}`, keep it or omit (same default).

    Files with `getRarityTier` import that also need updating:
    - `collection-card.tsx` — imports getRarityTier for conditional styling; replace with getGemTier
    - `feed-card.tsx` — imports getRarityTier; replace with getGemTier
    - `record-search-card.tsx` — imports getRarityTier; replace with getGemTier
    - `release-hero.tsx` — imports getRarityTier; replace with getGemTier
    - `compare/page.tsx` — imports getRarityTier for comparison; replace with getGemTier

    Also update `apps/web/src/lib/wantlist/radar-queries.ts` if it imports getRarityTier — replace with getGemTier from `@/lib/gems/constants`.

    Also update `apps/web/src/app/(protected)/(profile)/perfil/[username]/compare/page.tsx` — replace getRarityTier import and usage with getGemTier.
  </action>
  <verify>
    <automated>cd apps/web && npx tsc --noEmit 2>&1 | head -50</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "RarityPill" apps/web/src/app/ apps/web/src/components/` returns no matches (only rarity-pill.tsx file itself may remain as deprecated)
    - `grep -r "from.*rarity-pill" apps/web/src/app/` returns no matches
    - `grep -r "GemBadge" apps/web/src/app/` returns at least 8 matches
    - `grep -r "getRarityTier" apps/web/src/app/ apps/web/src/lib/wantlist/` returns no matches (only rarity.ts itself)
    - TypeScript compilation succeeds with no errors related to GemBadge or RarityPill
  </acceptance_criteria>
  <done>
    All 11 consumer files import GemBadge instead of RarityPill. All getRarityTier calls replaced with getGemTier. TypeScript compiles without errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update RankCard, LeaderboardRow, NotificationRow, AboutTab, and profile page data flow</name>
  <files>
    apps/web/src/app/(protected)/(profile)/perfil/_components/rank-card.tsx
    apps/web/src/app/(protected)/(explore)/explorar/_components/leaderboard-row.tsx
    apps/web/src/app/(protected)/(profile)/perfil/_components/about-tab.tsx
    apps/web/src/app/(protected)/(profile)/perfil/page.tsx
    apps/web/src/components/shell/notification-row.tsx
  </files>
  <read_first>
    apps/web/src/app/(protected)/(profile)/perfil/_components/rank-card.tsx
    apps/web/src/app/(protected)/(explore)/explorar/_components/leaderboard-row.tsx
    apps/web/src/app/(protected)/(profile)/perfil/_components/about-tab.tsx
    apps/web/src/app/(protected)/(profile)/perfil/page.tsx
    apps/web/src/components/shell/notification-row.tsx
    apps/web/src/lib/gamification/queries.ts
  </read_first>
  <action>
    **1. Update `rank-card.tsx` (per UI-SPEC.md):**
    - Change prop name from `rarityScore: number` to `gemScore: number`
    - Change display value from `rarityScore.toFixed(1)` to `gemScore.toLocaleString()`
    - Change label from "Rarity" to "Gem Score"
    - Keep the 3-column grid layout, all other styling unchanged

    **2. Update `leaderboard-row.tsx` (per UI-SPEC.md):**
    - Change score display from `{score.toFixed(1)}pts` to `{score.toLocaleString()} gems`
    - Keep all other styling unchanged (`font-mono text-sm text-primary tabular-nums text-right`)

    **3. Update `about-tab.tsx`:**
    - Change `AboutTabProps.stats.rarityScore` to `stats.gemScore`
    - Update the `globalScore` computation: `stats.gemScore * 0.7 + stats.contributionScore * 0.3`
    - Update RankCard usage: pass `gemScore={stats.gemScore}` instead of `rarityScore={stats.rarityScore}`
    - Update the RarityCardModal props: pass `avgRarity={stats.gemScore}` and update `signOgParams` call to pass `stats.gemScore`
    - Update the stats grid "Score" label value to use the new globalScore

    **4. Update `perfil/page.tsx`:**
    - Where `ranking?.rarityScore` is passed to AboutTab and ProfileHero, change to `ranking?.gemScore ?? 0`
    - Update the stats object passed to AboutTab: `gemScore: ranking?.gemScore ?? 0` (was `rarityScore: ranking?.rarityScore ?? 0`)

    **5. Update `notification-row.tsx`:**
    - Add `gem_tier_change` case to `getTypeIcon` switch:
      ```typescript
      case "gem_tier_change":
        return { icon: "diamond", className: "text-primary text-lg" };
      ```
    - No other changes to NotificationRow component

    **6. Also check and update the public profile page** at `apps/web/src/app/perfil/[username]/page.tsx` if it references `rarityScore` — update to `gemScore`.
  </action>
  <verify>
    <automated>cd apps/web && npx tsc --noEmit 2>&1 | head -50</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/app/(protected)/(profile)/perfil/_components/rank-card.tsx contains `gemScore: number` in props
    - apps/web/src/app/(protected)/(profile)/perfil/_components/rank-card.tsx contains `Gem Score` label text
    - apps/web/src/app/(protected)/(explore)/explorar/_components/leaderboard-row.tsx contains `gems` suffix
    - apps/web/src/app/(protected)/(profile)/perfil/_components/about-tab.tsx contains `stats.gemScore`
    - apps/web/src/app/(protected)/(profile)/perfil/page.tsx contains `gemScore: ranking?.gemScore`
    - apps/web/src/components/shell/notification-row.tsx contains `gem_tier_change`
    - TypeScript compilation succeeds with no errors
  </acceptance_criteria>
  <done>
    RankCard shows "Gem Score", leaderboard shows "gems" suffix, profile page passes gemScore through the component tree, NotificationRow handles gem_tier_change type, TypeScript compiles cleanly.
  </done>
</task>

</tasks>

<verification>
- `cd apps/web && npx tsc --noEmit` — zero TypeScript errors
- `grep -rn "RarityPill" apps/web/src/app/` — no remaining imports
- `grep -rn "getRarityTier" apps/web/src/app/` — no remaining calls
- `grep "Gem Score" apps/web/src/app/(protected)/(profile)/perfil/_components/rank-card.tsx` — label updated
- `grep "gems" apps/web/src/app/(protected)/(explore)/explorar/_components/leaderboard-row.tsx` — suffix updated
</verification>

<success_criteria>
- Every record display surface shows GemBadge instead of RarityPill
- RankCard displays "Gem Score" with localized number format
- Leaderboard rows show "{score} gems" format
- Profile page data flow uses gemScore from getUserRanking
- NotificationRow handles gem_tier_change notification type
- Zero TypeScript compilation errors
</success_criteria>

<output>
After completion, create `.planning/phases/20-gem-economy/20-03-SUMMARY.md`
</output>
