---
phase: 20-gem-economy
plan: 05
type: execute
wave: 3
depends_on:
  - 20-03
  - 20-04
files_modified:
  - apps/web/src/lib/gamification/badge-awards.ts
  - apps/web/src/actions/engagement.ts
  - apps/web/src/app/(protected)/(profile)/perfil/_components/rarity-card-modal.tsx
autonomous: false
requirements:
  - GEM-03
  - GEM-07

must_haves:
  truths:
    - "Badge rare_find is awarded based on Safira/Diamante tier (score >= 3.0) instead of rarity >= 2.0"
    - "Digger DNA rarity profile labels use gem terminology"
    - "RarityCardModal updated to show gem data"
    - "Full app compiles and all tests pass"
    - "Visual checkpoint confirms gem badges render correctly across all surfaces"
  artifacts:
    - path: "apps/web/src/lib/gamification/badge-awards.ts"
      provides: "Updated rare_find badge check using gem tier threshold"
      contains: "3.0"
    - path: "apps/web/src/actions/engagement.ts"
      provides: "Updated Digger DNA labels for gem terminology"
  key_links:
    - from: "apps/web/src/lib/gamification/badge-awards.ts"
      to: "apps/web/src/lib/gems/constants.ts"
      via: "gem tier threshold for badge award"
      pattern: ">= 3.0|sapphire|diamond"
---

<objective>
Update badge awards to use gem tier thresholds, update Digger DNA labels, update RarityCardModal, and run a final visual verification checkpoint.

Purpose: Ensures the remaining gamification touchpoints (badges, DNA profiles, share cards) are aligned with the gem system, then visually confirms the entire phase works.
Output: All gamification references updated, visual confirmation of gem economy across all surfaces.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/20-gem-economy/20-CONTEXT.md
@.planning/phases/20-gem-economy/20-RESEARCH.md
@.planning/phases/20-gem-economy/20-UI-SPEC.md
@.planning/phases/20-gem-economy/20-01-SUMMARY.md
@.planning/phases/20-gem-economy/20-02-SUMMARY.md
@.planning/phases/20-gem-economy/20-03-SUMMARY.md
@.planning/phases/20-gem-economy/20-04-SUMMARY.md

<interfaces>
<!-- From Plan 01: gem constants -->
```typescript
import { getGemTier, type GemTier } from "@/lib/gems/constants";
// getGemTier(score: number | null): GemTier | null
// Score >= 3.0 = sapphire, >= 6.0 = diamond
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update badge awards, Digger DNA labels, and RarityCardModal</name>
  <files>
    apps/web/src/lib/gamification/badge-awards.ts
    apps/web/src/actions/engagement.ts
    apps/web/src/app/(protected)/(profile)/perfil/_components/rarity-card-modal.tsx
  </files>
  <read_first>
    apps/web/src/lib/gamification/badge-awards.ts
    apps/web/src/actions/engagement.ts
    apps/web/src/app/(protected)/(profile)/perfil/_components/rarity-card-modal.tsx
    apps/web/src/lib/gems/constants.ts
  </read_first>
  <action>
    **1. Update `apps/web/src/lib/gamification/badge-awards.ts`:**

    Find the `rare_find` badge award check. Currently it checks `rarity_score >= 2.0` (or `gte("releases.rarity_score", 2.0)` or similar). Update the threshold to `>= 3.0` which corresponds to Safira tier — matching the updated BADGE_DEFINITIONS description "Added a Safira or Diamante gem to your collection".

    The exact code pattern will vary but look for:
    - A Supabase or Drizzle query checking `rarity_score` with a threshold
    - Update the threshold value from `2.0` to `3.0`

    **2. Update `apps/web/src/actions/engagement.ts`:**

    Find the `computeDiggerDna` function (or wherever Digger DNA rarity profile labels are defined). Per RESEARCH.md Open Question 3, keep using raw ratios for thresholds but rename labels to gem terminology:

    - "mainstream_maven" -> "quartz_collector" (avgRarity < 25 percentile)
    - "balanced_digger" -> "emerald_seeker" (avgRarity 25-50 percentile)
    - "deep_cutter" -> "ruby_hunter" (avgRarity 50-75 percentile)
    - "ultra_rare_hunter" -> "diamond_chaser" (avgRarity > 75 percentile)

    Keep the same threshold values (25/50/75 on raw ratio scale). Only change the string labels. This avoids threshold recalibration.

    **3. Update `apps/web/src/app/(protected)/(profile)/perfil/_components/rarity-card-modal.tsx`:**

    This component renders a share modal for the OG rarity card. Update:
    - Section heading text from "Rarity Card" to "Gem Card" if applicable
    - The OG image URL construction: change `ultra=` param to `gems=` to match the updated OG route
    - Update the signOgParams call if it still passes ultra count — pass gem score instead
    - Update any display text that says "rarity" to "gem" context

    **4. Run full test suite to verify nothing is broken:**

    ```bash
    cd apps/web && npx vitest run --reporter=verbose
    ```

    Fix any remaining TypeScript errors or test failures from the gem migration.
  </action>
  <verify>
    <automated>cd apps/web && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/lib/gamification/badge-awards.ts contains threshold `3.0` (or `3`) for rare_find badge (not `2.0`)
    - apps/web/src/actions/engagement.ts contains `quartz_collector` or `diamond_chaser` label
    - apps/web/src/app/(protected)/(profile)/perfil/_components/rarity-card-modal.tsx contains `gems=` or `Gem Card`
    - Full test suite (`npx vitest run`) exits 0
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>
    Badge award threshold updated to Safira (>= 3.0). Digger DNA labels use gem terminology. RarityCardModal shows gem data. Full test suite passes. TypeScript compiles cleanly.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of complete gem economy across all surfaces</name>
  <files>n/a</files>
  <action>
    This is a human verification checkpoint. The executor should present the verification steps to the user and wait for approval.

    Complete gem economy system: 6-tier gem badges (Quartzo through Diamante) replacing the old 3-tier rarity pills across every surface, gem-weighted ranking system, GemVault profile visualization, gem tier change notifications in import pipeline, and visual effects (hover glows, prismatic Diamante animation).

    Verification steps:
    1. Start the dev server: `cd apps/web && pnpm dev`
    2. Navigate to a collection page (/perfil?tab=collection)
       - Verify records show gem badges (Quartzo/Ametista/Esmeralda/Rubi/Safira/Diamante) instead of old "Common/Rare/Ultra Rare" pills
       - Hover over each gem tier badge and verify visual effects:
         - Quartzo: no effect (static)
         - Ametista: subtle purple glow
         - Esmeralda: green shimmer slides across
         - Rubi: warm red pulse
         - Safira: blue glow + sparkle particles
         - Diamante: prismatic rainbow animation (always active, not just hover)
    3. Navigate to profile About tab (/perfil?tab=about)
       - Verify GemVault section shows distribution bar with colored segments
       - Verify total Gem Score displayed with animation
       - Verify RankCard shows "Gem Score" label (not "Rarity")
    4. Navigate to Explorar leaderboard tab
       - Verify scores show "{N} gems" format (not "{N}pts")
    5. Open a release page (/release/{id})
       - Verify gem badge appears on the release hero
    6. Check Explorar browse grid and feed — gem badges on cards
  </action>
  <verify>User types "approved" or describes issues</verify>
  <done>User confirms all gem badges, GemVault, leaderboard format, and visual effects render correctly across all surfaces.</done>
</task>

</tasks>

<verification>
- Full test suite green: `cd apps/web && npx vitest run --reporter=verbose`
- TypeScript clean: `cd apps/web && npx tsc --noEmit`
- No remaining rarity references in app code: `grep -rn "getRarityTier\|RarityPill" apps/web/src/app/`
- Visual confirmation by user
</verification>

<success_criteria>
- Badge awards use Safira/Diamante threshold
- Digger DNA labels match gem terminology
- RarityCardModal/OG image show gem data
- All tests pass, TypeScript compiles
- User visually confirms gem badges, GemVault, leaderboard format, and visual effects
</success_criteria>

<output>
After completion, create `.planning/phases/20-gem-economy/20-05-SUMMARY.md`
</output>
