# Phase 34 — Evidence 06: Vault secrets populated
# Date: 2026-04-26
# Captured via: mcp__supabase__execute_sql

## Insertion (via public.vault_create_secret wrapper from migration 20260424000000)

SELECT public.vault_create_secret(
  'https://swyfhpgerzvvmoswkjyt.supabase.co',
  'trade_preview_project_url',
  'Phase 34 Plan 03: project URL used by trade-preview-cleanup cron to invoke the Edge Function via pg_net'
) AS secret_id;
-- secret_id: cd463fe3-f592-4e46-a481-1c7cfb1d5478

SELECT public.vault_create_secret(
  'sb_publishable_IeeRHNQVnO_06UUNfWdk_Q_cQAO5xXS',
  'trade_preview_publishable_key',
  'Phase 34 Plan 03: publishable key used by trade-preview-cleanup cron as Authorization Bearer when invoking the Edge Function'
) AS secret_id;
-- secret_id: 479aec8a-3de5-4c87-b62c-bb917c24f6e1

## Verify-only query (does NOT leak decrypted secret values)

SELECT name, length(decrypted_secret) AS secret_len, description
FROM vault.decrypted_secrets
WHERE name IN ('trade_preview_project_url', 'trade_preview_publishable_key')
ORDER BY name;

## Result

| name                          | secret_len | description |
|-------------------------------|-----------:|-------------|
| trade_preview_project_url     |         40 | Phase 34 Plan 03: project URL used by trade-preview-cleanup cron to invoke the Edge Function via pg_net |
| trade_preview_publishable_key |         46 | Phase 34 Plan 03: publishable key used by trade-preview-cleanup cron as Authorization Bearer when invoking the Edge Function |

## Length-based sanity (no value disclosure)

- Expected `trade_preview_project_url` length:
    "https://swyfhpgerzvvmoswkjyt.supabase.co" = 40 chars ✓
- Expected `trade_preview_publishable_key` length:
    "sb_publishable_IeeRHNQVnO_06UUNfWdk_Q_cQAO5xXS" = 46 chars ✓

Both lengths match expected values exactly. Decrypted values are NOT written to evidence
(by design — leaving them encrypted-at-rest in Vault and OAuth-scoped readable only via
service_role).

## Pitfall #11 timing satisfied

Vault was populated at 2026-04-26 ~16:05 (UTC-3 / 19:05 UTC). The next scheduled
execution of the `trade-preview-cleanup` cron job (every hour at minute 0) will be
20:00 UTC, well after the secrets are persisted. The cron function's guard:

  IF coalesce(trim(project_url), '') = '' OR coalesce(trim(publishable_key), '') = '' THEN
    RAISE NOTICE 'Skipping trade preview cleanup: ...';
    RETURN;
  END IF;

will pass on the next tick (both values populated, neither empty). DEP-SB-06 satisfied.

DEP-SB-06 satisfied: 2 named secrets exist in Vault, both invocable via the public wrapper.
