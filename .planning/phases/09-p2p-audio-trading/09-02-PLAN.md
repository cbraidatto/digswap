---
phase: 09-p2p-audio-trading
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/app/(protected)/trades/layout.tsx
  - src/app/(protected)/trades/page.tsx
  - src/app/(protected)/trades/_components/p2p-disabled-banner.tsx
  - src/app/(protected)/trades/_components/tos-modal.tsx
  - src/app/(protected)/trades/_components/trade-inbox.tsx
  - src/app/(protected)/trades/_components/trade-row.tsx
  - src/app/(protected)/trades/_components/trade-quota-counter.tsx
autonomous: true
requirements:
  - SEC-05
  - SEC-06
  - P2P-01

must_haves:
  truths:
    - "When P2P_ENABLED is false, the entire /trades section shows a compliance notice banner instead of the inbox"
    - "When user has not accepted ToS, a blocking modal appears before any trade action"
    - "User can see trade inbox with PENDING / ACTIVE / COMPLETED tabs"
    - "Each trade row displays [STATUS] . COUNTERPARTY . RECORD_TITLE . DATE . RATING"
    - "Free users see their trade quota counter (N/5) at the top of the inbox"
  artifacts:
    - path: "src/app/(protected)/trades/layout.tsx"
      provides: "Trades layout with P2P_ENABLED server-side gate"
      contains: "isP2PEnabled"
    - path: "src/app/(protected)/trades/page.tsx"
      provides: "Trade inbox server component"
      contains: "TradeInbox"
    - path: "src/app/(protected)/trades/_components/p2p-disabled-banner.tsx"
      provides: "Terminal error-style DMCA compliance banner"
      contains: "P2P_DISABLED"
    - path: "src/app/(protected)/trades/_components/tos-modal.tsx"
      provides: "Blocking ToS acceptance modal"
      contains: "TERMS_OF_SERVICE"
    - path: "src/app/(protected)/trades/_components/trade-inbox.tsx"
      provides: "Client component with PENDING/ACTIVE/COMPLETED tabs"
      contains: "PENDING"
    - path: "src/app/(protected)/trades/_components/trade-row.tsx"
      provides: "Single trade display row"
      contains: "TradeRow"
  key_links:
    - from: "src/app/(protected)/trades/layout.tsx"
      to: "src/lib/trades/constants.ts"
      via: "isP2PEnabled() import"
      pattern: "isP2PEnabled"
    - from: "src/app/(protected)/trades/page.tsx"
      to: "src/lib/trades/queries.ts"
      via: "getTradeInbox, getTradeCountThisMonth"
      pattern: "getTradeInbox"
    - from: "src/app/(protected)/trades/_components/tos-modal.tsx"
      to: "src/actions/trades.ts"
      via: "acceptToS server action"
      pattern: "acceptToS"
---

<objective>
Build the trades section shell: compliance gate (P2P_ENABLED layout + P2PDisabledBanner), ToS acceptance modal, trade inbox page with PENDING/ACTIVE/COMPLETED tabs, trade row component, and trade quota counter. Also install the shadcn `tabs` component needed by the inbox.

Purpose: This establishes the /trades section that all other trade pages live within. The layout enforces SEC-05 (DMCA gate) server-side. The ToS modal enforces SEC-06. The inbox is the primary navigation hub for all trades.
Output: Working /trades page with tabs, compliance gates, and empty states.
</objective>

<execution_context>
@C:\Users\INTEL\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\INTEL\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-p2p-audio-trading/09-CONTEXT.md
@.planning/phases/09-p2p-audio-trading/09-UI-SPEC.md
@.planning/phases/09-p2p-audio-trading/09-01-SUMMARY.md

@src/lib/trades/constants.ts
@src/lib/trades/queries.ts
@src/actions/trades.ts
@src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install shadcn tabs + trades layout with P2P gate + P2PDisabledBanner + ToS modal</name>
  <files>
    src/app/(protected)/trades/layout.tsx,
    src/app/(protected)/trades/_components/p2p-disabled-banner.tsx,
    src/app/(protected)/trades/_components/tos-modal.tsx
  </files>
  <read_first>
    src/app/(protected)/trades/new/page.tsx,
    src/app/globals.css,
    src/lib/trades/constants.ts,
    src/actions/trades.ts,
    src/lib/db/schema/users.ts,
    src/components/ui/dialog.tsx
  </read_first>
  <action>
**0. Install shadcn tabs:**
Run `npx shadcn@latest add tabs` to install the tabs component.

**1. Create src/app/(protected)/trades/layout.tsx** — Server component:
- Import `isP2PEnabled` from `@/lib/trades/constants`
- Import `createClient` from `@/lib/supabase/server`
- If `!isP2PEnabled()`, render `<P2PDisabledBanner />` instead of children (D-03 server-side enforcement per SEC-05)
- If P2P enabled, fetch user profile to check `trades_tos_accepted_at`:
  ```
  const supabase = await createClient();
  const { data: { user } } = await supabase.auth.getUser();
  // Fetch profile trades_tos_accepted_at
  ```
- Pass `tosAccepted: boolean` prop to client wrapper that conditionally renders ToS modal
- Layout wrapper: `<div className="min-h-screen">{/* conditionally: P2PDisabledBanner OR tosGatedChildren */}</div>`

**2. Create src/app/(protected)/trades/_components/p2p-disabled-banner.tsx** — Server component (per UI-SPEC layout):
```
max-w-4xl mx-auto px-4 md:px-8 py-16
```
Inner card:
```
border-l-4 border-destructive bg-surface-container-low p-6 rounded-r-lg
```
- Material icon: `shield_lock` with `text-destructive text-4xl mb-4`
- Label: `[P2P_DISABLED]` in `text-[10px] font-mono text-destructive uppercase tracking-[0.2em]`
- Body text: "DMCA registration pending -- trading will be enabled once compliance infrastructure is operational." in `text-sm text-on-surface-variant font-sans`
- Copy matches UI-SPEC Copywriting Contract exactly

**3. Create src/app/(protected)/trades/_components/tos-modal.tsx** — Client component ("use client"):
- Uses shadcn `Dialog` (AlertDialog pattern per UI-SPEC: `role="alertdialog"` with `aria-modal="true"`)
- Open state controlled by prop `tosAccepted: boolean` — open when false, cannot be dismissed without acceptance
- Modal overlay: `bg-background/80 backdrop-blur-sm`
- Modal card: `max-w-lg mx-auto bg-surface-container-low rounded-xl border border-outline-variant/10 p-8`
- Icon: material `gavel` in `text-primary text-4xl`
- Title: `TERMS_OF_SERVICE` in `text-2xl font-bold font-heading uppercase`
- Body text (3 bullet points per UI-SPEC Copywriting Contract):
  1. "You own the copyright to or have the legal right to share any files you transfer."
  2. "You assume full responsibility for the content of files shared through this platform."
  3. "DigSwap acts as a mere conduit -- no file data is stored on or passes through our servers."
- Each bullet prefixed with a dash, styled `text-sm font-sans text-on-surface-variant`
- Checkbox: `font-mono text-xs` with label "I accept the Terms of Service"
- Button: `ACCEPT_AND_CONTINUE` full-width, `bg-primary-container text-on-primary-container`, disabled until checkbox is checked
- On accept: call `acceptToS()` server action from `@/actions/trades.ts`, then close modal
- Focus trapped within modal (Radix Dialog handles this)
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -c "error" || echo "0 errors"</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/ui/tabs.tsx` exists (shadcn tabs installed)
    - src/app/(protected)/trades/layout.tsx contains `isP2PEnabled` and `P2PDisabledBanner` and `trades_tos_accepted_at`
    - src/app/(protected)/trades/_components/p2p-disabled-banner.tsx contains `P2P_DISABLED` and `shield_lock` and `DMCA registration pending`
    - src/app/(protected)/trades/_components/tos-modal.tsx contains `"use client"` and `TERMS_OF_SERVICE` and `acceptToS` and `ACCEPT_AND_CONTINUE` and `gavel`
    - src/app/(protected)/trades/_components/tos-modal.tsx contains `I accept the Terms of Service` (checkbox label per UI-SPEC)
  </acceptance_criteria>
  <done>Trades layout enforces P2P gate server-side, shows compliance banner when disabled, shows ToS modal for first-time traders</done>
</task>

<task type="auto">
  <name>Task 2: Trade inbox page with tabs + TradeRow + TradeQuotaCounter</name>
  <files>
    src/app/(protected)/trades/page.tsx,
    src/app/(protected)/trades/_components/trade-inbox.tsx,
    src/app/(protected)/trades/_components/trade-row.tsx,
    src/app/(protected)/trades/_components/trade-quota-counter.tsx
  </files>
  <read_first>
    src/app/(protected)/trades/layout.tsx,
    src/lib/trades/queries.ts,
    src/lib/trades/constants.ts,
    src/components/ui/tabs.tsx,
    src/components/ui/badge.tsx,
    src/components/ui/skeleton.tsx,
    src/app/(protected)/(explore)/explorar/page.tsx
  </read_first>
  <action>
**1. Create src/app/(protected)/trades/page.tsx** — Server component:
- Breadcrumb: `Protocol / Trades` in `text-[10px] font-mono text-primary tracking-[0.2em] uppercase`
- Title: `TRADE_INBOX` in `text-3xl font-bold font-heading uppercase`
- Subtitle: `Manage your P2P audio exchanges.` in `text-xs font-mono text-on-surface-variant`
- Fetch user auth via `createClient()`
- Fetch trade data: call `getTradeInbox(userId, "pending")` for initial data
- Fetch trade count: call `getTradeCountThisMonth(userId)` for quota display
- Fetch subscription plan from subscriptions table
- Pass data as props to `<TradeInbox>` client component
- Pass quota data as props to `<TradeQuotaCounter>`
- Page layout: `max-w-4xl mx-auto px-4 md:px-8 py-8`

**2. Create src/app/(protected)/trades/_components/trade-inbox.tsx** — Client component ("use client"):
- Uses shadcn `Tabs`, `TabsList`, `TabsTrigger`, `TabsContent` from `@/components/ui/tabs`
- Three tabs: PENDING, ACTIVE, COMPLETED (text-[10px] font-mono uppercase per UI-SPEC)
- Default tab from URL searchParam `?tab=pending` (per UI-SPEC Interaction Spec: URL updates to `?tab=pending|active|completed`)
- Tab switching uses `window.history.replaceState` for URL update without re-render (same pattern as Phase 8 explorar tabs per STATE.md decision)
- Tab counts shown as badge after label: `PENDING (3)` — fetch counts from server data passed as props
- Each tab renders a list of `<TradeRow>` components
- Loading state: 3 skeleton rows per tab (56px height each per UI-SPEC)
- Empty states per UI-SPEC Copywriting Contract:
  - Pending: "No pending trade requests. Request audio from a digger's collection to get started."
  - Active: "No active transfers. Accept a pending request to start a live session."
  - Completed: "No completed trades yet. Your trade history will appear here."
- Global empty state (no trades at all): heading "NO_TRADES_YET", body "Start by requesting audio from another digger's collection. Browse records on Explorar or visit a digger's profile to initiate a trade."

**3. Create src/app/(protected)/trades/_components/trade-row.tsx** — Client component:
- Per D-17 and UI-SPEC TradeRow Specification:
  - Height: 56px (`h-14`), background: `bg-surface-container-low`, border: `border border-outline-variant/10 rounded-lg`
  - Content format: `[STATUS] . COUNTERPARTY . RECORD_TITLE . DATE . RATING`
  - Row gap: `gap-2`
  - Vertical spacing between rows: `space-y-2`
- Status badge uses color mapping from UI-SPEC:
  - PENDING: `text-tertiary bg-tertiary/10 border-tertiary/20`
  - ACCEPTED: `text-secondary bg-secondary/10 border-secondary/20`
  - TRANSFERRING: `text-primary bg-primary/10 border-primary/20`
  - COMPLETED: `text-primary bg-primary/10 border-primary/20`
  - DECLINED: `text-on-surface-variant bg-surface-container-high`
  - CANCELLED: `text-on-surface-variant bg-surface-container-high`
  - EXPIRED: `text-destructive bg-destructive/10 border-destructive/20`
- All text segments: `font-mono text-[10px]`
- Counterparty: `text-secondary`, record title: `text-on-surface flex-1 truncate`, date: `text-on-surface-variant` (relative time), rating: `text-secondary` (stars, completed only)
- Dot separators: `text-outline` inline
- Clicking the row navigates to `/trades/[id]` for pending/active trades, `/trades/[id]/complete` for completed trades
- Wrap rows in `<ul role="list">`, each row in `<li role="listitem">` per UI-SPEC accessibility

**4. Create src/app/(protected)/trades/_components/trade-quota-counter.tsx** — Client component:
- Per UI-SPEC: `TRADES_THIS_MONTH: 2/5 [====------]` format
- Shows `text-[10px] font-mono` label and a progress bar segment
- Free users: show `N/5` with progress bar
- Premium users: show `UNLIMITED` text
- When quota reached (5/5), show `text-tertiary` warning color per UI-SPEC
- When trades >= 4, show `text-tertiary` (approaching limit per UI-SPEC color spec)
- Only rendered for free users (premium sees nothing or "UNLIMITED")
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -c "error" || echo "0 errors"</automated>
  </verify>
  <acceptance_criteria>
    - src/app/(protected)/trades/page.tsx contains `TRADE_INBOX` and `getTradeInbox` and `TradeInbox`
    - src/app/(protected)/trades/_components/trade-inbox.tsx contains `"use client"` and `TabsList` and `PENDING` and `ACTIVE` and `COMPLETED` and `NO_TRADES_YET`
    - src/app/(protected)/trades/_components/trade-row.tsx contains `"use client"` and `TradeRow` and `text-tertiary bg-tertiary/10` and `text-secondary bg-secondary/10` and `text-primary bg-primary/10`
    - src/app/(protected)/trades/_components/trade-row.tsx contains `role="list"` or component renders within a `ul` with `role="list"`
    - src/app/(protected)/trades/_components/trade-quota-counter.tsx contains `TRADES_THIS_MONTH` and `UNLIMITED`
  </acceptance_criteria>
  <done>Trade inbox page renders with 3 tabs, trade rows with correct status colors, quota counter, and empty states</done>
</task>

</tasks>

<verification>
- Navigate to `/trades` — see P2PDisabledBanner when P2P_ENABLED is not set
- Set P2P_ENABLED=true — see ToS modal on first visit, then trade inbox after acceptance
- Inbox shows PENDING/ACTIVE/COMPLETED tabs with empty states
- `npx tsc --noEmit` passes
</verification>

<success_criteria>
- /trades shows P2PDisabledBanner when P2P_ENABLED is false (SEC-05)
- /trades shows ToS modal when trades_tos_accepted_at is null (SEC-06)
- /trades shows trade inbox with 3 tabs and empty states
- Trade rows display in correct [STATUS] . COUNTERPARTY . RECORD format
- Free users see trade quota counter
- shadcn tabs component installed
</success_criteria>

<output>
After completion, create `.planning/phases/09-p2p-audio-trading/09-02-SUMMARY.md`
</output>
