---
phase: 09-p2p-audio-trading
plan: 05
type: execute
wave: 3
depends_on: ["09-01", "09-02"]
files_modified:
  - src/app/(protected)/trades/[id]/review/page.tsx
  - src/app/(protected)/trades/[id]/review/_components/spec-analysis.tsx
  - src/app/(protected)/trades/[id]/review/_components/spectrogram-canvas.tsx
  - src/app/(protected)/trades/[id]/complete/page.tsx
  - src/app/(protected)/trades/[id]/complete/_components/trade-rating-form.tsx
  - src/app/(protected)/(profile)/perfil/page.tsx
  - src/app/(protected)/(profile)/perfil/[username]/page.tsx
autonomous: true
requirements:
  - P2P-05
  - P2P-06
  - P2P-07

must_haves:
  truths:
    - "Recipient can view audio spectrum analysis after receiving a file"
    - "Recipient can rate audio quality (1-5 stars + optional comment)"
    - "Rating updates the sharer's reputation score"
    - "User's trade reputation (total trades + average rating) is visible on their profile"
    - "Trade complete page shows +15 pts contribution, not +150 XP"
    - "Free users get 1 spectrum analysis per trade; premium get unlimited"
  artifacts:
    - path: "src/app/(protected)/trades/[id]/review/page.tsx"
      provides: "Spec check page with real audio analysis"
      contains: "SpecAnalysis"
    - path: "src/app/(protected)/trades/[id]/review/_components/spec-analysis.tsx"
      provides: "Web Audio API analysis with spectrogram"
      contains: "AudioContext"
    - path: "src/app/(protected)/trades/[id]/review/_components/spectrogram-canvas.tsx"
      provides: "Canvas FFT rendering"
      contains: "canvas"
    - path: "src/app/(protected)/trades/[id]/complete/page.tsx"
      provides: "Trade complete page with rating form"
      contains: "TradeRatingForm"
    - path: "src/app/(protected)/trades/[id]/complete/_components/trade-rating-form.tsx"
      provides: "Star rating + comment + server action submission"
      contains: "SUBMIT_REVIEW"
  key_links:
    - from: "src/app/(protected)/trades/[id]/complete/_components/trade-rating-form.tsx"
      to: "src/actions/trades.ts"
      via: "completeTrade server action"
      pattern: "completeTrade"
    - from: "src/app/(protected)/trades/[id]/review/_components/spec-analysis.tsx"
      to: "src/lib/audio/spectrum-analyzer.ts"
      via: "renderSpectrogram import"
      pattern: "renderSpectrogram"
    - from: "src/app/(protected)/(profile)/perfil/page.tsx"
      to: "src/lib/trades/queries.ts"
      via: "getTradeReputation query"
      pattern: "getTradeReputation"
---

<objective>
Wire up the existing review and complete page stubs with real functionality: spectrum analysis using Web Audio API, trade rating form with star rating and comment, and trade reputation display on user profiles. Also integrate the spectrogram canvas for audio visualization.

Purpose: Completes the post-transfer experience (P2P-05, P2P-06, P2P-07). Recipients can verify audio quality via spectrogram, rate the file, and the sharer's reputation is visible on profiles.
Output: Working review page with spectrogram, working complete page with rating, reputation stat on profiles.
</objective>

<execution_context>
@C:\Users\INTEL\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\INTEL\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-p2p-audio-trading/09-CONTEXT.md
@.planning/phases/09-p2p-audio-trading/09-UI-SPEC.md
@.planning/phases/09-p2p-audio-trading/09-RESEARCH.md
@.planning/phases/09-p2p-audio-trading/09-01-SUMMARY.md

@src/app/(protected)/trades/[id]/review/page.tsx
@src/app/(protected)/trades/[id]/complete/page.tsx
@src/lib/audio/spectrum-analyzer.ts
@src/lib/audio/file-metadata.ts
@src/lib/trades/queries.ts
@src/actions/trades.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire review page with SpecAnalysis and SpectrogramCanvas</name>
  <files>
    src/app/(protected)/trades/[id]/review/page.tsx,
    src/app/(protected)/trades/[id]/review/_components/spec-analysis.tsx,
    src/app/(protected)/trades/[id]/review/_components/spectrogram-canvas.tsx
  </files>
  <read_first>
    src/app/(protected)/trades/[id]/review/page.tsx,
    src/lib/audio/spectrum-analyzer.ts,
    src/lib/audio/file-metadata.ts,
    src/lib/trades/queries.ts,
    src/actions/trades.ts,
    src/lib/trades/constants.ts
  </read_first>
  <action>
**1. Rewrite src/app/(protected)/trades/[id]/review/page.tsx** — Server component:
- Keep the overall page structure (breadcrumb, title SPEC_CHECK, 2-column grid) but replace hardcoded SPEC_CHECKS data and placeholder content with real data
- Auth check: `createClient().auth.getUser()` — redirect if not auth'd
- Fetch trade: `getTradeById(params.id, userId)` — verify participant, redirect to `/trades` if unauthorized
- Fetch subscription for premium check
- Remove the hardcoded `SPEC_CHECKS` array at the top of the file
- Replace `[PHASE_9_PENDING]` labels with real content
- Pass trade data to `<SpecAnalysis>` client component:
  ```
  tradeId, declaredMetadata (from trade request: fileFormat, declaredBitrate, fileName), isPremium: boolean
  ```
- The ACCEPT_TRADE button calls `acceptTrade` or `updateTradeStatus` server action then redirects to `/trades/${tradeId}/complete`
- The REJECT_TRADE button shows AlertDialog confirmation then calls `declineTrade` server action, redirects to `/trades`

**2. Create src/app/(protected)/trades/[id]/review/_components/spec-analysis.tsx** — Client component ("use client"):
Per UI-SPEC Spec Check / Review specification:

- **Audio_Preview card** (left column):
  - Play/pause button: `w-11 h-11` (44px minimum per UI-SPEC accessibility: WCAG 2.5.5)
  - `bg-primary-container rounded-full flex items-center justify-center text-on-primary-container`
  - Progress bar for playback position
  - Duration display: `font-mono text-xs text-on-surface-variant`
  - Uses `<audio>` element controlled by state
  - The audio file comes from the lobby transfer (receiver saved as Blob). Since the blob lives in browser memory only, this page receives it via:
    - Option A: URL query param referencing an IndexedDB key where the lobby stored the received file
    - Option B: Zustand store persisting the received blob across page navigation
    - Use Zustand store approach: create a minimal `useReceivedFileStore` in the same file or a shared location. The lobby sets the file blob + name in the store, and the review page reads it.
  - Create AudioContext inside the play button onClick handler (per Pitfall 6: user gesture required)
  - Call `audioContext.resume()` if suspended

- **Spectrogram_Analysis card** (right column):
  - Render `<SpectrogramCanvas>` component
  - Freemium gate (per D-22):
    - Free users: spectrogram renders once automatically. After first render, overlay with premium gate
    - Premium users: can click "Re-analyze" button to regenerate
    - Premium overlay: `bg-surface-container-lowest/90 backdrop-blur-sm` with lock icon and text "[PREMIUM_FEATURE] Upgrade to re-analyze"
  - Canvas container: `bg-surface-container-lowest rounded-lg aspect-video` (16:9)

- **Metadata_Verification table** (full width below grid):
  - Keep existing 4-column grid layout from stub
  - Header: `Metadata_Verification` with verified icon
  - Columns: LABEL, DECLARED (from trade request), ACTUAL (from Web Audio API analysis), [PASS]/[FAIL]
  - Rows: FORMAT, BITRATE, DURATION, CHANNELS (MD5_HASH shows "N/A" for actual — not computable client-side from decoded audio)
  - Populate DECLARED from trade request metadata (props)
  - Populate ACTUAL by calling `analyzeAudioFile(blob)` from `@/lib/audio/file-metadata` when audio file is available
  - Comparison logic: FORMAT passes if declared matches detected, DURATION passes if within 1s, CHANNELS always passes if detected
  - [PASS] in `text-primary`, [FAIL] in `text-destructive` (per UI-SPEC color spec)

**3. Create src/app/(protected)/trades/[id]/review/_components/spectrogram-canvas.tsx** — Client component ("use client"):
- Renders a `<canvas>` element with `aspect-video` aspect ratio
- Props: `audioElement: HTMLAudioElement | null, isActive: boolean`
- When isActive and audioElement provided: calls `renderSpectrogram(canvas, audioElement)` from `@/lib/audio/spectrum-analyzer`
- Canvas background: `--surface-container-lowest` CSS variable
- Spectrogram colors: gradient from dark (silence) to `--primary` green (peak amplitude)
- Uses requestAnimationFrame loop (from spectrum-analyzer.ts)
- Cleanup: stop animation frame and close AudioContext on unmount
- Respect `prefers-reduced-motion`: if reduced motion preferred, render a static frequency snapshot instead of scrolling spectrogram
- Canvas sized to fill container via `width: 100%, height: 100%` with `style={{ width: '100%', height: '100%' }}`
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -c "error" || echo "0 errors"</automated>
  </verify>
  <acceptance_criteria>
    - src/app/(protected)/trades/[id]/review/page.tsx does NOT contain `PHASE_9_PENDING`
    - src/app/(protected)/trades/[id]/review/page.tsx contains `SpecAnalysis` and `getTradeById` and `isPremium`
    - src/app/(protected)/trades/[id]/review/_components/spec-analysis.tsx contains `"use client"` and `AudioContext` and `analyzeAudioFile` and `PREMIUM_FEATURE` and `Metadata_Verification`
    - src/app/(protected)/trades/[id]/review/_components/spec-analysis.tsx contains `w-11 h-11` (44px play button per WCAG 2.5.5)
    - src/app/(protected)/trades/[id]/review/_components/spectrogram-canvas.tsx contains `"use client"` and `canvas` and `renderSpectrogram` and `requestAnimationFrame` or delegates to spectrum-analyzer
    - src/app/(protected)/trades/[id]/review/_components/spectrogram-canvas.tsx contains `prefers-reduced-motion` or checks for reduced motion preference
  </acceptance_criteria>
  <done>Review page shows real audio preview, live spectrogram, metadata comparison table with pass/fail, freemium gate on spectrogram</done>
</task>

<task type="auto">
  <name>Task 2: Wire complete page with TradeRatingForm + profile reputation stat</name>
  <files>
    src/app/(protected)/trades/[id]/complete/page.tsx,
    src/app/(protected)/trades/[id]/complete/_components/trade-rating-form.tsx,
    src/app/(protected)/(profile)/perfil/page.tsx,
    src/app/(protected)/(profile)/perfil/[username]/page.tsx
  </files>
  <read_first>
    src/app/(protected)/trades/[id]/complete/page.tsx,
    src/app/(protected)/(profile)/perfil/page.tsx,
    src/app/(protected)/(profile)/perfil/[username]/page.tsx,
    src/lib/trades/queries.ts,
    src/actions/trades.ts,
    src/lib/gamification/constants.ts
  </read_first>
  <action>
**1. Rewrite src/app/(protected)/trades/[id]/complete/page.tsx** — Server component:
- Keep the overall page structure (success animation, title TRADE_COMPLETE, trade summary, rating section)
- Auth check + fetch trade data via `getTradeById(params.id, userId)` — redirect if not participant
- Replace hardcoded trade summary values with real data:
  - TRADE_ID: `vnl-#${trade.id.substring(0, 3)}` or the actual trade ID prefix
  - COUNTERPARTY: real counterparty username from trade data
  - FILES_TRANSFERRED: `[CONFIRMED]` in `text-primary`
  - **Replace `XP_EARNED: +150 XP` with `CONTRIBUTION: +15 pts`** in `text-tertiary` (per D-24 and CONTEXT.md specifics — CONTRIBUTION_POINTS.trade_completed = 15, not XP)
- Keep the success animation area (merge icon, radial glow) as-is
- Keep the status label `STATUS / MERGED` format
- Replace the static star buttons and comment input with `<TradeRatingForm>` client component
- Pass trade data as props: `tradeId, counterpartyUsername`

**2. Create src/app/(protected)/trades/[id]/complete/_components/trade-rating-form.tsx** — Client component ("use client"):
Per UI-SPEC Trade Complete specification:

- **Star rating section:**
  - Heading: `Rate_File_Quality` in `text-xs font-mono uppercase tracking-widest text-on-surface-variant`
  - 5 stars in a row, each `w-10 h-10` (40x40px per UI-SPEC, with 4px gap = 44px effective touch target per WCAG 2.5.5)
  - Uses `role="radiogroup"` with `aria-label="Audio quality rating"` (UI-SPEC accessibility)
  - Each star: `role="radio"` with `aria-checked`
  - Hover: stars up to hovered position fill with `text-secondary`
  - Click: locks selection, filled stars stay `text-secondary`, unfilled stay `text-on-surface-variant`
  - Stars navigable via arrow keys (keyboard accessibility per UI-SPEC)
  - React state: `selectedRating: number | null`, `hoveredRating: number | null`

- **Comment input:**
  - Placeholder: "Add a comment (optional)..." (per UI-SPEC Copywriting Contract)
  - Style: `w-full bg-surface-container-lowest border-none text-xs font-mono text-on-surface placeholder:text-on-surface-variant/40 rounded p-3 outline-none border-l-2 border-primary/30 focus:border-primary transition-colors`
  - Optional field

- **Two action buttons** (per UI-SPEC):
  - `SUBMIT_REVIEW`: `flex-1 py-3 bg-primary-container text-on-primary-container font-mono text-sm font-bold rounded hover:brightness-110 transition-all`
    - Disabled until rating selected (button disabled without star selection per UI-SPEC interaction)
    - On click: call `completeTrade(tradeId, selectedRating, comment)` server action
    - Loading state: "SUBMITTING..." with disabled
    - On success: redirect to `/trades` inbox via `router.push("/trades")`, show success toast via sonner
    - On error: show error toast
  - `SKIP_REVIEW`: `flex-1 py-3 bg-surface-container-high text-on-surface-variant font-mono text-sm rounded hover:bg-surface-bright transition-all border border-outline-variant/20`
    - On click: call `skipReview(tradeId)` server action (completes trade without review), redirect to `/trades`

**3. Modify src/app/(protected)/(profile)/perfil/page.tsx** — Add trade reputation stat line:
- Read the current file fully first
- Import `getTradeReputation` from `@/lib/trades/queries`
- In the server component, after fetching existing profile data, also fetch `getTradeReputation(userId)`
- In the profile header area (where existing stats like followers/following/records are displayed), add trade reputation line when `totalTrades > 0`:
  ```
  TRADES: {totalTrades} . AVG: {averageRating.toFixed(1)} [star icon]
  ```
- Style per UI-SPEC and D-23:
  - Container: `font-mono text-[10px] text-on-surface-variant`
  - Trade count number: `text-primary`
  - Average rating number: `text-secondary`
  - Star icon: `text-secondary` (material `star` at 14px)
- Only shown when `totalTrades > 0` (per UI-SPEC)

**4. Modify src/app/(protected)/(profile)/perfil/[username]/page.tsx** — Add trade reputation stat on public profiles:
- Read the current file fully first
- Same as own profile: import and call `getTradeReputation(profileUserId)`, display trade stat line in profile header
- Same style and conditional rendering as own profile
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -c "error" || echo "0 errors"</automated>
  </verify>
  <acceptance_criteria>
    - src/app/(protected)/trades/[id]/complete/page.tsx does NOT contain `+150 XP` (replaced with contribution points)
    - src/app/(protected)/trades/[id]/complete/page.tsx contains `CONTRIBUTION` and `+15 pts` or `CONTRIBUTION_POINTS.trade_completed`
    - src/app/(protected)/trades/[id]/complete/page.tsx contains `TradeRatingForm` and `getTradeById`
    - src/app/(protected)/trades/[id]/complete/_components/trade-rating-form.tsx contains `"use client"` and `SUBMIT_REVIEW` and `SKIP_REVIEW` and `completeTrade`
    - src/app/(protected)/trades/[id]/complete/_components/trade-rating-form.tsx contains `role="radiogroup"` and `aria-label="Audio quality rating"` (accessibility)
    - src/app/(protected)/trades/[id]/complete/_components/trade-rating-form.tsx contains `w-10 h-10` (star button size per UI-SPEC)
    - src/app/(protected)/(profile)/perfil/page.tsx contains `getTradeReputation`
    - src/app/(protected)/(profile)/perfil/page.tsx contains `TRADES:` and `AVG:` (reputation stat line per D-23)
    - src/app/(protected)/(profile)/perfil/[username]/page.tsx contains `getTradeReputation`
  </acceptance_criteria>
  <done>Trade complete page shows +15 pts contribution, rating form with accessible stars, reputation stat on both own and public profiles</done>
</task>

</tasks>

<verification>
- Navigate to `/trades/[id]/review` for a completed trade — see spectrogram, metadata table, accept/reject buttons
- Navigate to `/trades/[id]/complete` — see CONTRIBUTION: +15 pts (not XP), star rating, submit/skip buttons
- Visit own profile — see TRADES: N . AVG: N.N star stat line (if trades exist)
- Visit public profile — see trade reputation stat
- `npx tsc --noEmit` passes
</verification>

<success_criteria>
- Review page has working audio preview, live spectrogram, metadata comparison with pass/fail
- Free users see premium overlay after first spectrogram analysis
- Complete page shows +15 pts contribution (not +150 XP)
- Star rating is accessible (radiogroup, aria-checked, keyboard navigable)
- Trade reputation visible on own and public profiles when trades > 0
- Both completeTrade and skipReview paths work
</success_criteria>

<output>
After completion, create `.planning/phases/09-p2p-audio-trading/09-05-SUMMARY.md`
</output>
