---
phase: 09-p2p-audio-trading
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/db/schema/trades.ts
  - src/lib/db/schema/users.ts
  - src/lib/trades/constants.ts
  - src/lib/trades/queries.ts
  - src/lib/webrtc/chunked-transfer.ts
  - src/lib/audio/spectrum-analyzer.ts
  - src/lib/audio/file-metadata.ts
  - src/actions/trades.ts
  - tests/unit/trades/create-trade.test.ts
  - tests/unit/trades/trade-review.test.ts
  - tests/unit/trades/trade-reputation.test.ts
  - tests/unit/trades/p2p-gate.test.ts
  - tests/unit/trades/tos-gate.test.ts
  - tests/unit/trades/turn-credentials.test.ts
  - tests/unit/trades/chunked-transfer.test.ts
  - tests/unit/trades/trade-counter.test.ts
autonomous: true
requirements:
  - P2P-01
  - P2P-02
  - P2P-03
  - P2P-05
  - P2P-06
  - SEC-05
  - SEC-06
  - SEC-07

must_haves:
  truths:
    - "Trade requests table has columns for file metadata, expiry, and all status values"
    - "Profiles table has trades_tos_accepted_at column for ToS gate"
    - "Server actions exist for full trade lifecycle (create, accept, decline, cancel, complete)"
    - "TURN credentials are fetched server-side via server action, never exposed client-side"
    - "Chunked transfer utility can slice a file into 64KB chunks and reassemble"
    - "Trade counter enforcement blocks free users at 5 trades/month"
    - "Wave 0 test scaffolds exist for all 8 test files per VALIDATION.md"
  artifacts:
    - path: "src/lib/db/schema/trades.ts"
      provides: "tradeRequests with expiresAt, fileName, fileFormat, declaredBitrate, fileSizeBytes columns"
      contains: "expiresAt"
    - path: "src/lib/db/schema/users.ts"
      provides: "profiles with trades_tos_accepted_at column"
      contains: "tradesTosAcceptedAt"
    - path: "src/lib/trades/constants.ts"
      provides: "CHUNK_SIZE, TRADE_STATUS, TRADE_EXPIRY_OPTIONS, isP2PEnabled"
      exports: ["CHUNK_SIZE", "TRADE_STATUS", "isP2PEnabled"]
    - path: "src/actions/trades.ts"
      provides: "createTrade, acceptTrade, declineTrade, cancelTrade, completeTrade, getTurnCredentials, acceptToS, getTradesRemaining"
      exports: ["createTrade", "acceptTrade", "completeTrade", "getTurnCredentials"]
    - path: "src/lib/trades/queries.ts"
      provides: "getTradeInbox, getTradeById, getTradeReputation"
      exports: ["getTradeInbox", "getTradeById", "getTradeReputation"]
    - path: "src/lib/webrtc/chunked-transfer.ts"
      provides: "sendFileChunked, receiveFileChunked, CHUNK_SIZE"
      exports: ["sendFileChunked", "receiveFileChunked"]
    - path: "tests/unit/trades/create-trade.test.ts"
      provides: "Test scaffold for P2P-01"
      contains: "describe"
  key_links:
    - from: "src/actions/trades.ts"
      to: "src/lib/db/schema/trades.ts"
      via: "Supabase admin client queries"
      pattern: "from.*trade_requests"
    - from: "src/actions/trades.ts"
      to: "src/lib/gamification/badge-awards.ts"
      via: "awardBadge call on completeTrade"
      pattern: "awardBadge.*connector"
    - from: "src/actions/trades.ts"
      to: "src/lib/trades/constants.ts"
      via: "imports TRADE_STATUS, isP2PEnabled"
      pattern: "import.*constants"
---

<objective>
Build the complete data foundation for P2P audio trading: schema migration (new columns on tradeRequests and profiles), trade constants/types, server actions for the full trade lifecycle, trade query functions, utility modules (chunked transfer, audio analysis), and all 8 Wave 0 test scaffolds per VALIDATION.md.

Purpose: Every subsequent plan depends on these server actions, types, and queries. Building them first means UI plans can import and wire without defining contracts.
Output: Migrated schema, complete server action module, query functions, WebRTC/audio utilities, 8 test scaffold files.
</objective>

<execution_context>
@C:\Users\INTEL\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\INTEL\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-p2p-audio-trading/09-CONTEXT.md
@.planning/phases/09-p2p-audio-trading/09-RESEARCH.md
@.planning/phases/09-p2p-audio-trading/09-VALIDATION.md

@src/lib/db/schema/trades.ts
@src/lib/db/schema/users.ts
@src/lib/db/schema/subscriptions.ts
@src/lib/db/schema/index.ts
@src/actions/social.ts
@src/lib/gamification/constants.ts
@src/lib/gamification/badge-awards.ts
@src/components/shell/notification-row.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema migration + trade constants + query functions</name>
  <files>
    src/lib/db/schema/trades.ts,
    src/lib/db/schema/users.ts,
    src/lib/trades/constants.ts,
    src/lib/trades/queries.ts,
    src/lib/webrtc/chunked-transfer.ts,
    src/lib/audio/spectrum-analyzer.ts,
    src/lib/audio/file-metadata.ts
  </files>
  <read_first>
    src/lib/db/schema/trades.ts,
    src/lib/db/schema/users.ts,
    src/lib/db/schema/subscriptions.ts,
    src/lib/db/schema/index.ts,
    src/actions/social.ts,
    src/lib/gamification/constants.ts
  </read_first>
  <action>
**1. Schema migration — src/lib/db/schema/trades.ts:**
Add these columns to the existing `tradeRequests` pgTable (per D-10, D-12, Pitfall 7 from RESEARCH.md):
- `expiresAt: timestamp("expires_at", { withTimezone: true })` — trade request expiry (D-12)
- `fileName: varchar("file_name", { length: 255 })` — declared file name
- `fileFormat: varchar("file_format", { length: 50 })` — declared format (FLAC, WAV, MP3, etc.)
- `declaredBitrate: varchar("declared_bitrate", { length: 50 })` — declared bitrate string
- `fileSizeBytes: integer("file_size_bytes")` — declared file size in bytes

Keep ALL existing columns, imports, RLS policies unchanged. Only ADD the new columns inside the same pgTable call.

**2. Schema migration — src/lib/db/schema/users.ts:**
Add to the existing `profiles` pgTable (per D-02):
- `tradesTosAcceptedAt: timestamp("trades_tos_accepted_at", { withTimezone: true })` — null = ToS not yet accepted

Keep ALL existing columns, imports, RLS policies unchanged.

**3. Create src/lib/trades/constants.ts:**
```typescript
export const CHUNK_SIZE = 64 * 1024; // 64KB per D-10

export const TRADE_STATUS = {
  PENDING: "pending",
  ACCEPTED: "accepted",
  TRANSFERRING: "transferring",
  COMPLETED: "completed",
  DECLINED: "declined",
  CANCELLED: "cancelled",
  EXPIRED: "expired",
} as const;

export type TradeStatus = (typeof TRADE_STATUS)[keyof typeof TRADE_STATUS];

export const TRADE_EXPIRY_OPTIONS = [
  { label: "6h", value: 6 },
  { label: "12h", value: 12 },
  { label: "24h", value: 24 },
  { label: "48h", value: 48 },
] as const;

export const DEFAULT_EXPIRY_HOURS = 48;

export const MAX_FREE_TRADES_PER_MONTH = 5;

export const ACCEPTED_AUDIO_TYPES = [
  "audio/flac",
  "audio/wav",
  "audio/mp3",
  "audio/mpeg",
  "audio/ogg",
  "audio/aac",
] as const;

export function isP2PEnabled(): boolean {
  return process.env.P2P_ENABLED === "true";
}
```

**4. Create src/lib/trades/queries.ts:**
Query functions using Supabase admin client (pattern from src/lib/social/queries.ts, src/lib/discovery/queries.ts):

- `getTradeInbox(userId: string, tab: "pending" | "active" | "completed")` — returns trade rows for the user filtered by tab. "pending" = status in ['pending', 'accepted'], "active" = status = 'transferring', "completed" = status in ['completed', 'declined', 'cancelled', 'expired']. Each row includes counterparty username/avatar via a join or separate query. Return type: `TradeInboxRow[]` with fields: `id, status, counterpartyUsername, counterpartyAvatarUrl, releaseTitle, releaseArtist, createdAt, qualityRating (null if no review)`.
- `getTradeById(tradeId: string, userId: string)` — returns full trade request row. Verifies userId is requester or provider (IDOR prevention). Includes release info and counterparty profile.
- `getTradeReputation(userId: string)` — returns `{ totalTrades: number, averageRating: number | null }` by querying tradeReviews where reviewedId = userId. Uses SQL avg() and count().
- `getTradeCountThisMonth(userId: string)` — returns `{ count: number, resetDate: string | null, plan: string }` from subscriptions table. Implements check-on-read reset: if `tradesMonthReset` is in a different calendar month than now, reset `tradesThisMonth` to 0 and update `tradesMonthReset` to now.

All queries use `createAdminClient()` from `@/lib/supabase/admin` (pattern from existing codebase — admin client for cross-user reads with RLS bypass).

**5. Create src/lib/webrtc/chunked-transfer.ts:**
Pure utility functions (no PeerJS dependency — takes generic send callback):

```typescript
export const CHUNK_SIZE = 64 * 1024;

export interface ChunkMessage {
  type: "chunk";
  index: number;
  total: number;
  data: ArrayBuffer;
}

export interface DoneMessage {
  type: "done";
  fileName: string;
  fileSize: number;
}

export type TransferMessage = ChunkMessage | DoneMessage;

export function sliceFileIntoChunks(file: File): { totalChunks: number; getChunk: (index: number) => Promise<ArrayBuffer> } {
  const totalChunks = Math.ceil(file.size / CHUNK_SIZE);
  return {
    totalChunks,
    getChunk: async (index: number) => {
      const start = index * CHUNK_SIZE;
      const end = Math.min(start + CHUNK_SIZE, file.size);
      return file.slice(start, end).arrayBuffer();
    },
  };
}

export function reassembleChunks(chunks: ArrayBuffer[], fileName: string): Blob {
  return new Blob(chunks, { type: "application/octet-stream" });
}

export function calculateTransferStats(bytesTransferred: number, totalBytes: number, startTime: number): {
  progress: number;
  speed: number;
  eta: number;
} {
  const elapsed = (Date.now() - startTime) / 1000;
  const speed = elapsed > 0 ? bytesTransferred / elapsed : 0;
  const remaining = totalBytes - bytesTransferred;
  const eta = speed > 0 ? remaining / speed : 0;
  return {
    progress: totalBytes > 0 ? Math.round((bytesTransferred / totalBytes) * 100) : 0,
    speed,
    eta,
  };
}
```

**6. Create src/lib/audio/spectrum-analyzer.ts:**
Export `renderSpectrogram(canvas: HTMLCanvasElement, audioElement: HTMLAudioElement)` following the pattern from RESEARCH.md code example. Returns `{ audioCtx, analyser, cleanup }` where cleanup stops the animation frame and closes the context. Uses AnalyserNode with fftSize 2048, smoothingTimeConstant 0.8. Color gradient maps amplitude 0-255 to Ghost Protocol green (rgb channels: R=111*percent, G=221*percent, B=120*percent). Scrolling spectrogram pattern (shift left, draw new column on right edge).

**7. Create src/lib/audio/file-metadata.ts:**
Export `analyzeAudioFile(file: Blob): Promise<{ format: string; duration: number; channels: number; sampleRate: number }>` using AudioContext.decodeAudioData. Export `formatDuration(seconds: number): string` for mm:ss display. Export `formatFileSize(bytes: number): string` for human-readable size (KB, MB, GB).
  </action>
  <verify>
    <automated>npx tsc --noEmit --strict src/lib/trades/constants.ts src/lib/trades/queries.ts src/lib/webrtc/chunked-transfer.ts 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/db/schema/trades.ts contains `expiresAt: timestamp("expires_at"` and `fileName: varchar("file_name"` and `fileFormat: varchar("file_format"` and `declaredBitrate: varchar("declared_bitrate"` and `fileSizeBytes: integer("file_size_bytes"`
    - src/lib/db/schema/users.ts contains `tradesTosAcceptedAt: timestamp("trades_tos_accepted_at"`
    - src/lib/trades/constants.ts contains `export const CHUNK_SIZE = 64 * 1024` and `export const TRADE_STATUS` and `export function isP2PEnabled()` and `export const MAX_FREE_TRADES_PER_MONTH = 5`
    - src/lib/trades/queries.ts contains `export async function getTradeInbox` and `export async function getTradeById` and `export async function getTradeReputation` and `export async function getTradeCountThisMonth`
    - src/lib/webrtc/chunked-transfer.ts contains `export function sliceFileIntoChunks` and `export function reassembleChunks` and `export function calculateTransferStats` and `export const CHUNK_SIZE = 64 * 1024`
    - src/lib/audio/spectrum-analyzer.ts contains `export function renderSpectrogram`
    - src/lib/audio/file-metadata.ts contains `export async function analyzeAudioFile` and `export function formatFileSize`
  </acceptance_criteria>
  <done>Schema has new columns, all constants/types/queries/utilities exported and type-check clean</done>
</task>

<task type="auto">
  <name>Task 2: Server actions + Wave 0 test scaffolds</name>
  <files>
    src/actions/trades.ts,
    tests/unit/trades/create-trade.test.ts,
    tests/unit/trades/trade-review.test.ts,
    tests/unit/trades/trade-reputation.test.ts,
    tests/unit/trades/p2p-gate.test.ts,
    tests/unit/trades/tos-gate.test.ts,
    tests/unit/trades/turn-credentials.test.ts,
    tests/unit/trades/chunked-transfer.test.ts,
    tests/unit/trades/trade-counter.test.ts
  </files>
  <read_first>
    src/actions/social.ts,
    src/actions/community.ts,
    src/lib/gamification/badge-awards.ts,
    src/lib/gamification/constants.ts,
    src/lib/trades/constants.ts,
    src/lib/trades/queries.ts,
    tests/unit/gamification/badge-awards.test.ts,
    tests/unit/community/community-actions.test.ts
  </read_first>
  <action>
**1. Create src/actions/trades.ts** — Full trade lifecycle server actions following existing pattern from src/actions/social.ts (use `"use server"` directive, `createClient()` for auth, `createAdminClient()` for cross-user writes):

- `createTrade(formData: { providerId: string; releaseId?: string; fileName: string; fileFormat: string; declaredBitrate: string; fileSizeBytes: number; expiryHours: number; message?: string })` — Per D-05, P2P-01.
  - Check isP2PEnabled() — throw error if false (D-03 server-side enforcement)
  - Auth check via createClient().auth.getUser()
  - Check ToS acceptance: query profiles.trades_tos_accepted_at for user. If null, return `{ error: "ToS not accepted" }`
  - Check trade quota: call getTradeCountThisMonth(). If count >= MAX_FREE_TRADES_PER_MONTH and plan === 'free', return `{ error: "Trade quota reached", tradesRemaining: 0 }`
  - Insert into trade_requests via admin client with status 'pending', computed expiresAt (now + expiryHours)
  - Create notification for provider (type: 'trade_request', link: `/trades/${tradeId}`)
  - Send trade request email via Resend (import sendEmail pattern from existing notifications/email.ts, add trade-specific email)
  - Return `{ success: true, tradeId }`

- `acceptTrade(tradeId: string)` — Per D-12. Auth + verify user is providerId. Conditional update: `status = 'pending'` -> `'accepted'`. Notify requester. Return updated trade.

- `declineTrade(tradeId: string)` — Auth + verify user is providerId. Conditional update: `status = 'pending'` -> `'declined'`. Notify requester.

- `cancelTrade(tradeId: string)` — Auth + verify user is requesterId. Conditional update: `status = 'pending'` -> `'cancelled'`. Notify provider.

- `updateTradeStatus(tradeId: string, newStatus: string)` — For lobby transitions (accepted -> transferring, transferring -> completed). Auth + verify participant. Conditional update with valid source state check.

- `completeTrade(tradeId: string, qualityRating: number, comment: string | null)` — Per RESEARCH.md completeTrade pattern. Auth, conditional update to 'completed', insert tradeReview, increment tradesThisMonth for both parties (use admin rpc or direct update), awardBadge('connector') for both, increment contribution scores (+15 per CONTRIBUTION_POINTS.trade_completed), logActivity('completed_trade'), notify counterparty.

- `getTurnCredentials()` — Per D-09. Server action that fetches ICE servers from Metered.ca REST API: `https://${METERED_APP_NAME}.metered.live/api/v1/turn/credentials?apiKey=${METERED_API_KEY}`. Falls back to Google STUN if env vars missing (dev mode). Returns `RTCIceServer[]`.

- `acceptToS()` — Auth, update profiles.trades_tos_accepted_at to now for the current user. Return `{ success: true }`.

- `skipReview(tradeId: string)` — Auth, verify participant, update status to completed without creating a review. Still awards badges and increments counters.

**2. Create all 8 Wave 0 test scaffolds** per VALIDATION.md:

Each test file follows the project convention: `vi.mock()` for full module isolation, mock Supabase admin client with `createQueryChain` helper pattern (see tests/unit/gamification/ for reference), mock Drizzle db thenable chain.

**tests/unit/trades/create-trade.test.ts:** Test createTrade server action:
- `it("creates a trade request with valid data")` — mock auth, mock isP2PEnabled true, mock ToS accepted, mock quota check returns remaining > 0, verify admin.from("trade_requests").insert() called with correct fields including expiresAt computed from expiryHours
- `it("rejects when P2P is disabled")` — mock isP2PEnabled false, verify error returned
- `it("rejects when ToS not accepted")` — mock tradesTosAcceptedAt null, verify error
- `it("rejects when trade quota reached")` — mock tradesThisMonth >= 5 with plan 'free', verify error with tradesRemaining: 0
- `it("allows premium users unlimited trades")` — mock plan 'premium_monthly' with tradesThisMonth = 50, verify success

**tests/unit/trades/trade-review.test.ts:** Test completeTrade:
- `it("creates review and updates status to completed")`
- `it("awards CONNECTOR badge to both parties")`
- `it("increments contribution score by 15 for both")`
- `it("rejects invalid quality rating (0 or 6)")`

**tests/unit/trades/trade-reputation.test.ts:** Test getTradeReputation:
- `it("returns average rating from trade reviews")`
- `it("returns null average when no reviews exist")`
- `it("returns correct totalTrades count")`

**tests/unit/trades/p2p-gate.test.ts:** Test isP2PEnabled:
- `it("returns true when P2P_ENABLED=true")`
- `it("returns false when P2P_ENABLED is unset")`
- `it("returns false when P2P_ENABLED=false")`

**tests/unit/trades/tos-gate.test.ts:** Test acceptToS:
- `it("sets trades_tos_accepted_at to current timestamp")`
- `it("returns error when not authenticated")`

**tests/unit/trades/turn-credentials.test.ts:** Test getTurnCredentials:
- `it("fetches ICE servers from Metered.ca API")`
- `it("falls back to STUN when env vars missing")`
- `it("throws on API failure")`

**tests/unit/trades/chunked-transfer.test.ts:** Test pure functions:
- `it("sliceFileIntoChunks returns correct chunk count for file size")`
- `it("reassembleChunks creates a blob from ArrayBuffer array")`
- `it("calculateTransferStats returns correct progress percentage")`
- `it("calculateTransferStats returns correct ETA")`

**tests/unit/trades/trade-counter.test.ts:** Test getTradeCountThisMonth:
- `it("returns current count for same month")`
- `it("resets count when month has rolled over")`
- `it("returns Infinity remaining for premium users")`
  </action>
  <verify>
    <automated>npx vitest run tests/unit/trades/ --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - src/actions/trades.ts contains `"use server"` on line 1
    - src/actions/trades.ts contains `export async function createTrade` and `export async function acceptTrade` and `export async function declineTrade` and `export async function cancelTrade` and `export async function completeTrade` and `export async function getTurnCredentials` and `export async function acceptToS`
    - src/actions/trades.ts contains `awardBadge(` (CONNECTOR badge award)
    - src/actions/trades.ts contains `CONTRIBUTION_POINTS.trade_completed` (contribution score increment)
    - src/actions/trades.ts contains `isP2PEnabled()` (server-side P2P gate check per D-03)
    - src/actions/trades.ts contains `METERED_API_KEY` or `process.env.METERED_API_KEY` (TURN credential fetch per D-09)
    - tests/unit/trades/create-trade.test.ts contains `describe(` and at least 4 `it(` blocks
    - tests/unit/trades/trade-review.test.ts contains `describe(` and at least 3 `it(` blocks
    - tests/unit/trades/trade-reputation.test.ts contains `describe(`
    - tests/unit/trades/p2p-gate.test.ts contains `describe(`
    - tests/unit/trades/tos-gate.test.ts contains `describe(`
    - tests/unit/trades/turn-credentials.test.ts contains `describe(`
    - tests/unit/trades/chunked-transfer.test.ts contains `describe(`
    - tests/unit/trades/trade-counter.test.ts contains `describe(`
    - All 8 test files exist in tests/unit/trades/
    - `npx vitest run tests/unit/trades/ -x` exits 0 (all tests pass)
  </acceptance_criteria>
  <done>Complete server action module with all trade lifecycle functions, 8 test files passing</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/unit/trades/ --reporter=verbose` — all 8 test files pass
- `npx tsc --noEmit` — no TypeScript errors in new files
- Schema files contain new columns (grep for expiresAt, tradesTosAcceptedAt)
- Server actions contain "use server" directive
- No server-side file storage logic anywhere (grep for "Supabase Storage" or "upload" returns nothing in trades.ts)
</verification>

<success_criteria>
- tradeRequests schema has 5 new columns (expiresAt, fileName, fileFormat, declaredBitrate, fileSizeBytes)
- profiles schema has tradesTosAcceptedAt column
- 8 server actions exported from src/actions/trades.ts
- 4 query functions exported from src/lib/trades/queries.ts
- Trade constants module with CHUNK_SIZE, TRADE_STATUS, isP2PEnabled, MAX_FREE_TRADES_PER_MONTH
- WebRTC chunked transfer utility with sliceFileIntoChunks, reassembleChunks, calculateTransferStats
- Audio analysis utilities (spectrum-analyzer.ts, file-metadata.ts)
- All 8 Wave 0 test scaffolds pass
</success_criteria>

<output>
After completion, create `.planning/phases/09-p2p-audio-trading/09-01-SUMMARY.md`
</output>
