---
phase: 23-test-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/tests/unit/gems/gem-badge.test.tsx
autonomous: true
requirements:
  - TEST-01

must_haves:
  truths:
    - "`vitest run` completes with 0 test failures"
    - "gem-badge.test.tsx passes all 16 tests"
  artifacts:
    - path: "apps/web/tests/unit/gems/gem-badge.test.tsx"
      provides: "Updated gem-badge tests matching Unicode glyph implementation"
      contains: "GEM_GLYPH"
  key_links: []
---

<objective>
Fix 4 failing tests in gem-badge.test.tsx that expect lucide-react icon `data-testid` attributes, but GemBadge now renders Unicode glyphs instead.

Purpose: Make `vitest run` pass with 0 failures.
Output: Updated test file matching the current GemBadge implementation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

@apps/web/src/components/ui/gem-badge.tsx
@apps/web/tests/unit/gems/gem-badge.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix gem-badge icon tests to match Unicode glyph implementation</name>
  <files>apps/web/tests/unit/gems/gem-badge.test.tsx</files>
  <read_first>apps/web/src/components/ui/gem-badge.tsx, apps/web/tests/unit/gems/gem-badge.test.tsx</read_first>
  <action>
The GemBadge component was redesigned to use Unicode glyphs instead of lucide-react icons:
- quartz: "◇" (was Hexagon icon)
- amethyst: "⬡" (was Gem icon)
- sapphire: "◆" (was Diamond icon)
- diamond: "❖" (was Diamond icon)

The component no longer imports from lucide-react at all.

**Fix the 4 failing tests:**

1. Remove the `vi.mock("lucide-react", ...)` block entirely (lines 6-16) — the component doesn't use lucide-react anymore.

2. Replace test "uses Hexagon icon for quartz tier" (line 76-79):
```typescript
it("uses quartz glyph for quartz tier", () => {
  render(<GemBadge score={0.1} />);
  expect(screen.getByText("◇")).toBeInTheDocument();
});
```

3. Replace test "uses Gem icon for amethyst tier" (line 81-84):
```typescript
it("uses amethyst glyph for amethyst tier", () => {
  render(<GemBadge score={0.5} />);
  expect(screen.getByText("⬡")).toBeInTheDocument();
});
```

4. Replace test "uses Diamond icon for sapphire tier" (line 86-89):
```typescript
it("uses sapphire glyph for sapphire tier", () => {
  render(<GemBadge score={4.0} />);
  expect(screen.getByText("◆")).toBeInTheDocument();
});
```

5. Replace test "uses Diamond icon for diamond tier" (line 91-94):
```typescript
it("uses diamond glyph for diamond tier", () => {
  render(<GemBadge score={7.0} />);
  expect(screen.getByText("❖")).toBeInTheDocument();
});
```

Do NOT change any other tests. The remaining 12 tests already pass.
  </action>
  <verify>
    <automated>cd apps/web && npx vitest run tests/unit/gems/gem-badge.test.tsx 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
- `vi.mock("lucide-react"` does NOT appear in gem-badge.test.tsx
- `screen.getByText("◇")` appears in the quartz glyph test
- `screen.getByText("⬡")` appears in the amethyst glyph test
- `screen.getByText("◆")` appears in the sapphire glyph test
- `screen.getByText("❖")` appears in the diamond glyph test
- `cd apps/web && npx vitest run tests/unit/gems/gem-badge.test.tsx` passes with 16/16 tests
- `cd apps/web && npx vitest run` passes with 0 failures total
  </acceptance_criteria>
  <done>All 4 failing icon tests updated to match Unicode glyph implementation. All 16 gem-badge tests pass. Full test suite passes with 0 failures.</done>
</task>

</tasks>

<verification>
1. `cd apps/web && npx vitest run tests/unit/gems/gem-badge.test.tsx` — all 16 tests pass
2. `cd apps/web && npx vitest run` — 0 failures total
3. `grep -c "lucide-react" apps/web/tests/unit/gems/gem-badge.test.tsx` — returns 0
</verification>

<success_criteria>
- `vitest run` completes with 0 test failures (TEST-01)
- gem-badge.test.tsx tests match the current Unicode glyph implementation
- No other test files modified
</success_criteria>

<output>
After completion, create `.planning/phases/23-test-fix/23-01-SUMMARY.md`
</output>
