---
phase: 01-foundation-authentication
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - biome.json
  - src/app/layout.tsx
  - src/app/globals.css
  - src/app/page.tsx
  - src/lib/utils.ts
  - src/components/ui/*
  - components.json
  - .env.local.example
autonomous: true
requirements: [AUTH-01, SEC-01]
user_setup:
  - service: supabase
    why: "Authentication and database backend"
    env_vars:
      - name: NEXT_PUBLIC_SUPABASE_URL
        source: "Supabase Dashboard -> Project Settings -> API -> Project URL"
      - name: NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY
        source: "Supabase Dashboard -> Project Settings -> API -> anon public key"
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: "Supabase Dashboard -> Project Settings -> API -> service_role key (secret)"
      - name: DATABASE_URL
        source: "Supabase Dashboard -> Project Settings -> Database -> Connection string (URI) -> Transaction mode (port 6543)"
    dashboard_config:
      - task: "Create a new Supabase project"
        location: "https://supabase.com/dashboard -> New Project"
      - task: "Enable 'Confirm email' for email verification"
        location: "Supabase Dashboard -> Authentication -> Providers -> Email -> Confirm email toggle ON"
  - service: upstash
    why: "Serverless rate limiting for auth endpoints"
    env_vars:
      - name: UPSTASH_REDIS_REST_URL
        source: "Upstash Console -> Redis -> Database -> REST API -> UPSTASH_REDIS_REST_URL"
      - name: UPSTASH_REDIS_REST_TOKEN
        source: "Upstash Console -> Redis -> Database -> REST API -> UPSTASH_REDIS_REST_TOKEN"
    dashboard_config:
      - task: "Create a Redis database"
        location: "https://console.upstash.com -> Create Database -> select closest region"

must_haves:
  truths:
    - "Next.js 15 app runs with `npm run dev` and shows a page"
    - "Dark-warm VinylDig theme renders with correct fonts, colors, and grain texture"
    - "shadcn/ui components are installed and styled with the dark-warm theme"
    - "Security headers are set on all responses"
    - "Biome linting and formatting works via npm scripts"
  artifacts:
    - path: "src/app/layout.tsx"
      provides: "Root layout with Fraunces + DM Sans fonts, grain texture, dark-warm theme"
      min_lines: 30
    - path: "src/app/globals.css"
      provides: "CSS variables for dark-warm theme (OKLCH), grain texture, Tailwind @theme"
      min_lines: 40
    - path: "next.config.ts"
      provides: "Security headers (CSP, HSTS, X-Frame-Options, etc.)"
      min_lines: 20
    - path: "biome.json"
      provides: "Biome linter/formatter configuration"
    - path: ".env.local.example"
      provides: "Template for required environment variables"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/app/globals.css"
      via: "CSS import"
      pattern: "import.*globals\\.css"
    - from: "src/app/layout.tsx"
      to: "next/font/google"
      via: "Font loading"
      pattern: "Fraunces|DM_Sans"
---

<objective>
Scaffold the Next.js 15 project with the VinylDig dark-warm design system, install all Phase 1 dependencies, configure shadcn/ui with custom theme, set up security headers, and configure Biome.

Purpose: This is the foundation that every other plan in Phase 1 (and every subsequent phase) builds on. Nothing works without a running Next.js project with the correct theme and tooling.

Output: A running Next.js 15 app with the VinylDig retro/analog aesthetic, all dependencies installed, security headers configured, and Biome linting operational.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-authentication/01-CONTEXT.md
@.planning/phases/01-foundation-authentication/01-RESEARCH.md
@.planning/phases/01-foundation-authentication/01-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js 15 project and install all Phase 1 dependencies</name>
  <files>package.json, tsconfig.json, .env.local.example</files>
  <action>
    1. Create the Next.js 15 project:
       ```
       npx create-next-app@15 vinyldig --typescript --tailwind --app --src-dir
       ```
       After creation, move all contents from the `vinyldig/` subdirectory up to the project root (the project root is the current working directory). Remove the empty `vinyldig/` directory.

    2. Install core dependencies (per 01-RESEARCH.md standard stack):
       ```
       npm install @supabase/supabase-js @supabase/ssr drizzle-orm postgres
       npm install zod react-hook-form @hookform/resolvers
       npm install @upstash/ratelimit @upstash/redis
       npm install zustand resend qrcode
       ```

    3. Install dev dependencies:
       ```
       npm install -D @types/qrcode drizzle-kit
       npm install -D vitest @testing-library/react @testing-library/jest-dom jsdom
       npm install -D @playwright/test
       npm install -D @biomejs/biome
       ```

    4. Initialize shadcn/ui:
       ```
       npx shadcn@latest init
       ```
       Select: New York style, OKLCH colors, CSS variables. The CLI will detect Tailwind v4.

    5. Add required shadcn components (per 01-UI-SPEC.md):
       ```
       npx shadcn@latest add button input label card form separator avatar skeleton badge sonner dialog alert
       ```

    6. Create `.env.local.example` with all required environment variables:
       ```
       NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
       NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=your_supabase_anon_key
       SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key
       DATABASE_URL=your_supabase_connection_pooler_url
       NEXT_PUBLIC_SITE_URL=http://localhost:3000
       UPSTASH_REDIS_REST_URL=your_upstash_redis_url
       UPSTASH_REDIS_REST_TOKEN=your_upstash_redis_token
       ```

    7. Configure Biome (`biome.json`): Enable formatting (indent: tab, line width: 100) and linting (recommended rules). Add npm scripts to package.json:
       - `"lint": "biome check src/"`
       - `"lint:fix": "biome check --fix src/"`
       - `"format": "biome format --write src/"`

    8. Add test scripts to package.json:
       - `"test": "vitest run"`
       - `"test:watch": "vitest"`
       - `"test:e2e": "playwright test"`
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Project builds successfully. All dependencies in package.json. `.env.local.example` exists with all required vars. Biome config exists. npm scripts for lint, format, test, test:e2e exist.</done>
</task>

<task type="auto">
  <name>Task 2: Configure dark-warm design system with security headers</name>
  <files>src/app/globals.css, src/app/layout.tsx, src/app/page.tsx, next.config.ts</files>
  <action>
    1. **globals.css** -- Replace shadcn default theme with VinylDig dark-warm theme (per D-01, D-02, D-05, 01-UI-SPEC.md CSS Variable Map):
       - Set all CSS variables from the UI-SPEC CSS Variable Map section using OKLCH values
       - Since this is dark-only (D-01), set these as `:root` values with no `.dark` class or media query
       - Add Tailwind `@theme` directives to register custom tokens (spacing scale from UI-SPEC)
       - Add the grain texture CSS (per D-03, 01-UI-SPEC.md Grain Texture section):
         ```css
         .grain::after {
           content: "";
           position: fixed;
           inset: 0;
           opacity: 0.03;
           pointer-events: none;
           z-index: 50;
           background-image: url("data:image/svg+xml,..."); /* SVG feTurbulence from UI-SPEC */
         }
         ```
       - Add skeleton shimmer keyframes with warm gradient (per UI-SPEC Loading States)
       - Add `@media (prefers-reduced-motion: reduce)` to disable grain animation and transitions (per UI-SPEC Accessibility)

    2. **layout.tsx** -- Configure root layout (per UI-SPEC Font Loading and Layout Pattern):
       - Load Fraunces (weights 400, 600; variable --font-fraunces) and DM Sans (weights 400, 600; variable --font-dm-sans) via `next/font/google`
       - Set `<html lang="en" className={`${dmSans.variable} ${fraunces.variable}`}>`
       - Apply DM Sans as default body font via className
       - Wrap children in a `<div className="grain">` for the texture overlay
       - Set metadata: title "VinylDig", description "A social network for vinyl diggers"
       - Add Sonner `<Toaster />` component for toast notifications (warm theme)

    3. **page.tsx** -- Create a minimal landing page that demonstrates the theme:
       - Display "VinylDig" in Fraunces (display size per UI-SPEC: 36px desktop, 28px mobile)
       - Show a "Welcome" subtitle in DM Sans
       - Include a primary Button component (amber accent) and a Card component to verify shadcn theming
       - This page is temporary -- it will be replaced by auth redirect logic later

    4. **next.config.ts** -- Configure security headers (per D-17, SEC-01, 01-RESEARCH.md security headers pattern):
       - X-DNS-Prefetch-Control: on
       - Strict-Transport-Security: max-age=63072000; includeSubDomains; preload
       - X-Frame-Options: DENY
       - X-Content-Type-Options: nosniff
       - Referrer-Policy: origin-when-cross-origin
       - Permissions-Policy: camera=(), microphone=(), geolocation=()
       - Content-Security-Policy: default-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; img-src 'self' data: https:; font-src 'self' https://fonts.gstatic.com; connect-src 'self' https://*.supabase.co wss://*.supabase.co; frame-ancestors 'none'
  </action>
  <verify>
    <automated>npm run dev -- --port 3099 &amp; PID=$!; sleep 8; curl -s -o /dev/null -w "%{http_code}" http://localhost:3099 &amp;&amp; curl -sI http://localhost:3099 | grep -i "x-frame-options\|strict-transport\|content-security-policy" | head -3; kill $PID 2>/dev/null</automated>
  </verify>
  <done>Dev server starts and returns 200. Security headers (X-Frame-Options, HSTS, CSP) are present in response. Page renders with dark background (#0D0B09), amber accent, Fraunces heading, DM Sans body text, and grain texture overlay.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds without errors
- `npm run lint` runs without errors
- Dev server starts and shows themed VinylDig page
- Security headers present on all responses (verify with curl -I)
- Fraunces and DM Sans fonts load correctly
- Dark-warm theme colors render (no white backgrounds, no default shadcn blue)
- Grain texture visible on background
</verification>

<success_criteria>
- Next.js 15 project runs at localhost:3000 with dark-warm VinylDig aesthetic
- All Phase 1 dependencies installed (Supabase, Drizzle, Zod, RHF, Upstash, shadcn components)
- Security headers configured in next.config.ts (CSP, HSTS, X-Frame-Options, X-Content-Type-Options)
- Biome linting operational
- Environment variable template exists
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-authentication/01-01-SUMMARY.md`
</output>
