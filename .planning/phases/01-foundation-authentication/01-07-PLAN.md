---
phase: 01-foundation-authentication
plan: 07
type: execute
wave: 4
depends_on: [01-04, 01-06]
files_modified:
  - src/app/(protected)/onboarding/page.tsx
  - src/app/(protected)/onboarding/2fa/page.tsx
  - src/app/(protected)/onboarding/layout.tsx
  - src/components/onboarding/profile-setup.tsx
  - src/components/onboarding/security-setup.tsx
  - src/components/onboarding/discogs-connect.tsx
  - src/components/onboarding/onboarding-complete.tsx
  - src/components/onboarding/step-indicator.tsx
  - src/actions/onboarding.ts
autonomous: false
requirements: [AUTH-01, AUTH-05]

must_haves:
  truths:
    - "New user lands on onboarding after first login (not Feed)"
    - "User can set display name and avatar in step 1"
    - "User is offered 2FA setup in step 2 (can skip)"
    - "User is offered Discogs connection in step 3 (can skip)"
    - "Completing onboarding marks profile as onboarding_completed"
    - "Step indicator shows progress through onboarding"
  artifacts:
    - path: "src/app/(protected)/onboarding/page.tsx"
      provides: "Multi-step onboarding page"
      min_lines: 20
    - path: "src/components/onboarding/profile-setup.tsx"
      provides: "Step 1: display name + avatar form"
      min_lines: 40
    - path: "src/components/onboarding/security-setup.tsx"
      provides: "Step 2: 2FA suggestion with skip option"
      min_lines: 30
    - path: "src/components/onboarding/discogs-connect.tsx"
      provides: "Step 3: Discogs connection (placeholder) with skip"
      min_lines: 20
    - path: "src/actions/onboarding.ts"
      provides: "Server Actions for profile update and onboarding completion"
      exports: ["updateProfile", "completeOnboarding"]
  key_links:
    - from: "src/components/onboarding/profile-setup.tsx"
      to: "src/actions/onboarding.ts"
      via: "updateProfile server action"
      pattern: "updateProfile"
    - from: "src/components/onboarding/security-setup.tsx"
      to: "src/components/auth/totp-setup.tsx"
      via: "TOTP setup component reuse"
      pattern: "TotpSetup"
    - from: "src/actions/onboarding.ts"
      to: "src/lib/db/index.ts"
      via: "profiles table update"
      pattern: "db\\.update.*profiles"
---

<objective>
Build the multi-step onboarding flow that new users go through after their first login: profile setup (display name/avatar), 2FA suggestion (optional), and Discogs connection (placeholder, skippable).

Purpose: Per D-06, users go through onboarding after signup -- not directly to Feed. Per D-08, 2FA is suggested during onboarding but not mandatory. This creates a welcoming first-run experience for the digger community.

Output: Working 3-step onboarding wizard with profile setup, 2FA suggestion, Discogs placeholder, and completion screen.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-authentication/01-CONTEXT.md
@.planning/phases/01-foundation-authentication/01-UI-SPEC.md

<interfaces>
From src/lib/supabase/server.ts:
  export async function createClient(): Promise<SupabaseClient>

From src/lib/db/index.ts:
  export const db: DrizzleClient

From src/lib/db/schema/users.ts:
  export const profiles: PgTable  // display_name, avatar_url, onboarding_completed

From src/components/auth/totp-setup.tsx:
  export function TotpSetup({ onComplete, onSkip }): JSX.Element

From src/components/auth/auth-card.tsx:
  export function AuthCard({ title, subtitle?, children, footer?, maxWidth? }): JSX.Element
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create onboarding server actions and step indicator</name>
  <files>src/actions/onboarding.ts, src/components/onboarding/step-indicator.tsx, src/app/(protected)/onboarding/layout.tsx</files>
  <action>
    1. **src/actions/onboarding.ts** -- Server Actions for onboarding:
       - `updateProfile(formData: FormData)`: "use server"
         - Get current user via getClaims()
         - Validate display_name (3-50 chars, no special characters except hyphens and underscores)
         - Update profiles table: set display_name, avatar_url (if provided)
         - Return success/error
       - `completeOnboarding()`: "use server"
         - Get current user via getClaims()
         - Update profiles table: set `onboarding_completed = true`
         - Return success with redirect to `/` (main app)
       - `skipToStep(step: number)`: "use server"
         - No-op server action, just returns the next step number
         - Used to track progress without requiring database writes for skipped steps

    2. **src/components/onboarding/step-indicator.tsx** -- Step progress indicator ("use client"):
       - Props: `currentStep: number`, `totalSteps: number` (default 3)
       - Render dots/circles for each step
       - Current step: amber accent fill
       - Completed steps: amber outline with checkmark
       - Future steps: muted border, no fill
       - Horizontal layout, centered above the form card
       - Step indicator dots shown immediately during loading (per UI-SPEC Loading States)

    3. **src/app/(protected)/onboarding/layout.tsx** -- Onboarding layout:
       - Server Component
       - Check auth via getClaims() -- redirect to /signin if not authenticated
       - Check if onboarding already completed -- redirect to / if so
       - Full viewport height centered layout (same pattern as auth layout)
       - VinylDig wordmark above content
       - Renders children
  </action>
  <verify>
    <automated>npx tsc --noEmit src/actions/onboarding.ts src/components/onboarding/step-indicator.tsx 2>&1 | head -10</automated>
  </verify>
  <done>Onboarding server actions handle profile updates and completion marking. Step indicator renders progress dots. Layout protects onboarding routes.</done>
</task>

<task type="auto">
  <name>Task 2: Build 3-step onboarding wizard pages</name>
  <files>src/app/(protected)/onboarding/page.tsx, src/app/(protected)/onboarding/2fa/page.tsx, src/components/onboarding/profile-setup.tsx, src/components/onboarding/security-setup.tsx, src/components/onboarding/discogs-connect.tsx, src/components/onboarding/onboarding-complete.tsx</files>
  <action>
    1. **src/components/onboarding/profile-setup.tsx** -- Step 1: Profile setup ("use client", per UI-SPEC Onboarding Copywriting):
       - Title: "Set Up Your Profile" (per UI-SPEC)
       - Body: "Choose a display name and avatar. This is how other diggers will find you." (per UI-SPEC)
       - Fields: display name (Input, required), avatar (optional, file upload or URL -- keep simple for Phase 1, use a text URL input)
       - CTA button: "Continue Setup" (per UI-SPEC, primary amber)
       - On submit: call updateProfile server action, then advance to step 2
       - Loading skeleton: avatar circle (64px) + 2 skeleton text lines + skeleton button (per UI-SPEC)

    2. **src/components/onboarding/security-setup.tsx** -- Step 2: 2FA suggestion ("use client", per UI-SPEC, D-08):
       - Title: "Secure Your Account" (per UI-SPEC)
       - Body: "Two-factor authentication adds an extra layer of security. We recommend enabling it." (per UI-SPEC)
       - Primary CTA: "Enable 2FA" (per UI-SPEC) -> navigates to /onboarding/2fa
       - Secondary CTA: "Skip for Now" (per UI-SPEC) -> advances to step 3
       - This step does NOT force 2FA -- it's a suggestion per D-08

    3. **src/app/(protected)/onboarding/2fa/page.tsx** -- 2FA setup during onboarding:
       - Server Component
       - Renders AuthCard (max-width 420px) with TotpSetup component from Plan 06
       - On completion: redirect back to onboarding step 3
       - On cancel: redirect back to onboarding step 3

    4. **src/components/onboarding/discogs-connect.tsx** -- Step 3: Discogs connection placeholder ("use client", per UI-SPEC):
       - Title: "Connect Discogs" (per UI-SPEC)
       - Body: "Link your Discogs account to import your collection and wantlist. You can always do this later." (per UI-SPEC)
       - Primary CTA: "Connect Discogs" (per UI-SPEC) -- DISABLED/placeholder for Phase 1. Show tooltip "Coming soon in Phase 3" or just disable the button.
       - Secondary CTA: "Skip for Now" (per UI-SPEC) -> advances to completion
       - Discogs integration is Phase 3 work -- this step just establishes the onboarding slot

    5. **src/components/onboarding/onboarding-complete.tsx** -- Completion screen ("use client", per UI-SPEC):
       - Title: "You're In" (per UI-SPEC)
       - Body: "Welcome to VinylDig. Start digging." (per UI-SPEC)
       - CTA: "Go to Feed" (per UI-SPEC, primary amber) -> calls completeOnboarding() then redirects to /

    6. **src/app/(protected)/onboarding/page.tsx** -- Main onboarding page:
       - "use client" component (needs state for step tracking)
       - Manage current step state (1-4, where 4 is completion)
       - Render StepIndicator with currentStep
       - Render AuthCard with maxWidth 520px (per UI-SPEC onboarding width)
       - Switch on currentStep to render ProfileSetup, SecuritySetup, DiscogsConnect, or OnboardingComplete
       - Each step component receives `onComplete` and `onSkip` callbacks to advance steps
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>Onboarding wizard renders 3 steps + completion at /onboarding. Profile setup collects display name. 2FA is suggested but skippable. Discogs placeholder exists. Completion marks onboarding done and redirects. All copy matches UI-SPEC.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete authentication flows: signup, signin, email verification, password reset, OAuth buttons, 2FA enrollment, backup codes, and onboarding wizard -- all with the VinylDig dark-warm design system.</what-built>
  <how-to-verify>
    1. Visit http://localhost:3000 -- should see VinylDig themed page with dark background, amber accents, grain texture
    2. Navigate to /signup -- fill out form, verify inline validation (try weak password), submit
    3. Check /verify-email page renders with correct copy
    4. Navigate to /signin -- fill out form, verify error messages
    5. Visit /forgot-password -- submit email, verify success message
    6. Visit /onboarding -- step through all 3 steps + completion screen
    7. Verify all pages use dark-warm theme (no white backgrounds, amber CTAs, Fraunces headings, DM Sans body)
    8. Verify step indicator dots show progress through onboarding
    9. Verify responsive layout: narrow the browser to mobile width, check card sizing and padding
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` succeeds
- /onboarding renders multi-step wizard with step indicator
- Step 1 collects display name
- Step 2 offers 2FA with skip option
- Step 3 shows Discogs placeholder with skip option
- Completion screen shows "You're In" with "Go to Feed" button
- onboarding_completed flag is set in profiles table on completion
- All copy matches UI-SPEC Onboarding section
</verification>

<success_criteria>
- New users land on /onboarding after first login (D-06)
- Profile setup works: display name saved to profiles table
- 2FA is suggested but skippable (D-08)
- Discogs step is a placeholder for Phase 3
- Completing onboarding sets onboarding_completed and redirects to /
- All auth pages visually match the dark-warm VinylDig aesthetic
- Step indicator correctly shows progress
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-authentication/01-07-SUMMARY.md`
</output>
