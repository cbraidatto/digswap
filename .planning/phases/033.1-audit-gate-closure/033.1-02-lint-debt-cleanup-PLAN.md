---
phase: 033.1-audit-gate-closure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/app/(protected)/(profile)/perfil/page.tsx
  - apps/web/src/app/(protected)/(profile)/perfil/stats/page.tsx
  - apps/web/src/app/(protected)/(profile)/perfil/_components/cover-banner.tsx
  - apps/web/src/app/(protected)/(profile)/perfil/_components/edit-profile-modal.tsx
  - apps/web/src/app/(protected)/biblioteca/page.tsx
  - apps/web/src/app/(protected)/settings/_components/notification-preferences.tsx
  - apps/web/src/app/(protected)/trades/[id]/_components/AudioUploadSection.tsx
  - apps/web/src/app/(protected)/trades/[id]/_components/PreviewApprovalSection.tsx
  - apps/web/src/app/(protected)/trades/[id]/_components/TradeDetailHeader.tsx
  - apps/web/src/app/(protected)/trades/[id]/_components/TradeStatusWatcher.tsx
  - apps/web/src/app/(protected)/trades/[id]/_components/ProposalHistoryThread.tsx
  - .planning/phases/033.1-audit-gate-closure/evidence/02a-lint-final.txt
autonomous: true
requirements:
  - DEP-AUD-01

must_haves:
  truths:
    - "pnpm --filter @digswap/web lint exits 0"
    - "biome check src/ tests/ reports zero errors (warnings/infos may remain — they're non-blocking)"
    - "All 20 residual errors enumerated in evidence/01b-lint.txt are eliminated"
    - "No new errors introduced (file count of touched files matches the surfaced rule list)"
    - "Zero noExplicitAny diagnostics remain — final lint output shows 0 occurrences of the rule"
  artifacts:
    - path: ".planning/phases/033.1-audit-gate-closure/evidence/02a-lint-final.txt"
      provides: "Proof of lint exit=0 + Found 0 errors"
      contains: "Found 0 errors"
  key_links:
    - from: "biome check"
      to: "exit 0"
      via: "pnpm --filter @digswap/web lint"
      pattern: "Found 0 errors"
---

<objective>
Close DEP-AUD-01 PARTIAL verdict. After Phase 33 ran `biome check --write` autofix on 98 files + `--write --unsafe` on 3 targeted files, **20 residual errors remain** across 11 source files (per `evidence/01b-lint.txt`). These are not autofixable — each needs a human-judgment fix because the rules involve type narrowing (noExplicitAny), code intent (noUnusedFunctionParameters → underscore-prefix vs delete), or semantic correctness (useExhaustiveDependencies, useAriaPropsSupportedByRole).

Purpose: SC#1 of Phase 33 requires `pnpm --filter @digswap/web lint` to exit 0. Phase 34 cannot start while CI lint gate is red.
Output: 11 files touched, 20 errors closed, `Found 0 errors` confirmed on the next `lint` run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/033-pre-deploy-audit-gate/033-VERIFICATION.md
@.planning/phases/033-pre-deploy-audit-gate/AUDIT-REPORT.md
@.planning/phases/033-pre-deploy-audit-gate/evidence/01b-lint.txt
@apps/web/biome.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix all 20 residual lint errors enumerated in evidence/01b-lint.txt</name>
  <files>apps/web/src/app/(protected)/(profile)/perfil/page.tsx, apps/web/src/app/(protected)/(profile)/perfil/stats/page.tsx, apps/web/src/app/(protected)/(profile)/perfil/_components/cover-banner.tsx, apps/web/src/app/(protected)/(profile)/perfil/_components/edit-profile-modal.tsx, apps/web/src/app/(protected)/biblioteca/page.tsx, apps/web/src/app/(protected)/settings/_components/notification-preferences.tsx, apps/web/src/app/(protected)/trades/[id]/_components/AudioUploadSection.tsx, apps/web/src/app/(protected)/trades/[id]/_components/PreviewApprovalSection.tsx, apps/web/src/app/(protected)/trades/[id]/_components/TradeDetailHeader.tsx, apps/web/src/app/(protected)/trades/[id]/_components/TradeStatusWatcher.tsx, apps/web/src/app/(protected)/trades/[id]/_components/ProposalHistoryThread.tsx</files>
  <read_first>
    - .planning/phases/033-pre-deploy-audit-gate/evidence/01b-lint.txt (THE concrete error list — read in full)
    - apps/web/biome.json (rule configuration — confirm severity levels)
    - .planning/phases/033-pre-deploy-audit-gate/AUDIT-REPORT.md §1 lines 67-76 (rule type breakdown)
    - Each of the 11 listed files BEFORE editing — understand the surrounding code so the fix isn't a band-aid (e.g., decide whether an unused param should be deleted or prefixed with `_`)
  </read_first>
  <action>
    Work through `evidence/01b-lint.txt` rule-by-rule. The 20 errors break down by rule (counts per VERIFICATION.md gap 1 + AUDIT-REPORT §1 + spot-check of 01b tail showing additional categories surfaced after autofix reshuffle):

    **Group A — noUnusedVariables / noUnusedFunctionParameters (mostly FIXABLE):**
    - `perfil/page.tsx:91-93` — three destructured promise results (`weeklyAdds`, `tradesThisWeek`, `wantlistFound`) are unused. Either: (a) remove from `Promise.all` if the side-effect query isn't needed, OR (b) prefix with `_weeklyAdds`, `_tradesThisWeek`, `_wantlistFound` if the query MUST run for its side-effects. **Read the surrounding `Promise.all` block first** — if the calls are pure data fetches with no side-effects (likely), DELETE the entries from the array. If they trigger DB writes/cache warming (unlikely here), prefix with `_`.
    - `biblioteca/page.tsx:125` — `useCallback(async (folder: string, mode: ...))` — `folder` unused. Prefix `_folder` (Biome's auto-fix suggestion).
    - `trades/[id]/_components/TradeDetailHeader.tsx:27` and `TradeStatusWatcher.tsx:12` — unused variables. Apply Biome's FIXABLE suggestion (typically delete or prefix).

    **Group B — noNonNullAssertion (NOT autofixable — requires type narrowing):**
    - `perfil/stats/page.tsx:204` — `<StatRow label={l.label!}` — replace `!` with a guard: `topLabels.filter(l => l.label != null).map(l => <StatRow label={l.label} ...>)` OR use nullish-coalescing `label={l.label ?? ""}` if empty string is acceptable.
    - `settings/_components/notification-preferences.tsx:203` — same pattern. Inspect what's being asserted; replace with `if (x == null) return null` early-return or `??` fallback.
    - `trades/[id]/_components/AudioUploadSection.tsx:125` (FIXABLE — Biome may auto-suggest), `PreviewApprovalSection.tsx:60`, `PreviewApprovalSection.tsx:86` — same approach. Each `x!` becomes a narrowed `x` via either an early-return guard at the top of the function/closure or a `??` fallback at the use site.

    **Group C — useExhaustiveDependencies (semantic — read closure carefully):**
    - `perfil/_components/cover-banner.tsx:136` and `biblioteca/page.tsx:116` (FIXABLE) — Biome will list missing deps in the diagnostic. Add them to the dep array. If the dep is intentionally omitted (rare, e.g., a stable ref), use a `// biome-ignore lint/correctness/useExhaustiveDependencies: <reason>` directive on the line above.

    **Group D — Accessibility (a11y) errors:**
    - `perfil/_components/edit-profile-modal.tsx:125` (`useSemanticElements`) — Biome will suggest the correct semantic element (likely `<button>`, `<dialog>`, `<header>`, etc. instead of `<div role="...">`). Apply the suggestion.
    - `trades/[id]/_components/AudioUploadSection.tsx:39, 58, 75` (`noSvgWithoutTitle`) — three `<svg>` elements without `<title>`. Add `<title>Upload icon</title>` (or contextually appropriate text) as the first child of each `<svg>`. Match existing icon labels in the codebase.
    - `trades/[id]/_components/ProposalHistoryThread.tsx:154` (`useAriaPropsSupportedByRole`) — `<div aria-label={...}>` without an explicit role. Either add `role="article"` (or `"region"` if appropriate), OR move the `aria-label` to a child element that is already semantic, OR use `aria-labelledby` pointing at a heading inside the card.

    **Group E — Misc (one-off):**
    - `useParseIntRadix` (1 occurrence per AUDIT-REPORT §1) — find the `parseInt(x)` call and change to `parseInt(x, 10)`. Search across the 11 files; biome line is in 01b-lint.txt full output (use `grep useParseIntRadix evidence/01b-lint.txt` to locate the file:line).
    - `noImgElement` (1 occurrence) — find `<img ...>` and replace with Next.js `<Image ...>` from `next/image`. Confirm width/height/alt are provided.
    - `noExplicitAny` (6 occurrences in profile-hero / profile-sidebar — note these aren't in the partial 01b-lint.txt printout because Biome cuts at 20 diagnostics; the message "Diagnostics not shown: 107" includes them). Run `pnpm --filter @digswap/web lint 2>&1 | grep noExplicitAny` first to enumerate them, then replace each `: any` with the actual type. If the type is genuinely unknown, use `: unknown` and narrow at the use site with a type guard.

    **Working order:** Tackle Group A first (most autofixable), then B (noNonNullAssertion — they form clusters in 3 files, can be batched), then C, then D, then E. After each group, run `pnpm --filter @digswap/web lint 2>&1 | tail -5` to confirm the error count drops.

    **Do NOT use blanket `// biome-ignore` directives** to silence errors. Each ignore must have a one-line reason and survives code review. Biome ignores without reasons themselves trigger lint warnings.

    **Note on the autofix-reshuffle problem (per AUDIT-REPORT §1 Finding #2):** running `biome check --write` early may surface NEW errors (e.g., noSvgWithoutTitle, more noNonNullAssertion). DO NOT run autofix again at this stage — work through the static error list. After all 20 manual fixes land, the next `lint` run should be clean (or surface a small additional set of <5 errors which are then fixed in-place).
  </action>
  <verify>
    <automated>pnpm --filter @digswap/web lint 2>&1 | tee .planning/phases/033.1-audit-gate-closure/evidence/02a-lint-final.txt | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `evidence/02a-lint-final.txt` ends with `Found 0 errors.` (warnings + infos may remain — those are non-blocking per Biome severity model)
    - `pnpm --filter @digswap/web lint` exits 0 (`echo $?` after the command returns 0)
    - `grep -cE "lint/(correctness|style|a11y|suspicious)/\\w+ ━" .planning/phases/033.1-audit-gate-closure/evidence/02a-lint-final.txt` returns 0 (zero error-class diagnostic headers)
    - `pnpm --filter @digswap/web lint 2>&1 | grep -c "noExplicitAny"` returns 0 — zero remaining `any` diagnostics
    - `pnpm --filter @digswap/web typecheck` still exits 0 (sanity — fixes shouldn't break types)
    - `pnpm --filter @digswap/web test` still exits 0 (sanity — fixes shouldn't break tests)
  </acceptance_criteria>
  <done>
    `pnpm --filter @digswap/web lint` exits 0 with `Found 0 errors`. Evidence captured. Typecheck + test gates remain green.
  </done>
</task>

</tasks>

<verification>
Phase-level proof for DEP-AUD-01 closure:
```bash
pnpm --filter @digswap/web lint            # MUST exit 0
pnpm --filter @digswap/web typecheck       # sanity — must remain 0
pnpm --filter @digswap/web test            # sanity — must remain 0
grep "Found 0 errors" .planning/phases/033.1-audit-gate-closure/evidence/02a-lint-final.txt
pnpm --filter @digswap/web lint 2>&1 | grep -c "noExplicitAny"  # MUST be 0
```
</verification>

<success_criteria>
- DEP-AUD-01 flips from PARTIAL to PASS — all 4 CI gates (typecheck, build, test, lint) green on Phase 33.1 HEAD
- `evidence/02a-lint-final.txt` shows `Found 0 errors`
- Zero `noExplicitAny` diagnostics remain in the final lint output
- No new lint errors introduced; warning/info count may decrease as a happy side-effect
</success_criteria>

<output>
After completion, create `.planning/phases/033.1-audit-gate-closure/033.1-02-lint-debt-cleanup-SUMMARY.md` using the summary template. List the 20 fixes by rule type with file:line refs (so a future audit can trace each closure to its commit).
</output>
