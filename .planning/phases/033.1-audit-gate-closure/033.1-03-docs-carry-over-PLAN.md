---
phase: 033.1-audit-gate-closure
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ADR-003-drizzle-dev-only.md
  - .planning/phases/033.1-audit-gate-closure/RUNBOOK.md
  - .planning/phases/033.1-audit-gate-closure/evidence/03a-adr-003-grep.txt
autonomous: false
requirements:
  - DEP-AUD-01
  - DEP-AUD-02
  - DEP-AUD-08

must_haves:
  truths:
    - "ADR-003 contains the historical-correctness note: 'supabase/migrations/ became authoritative on 2026-04-23 via commit 090bdcc'"
    - "ADR-003 explicitly references the drift-capture migration (20260107) and the 7 drift category fixes table from AUDIT-REPORT.md §2"
    - "RUNBOOK.md documents the missing NEXT_PUBLIC_APP_URL local-env-file step (Plan 02 carry-over)"
    - "User has confirmed they added NEXT_PUBLIC_APP_URL to apps/web/.env.local OR explicitly accepted the deferred risk in the checkpoint"
  artifacts:
    - path: ".planning/ADR-003-drizzle-dev-only.md"
      provides: "ADR-003 with timeline note documenting when supabase/migrations/ actually became authoritative"
      contains: "2026-04-23 via commit 090bdcc"
    - path: ".planning/phases/033.1-audit-gate-closure/RUNBOOK.md"
      provides: "Operator runbook entry covering the .env.local NEXT_PUBLIC_APP_URL prerequisite"
      contains: "NEXT_PUBLIC_APP_URL=http://localhost:3000"
  key_links:
    - from: "ADR-003 Status section"
      to: "AUDIT-REPORT.md §2 'Resolution landed 2026-04-23 (commit 090bdcc)'"
      via: "explicit cross-reference paragraph"
      pattern: "090bdcc"
    - from: "RUNBOOK.md Local Setup section"
      to: "apps/web/.env.local.example line 9"
      via: "instruction to copy NEXT_PUBLIC_APP_URL into local file"
      pattern: "NEXT_PUBLIC_APP_URL"
---

<objective>
Close the two non-blocking carry-over gaps from Phase 33 VERIFICATION.md (gaps 4 and 5):

- **Gap 4 — ADR-003 historical-claim timeline**: ADR-003 (`accepted 2026-04-21`) claims `supabase/migrations/` is "the sole authoritative trail for production". This was actually FALSE as of 2026-04-22 — `leads` and 5 other tables existed only on dev Supabase via ad-hoc `drizzle-kit push`. The claim BECAME TRUE on 2026-04-23 via commit `090bdcc` (the drift-capture migration `20260107_drift_capture_missing_tables.sql` + 7 distinct drift category fixes documented in AUDIT-REPORT §2). ADR-003 needs a one-paragraph timeline note so future readers know the architectural state aligns with the document only after that commit.

- **Gap 5 — User's `.env.local` missing `NEXT_PUBLIC_APP_URL`**: Plan 02 build initially failed because the user's local `apps/web/.env.local` lacks `NEXT_PUBLIC_APP_URL=http://localhost:3000` (required by the Zod env schema, present in `.env.local.example` line 9). Was added locally during audit (non-committed) to unblock the build. Needs a permanent operator-runbook entry so the next operator (or the user re-cloning the repo) doesn't re-trip the same wire.

Purpose: Both gaps are housekeeping — neither blocks Phase 34 promotion, but both must close before Phase 33.1 itself can claim "all gaps remediated".
Output: Updated ADR-003 with a timeline section + new RUNBOOK.md with local-setup prerequisites.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/033-pre-deploy-audit-gate/033-VERIFICATION.md
@.planning/phases/033-pre-deploy-audit-gate/AUDIT-REPORT.md
@.planning/ADR-003-drizzle-dev-only.md
@apps/web/.env.local.example

<interfaces>
<!-- Current ADR-003 ## Status section (the section to amend) -->
```markdown
## Status

Accepted 2026-04-21 as part of Phase 33 (Pre-Deploy Audit Gate).
```

<!-- AUDIT-REPORT.md §2 "Resolution landed 2026-04-23 (commit 090bdcc)" lists 7 drift categories: -->
<!--   1. lexical ordering — drizzle 0000-0005 copied + 030_purge renamed -->
<!--   2. CREATE INDEX CONCURRENTLY in transaction — keyword removed -->
<!--   3. leads + 5 drift tables — new 20260107_drift_capture_missing_tables.sql -->
<!--   4. duplicate-policy errors — 20260405 archived to deprecated-migrations/ -->
<!--   5. duplicate version prefix collisions — 4 files renamed to 14-digit timestamps -->
<!--   6. invited_by/created_by columns never existed — 20260405 archived -->
<!--   7. open_for_trade column timing — 20260415 moved to 20260405000000 -->

<!-- .env.local.example line 9 (the required entry user is missing locally) -->
```bash
# App
NEXT_PUBLIC_SITE_URL=http://localhost:3000
NEXT_PUBLIC_APP_URL=http://localhost:3000
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add timeline note to ADR-003</name>
  <files>.planning/ADR-003-drizzle-dev-only.md, .planning/phases/033.1-audit-gate-closure/evidence/03a-adr-003-grep.txt</files>
  <read_first>
    - .planning/ADR-003-drizzle-dev-only.md (full file — only 36 lines)
    - .planning/phases/033-pre-deploy-audit-gate/AUDIT-REPORT.md §2 "Resolution landed 2026-04-23 (commit 090bdcc)" (the 7-category fix table)
    - .planning/phases/033-pre-deploy-audit-gate/033-VERIFICATION.md gap 4 entry
  </read_first>
  <action>
    Edit `.planning/ADR-003-drizzle-dev-only.md`. Insert a new section titled `## Historical Note (added 2026-04-24, Phase 33.1)` BETWEEN the existing `## Consequences` section and the `## Status` section. Use this exact prose:

    ```markdown
    ## Historical Note (added 2026-04-24, Phase 33.1)

    The decision above describes the *intended* architectural state. As of 2026-04-21 (when this ADR was accepted) and 2026-04-22 (when Phase 33 audit began), the claim that `supabase/migrations/` was the "sole authoritative trail for production" was **historically false**. Several tables (notably `leads`, plus 5 others — `crew_members`, `crew_invites`, `feed_post_reactions`, `feed_post_comments`, and `discogs_tokens`) existed only on the dev Supabase project (`mrkgoucqcbqjhrdjcnpw`) via ad-hoc `drizzle-kit push` invocations and had never been reconciled into either the `drizzle/` journal or the `supabase/migrations/` SQL trail.

    `supabase/migrations/` became authoritative on **2026-04-23 via commit `090bdcc`**, which landed in Phase 33 Plan 03. That commit:
    1. Added `supabase/migrations/20260107_drift_capture_missing_tables.sql` capturing the 6 drift tables (drift-capture migration);
    2. Resolved 7 distinct drift categories documented in `.planning/phases/033-pre-deploy-audit-gate/AUDIT-REPORT.md §2 Findings`:
       - Lexical migration ordering (renamed `030_purge_soft_deleted.sql` → `20260419_purge_soft_deleted.sql`; copied `drizzle/0000-0005` into `supabase/migrations/` as `20260101-20260106_drizzle_*.sql`);
       - `CREATE INDEX CONCURRENTLY` in transaction (removed `CONCURRENTLY` keyword from `20260105_drizzle_0004_gin_indexes.sql`);
       - Drift tables created (`20260107_drift_capture_missing_tables.sql`);
       - Duplicate-policy creation in `20260405` (archived the file to `.planning/phases/033-pre-deploy-audit-gate/deprecated-migrations/`);
       - Duplicate version-prefix collisions (renamed `20260401`, `20260406`, `20260409`, `20260416` duplicates to 14-digit `YYYYMMDDHHMMSS` timestamps);
       - References to `invited_by` / `created_by` columns that never existed (archival of `20260405`);
       - Out-of-order column dependency on `open_for_trade` (moved `20260415_open_for_trade_and_rating.sql` earlier to `20260405000000_*.sql`).

    Future readers should treat this ADR as accurate from commit `090bdcc` forward. Audits or refactors looking at the repo state before that commit must be aware of the dev-vs-trail divergence.
    ```

    Then save a grep-confirmation snapshot to `evidence/03a-adr-003-grep.txt`:
    ```bash
    grep -n "090bdcc\|2026-04-23\|drift-capture" .planning/ADR-003-drizzle-dev-only.md > .planning/phases/033.1-audit-gate-closure/evidence/03a-adr-003-grep.txt
    ```
  </action>
  <verify>
    <automated>grep -c "2026-04-23 via commit 090bdcc" .planning/ADR-003-drizzle-dev-only.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "2026-04-23 via commit 090bdcc" .planning/ADR-003-drizzle-dev-only.md` returns 1 (exactly one match — substring is unique to the new section)
    - `grep -c "## Historical Note" .planning/ADR-003-drizzle-dev-only.md` returns 1
    - `grep -c "drift-capture migration" .planning/ADR-003-drizzle-dev-only.md` returns at least 1
    - `evidence/03a-adr-003-grep.txt` exists and is non-empty (contains line numbers proving the new strings are present)
    - The original `## Decision` section is unchanged (run `grep -A 5 "## Decision" .planning/ADR-003-drizzle-dev-only.md` and confirm 4 numbered points are still there)
  </acceptance_criteria>
  <done>
    ADR-003 now documents both the intended state AND the historical state — when the architectural claim became true. Future audits won't re-discover this gap.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create RUNBOOK.md with the .env.local NEXT_PUBLIC_APP_URL prerequisite</name>
  <files>.planning/phases/033.1-audit-gate-closure/RUNBOOK.md</files>
  <read_first>
    - apps/web/.env.local.example (full file — to extract the exact required vars list)
    - .planning/phases/033-pre-deploy-audit-gate/033-VERIFICATION.md gap 5
    - .planning/phases/033-pre-deploy-audit-gate/AUDIT-REPORT.md §1 Finding #3 + §8 "Bonus audit finding"
  </read_first>
  <action>
    Create `.planning/phases/033.1-audit-gate-closure/RUNBOOK.md` with this content:

    ```markdown
    # Phase 33.1 — Operator Runbook

    Local-development prerequisites and gotchas surfaced by the Phase 33 audit. Read this before running ANY plan in `.planning/phases/033.1-audit-gate-closure/`.

    ## 1. Local `.env.local` MUST include NEXT_PUBLIC_APP_URL

    **Symptom:** `pnpm --filter @digswap/web build` fails with a Zod env-schema error like:
    ```
    Invalid environment variables: { NEXT_PUBLIC_APP_URL: 'Required' }
    ```

    **Root cause:** `apps/web/.env.local.example` line 9 declares `NEXT_PUBLIC_APP_URL=http://localhost:3000` as a required var, but the user's local `apps/web/.env.local` (gitignored — never committed) was missing it as of 2026-04-22 (Phase 33 Plan 02 build initially failed for this reason; AUDIT-REPORT.md §1 Finding #3 + §8 Bonus audit finding).

    **Fix:** Add this line to your local `apps/web/.env.local`:
    ```bash
    NEXT_PUBLIC_APP_URL=http://localhost:3000
    ```
    Do NOT commit `.env.local`. Verify with:
    ```bash
    grep -c '^NEXT_PUBLIC_APP_URL=' apps/web/.env.local
    # expected output: 1
    ```

    Then re-run `pnpm --filter @digswap/web build` — it should now succeed.

    ## 2. Other env-var gotchas

    The `apps/web/.env.local.example` file lists 21 required + 4 optional vars (Phase 33 DEP-AUD-08 §8 inventory). The Zod schema in `apps/web/src/lib/env.ts` enforces all 21 required vars at boot. If you re-clone the repo, copy the example file:
    ```bash
    cp apps/web/.env.local.example apps/web/.env.local
    # then fill in real values from the dev Supabase project mrkgoucqcbqjhrdjcnpw
    ```

    ## 3. Vault smoke-test (after Plan 01 lands)

    After Plan 01 (`033.1-01-vault-remediation`) lands, the dev project should have the Vault extension installed. To re-verify before Phase 34, run:
    ```bash
    node --input-type=module -e "import pg from 'pg'; import fs from 'fs'; const url=fs.readFileSync('apps/web/.env.local','utf8').match(/^DATABASE_URL=(.+)$/m)[1].trim(); const c=new pg.Client({connectionString:url}); await c.connect(); const r=await c.query(\"SELECT count(*)::int AS plaintext_count FROM public.discogs_tokens\"); console.log(r.rows[0]); await c.end();"
    # expected output: { plaintext_count: 0 }
    ```

    ## 4. Session-revocation E2E (after Plan 04 lands)

    The Playwright spec `apps/web/tests/e2e/audit/session-revocation.audit.spec.ts` requires two env vars at run-time:
    ```bash
    export AUDIT_USER_EMAIL=audit+33@digswap.test
    export AUDIT_USER_PASSWORD=<chosen-value-from-Plan-04>
    pnpm --filter @digswap/web exec playwright test audit/session-revocation.audit.spec.ts
    ```

    The audit user `audit+33@digswap.test` must exist on dev project `mrkgoucqcbqjhrdjcnpw` with Auto-Confirm ON (created via Supabase Dashboard → Auth → Users → Add user as part of Plan 04 Task 1).
    ```

    Save the file and verify it parses as Markdown (no syntax errors):
    ```bash
    grep -c "^## " .planning/phases/033.1-audit-gate-closure/RUNBOOK.md
    # expected: 4 (four sections)
    ```
  </action>
  <verify>
    <automated>grep -c "NEXT_PUBLIC_APP_URL=http://localhost:3000" .planning/phases/033.1-audit-gate-closure/RUNBOOK.md</automated>
  </verify>
  <acceptance_criteria>
    - `RUNBOOK.md` exists at `.planning/phases/033.1-audit-gate-closure/RUNBOOK.md`
    - `grep -c "NEXT_PUBLIC_APP_URL=http://localhost:3000" .planning/phases/033.1-audit-gate-closure/RUNBOOK.md` returns at least 1
    - `grep -c "^## " .planning/phases/033.1-audit-gate-closure/RUNBOOK.md` returns 4 (four sections: env, gotchas, Vault, session E2E)
    - The file references the Zod env schema and the dev project ref `mrkgoucqcbqjhrdjcnpw`
  </acceptance_criteria>
  <done>
    Operator runbook captures the Plan 02 carry-over (.env.local missing var) and three other Phase 33.1 prerequisites in one place. Future operators don't re-discover the same wire.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3 (checkpoint): Confirm user has updated their local .env.local</name>
  <read_first>
    - .planning/phases/033.1-audit-gate-closure/RUNBOOK.md (Section 1)
    - apps/web/.env.local.example
  </read_first>
  <what-built>
    RUNBOOK.md (Section 1) documents that the user must add `NEXT_PUBLIC_APP_URL=http://localhost:3000` to their LOCAL (gitignored) `apps/web/.env.local` file. Claude cannot do this for the user — `.env.local` is gitignored by design and is user-machine-specific.
  </what-built>
  <how-to-verify>
    Run the following on the user's machine:
    ```bash
    grep -c '^NEXT_PUBLIC_APP_URL=' apps/web/.env.local
    ```
    Expected output: `1`

    If output is `0`:
    1. Open `apps/web/.env.local` in an editor
    2. Add a new line: `NEXT_PUBLIC_APP_URL=http://localhost:3000`
    3. Save the file
    4. Re-run the grep — expected output `1`
    5. As a final sanity check, run `pnpm --filter @digswap/web build` — must exit 0

    Then type one of:
    - `done` — variable added, grep returns 1, build succeeds
    - `skip-accepted` — explicitly accepting the deferred risk (Phase 34 setup will require it then; document the decision for the next operator)
    - `blocked: <reason>` — something prevents the fix (rare — flag for follow-up)
  </how-to-verify>
  <files>apps/web/.env.local</files>
  <action>Wait for user to respond "done", "skip-accepted", or "blocked: <reason>". User runs the grep + edits the local file themselves; Claude cannot read or write apps/web/.env.local because it is gitignored and outside the worktree's tracked surface.</action>
  <verify>
    <automated>echo "User self-attestation only — see 033.1-03 SUMMARY for recorded response"</automated>
  </verify>
  <done>User has typed "done" (verified locally) OR "skip-accepted" (deferred risk acknowledged) OR "blocked: <reason>" (escalated for follow-up); response is recorded in 033.1-03-docs-carry-over-SUMMARY.md.</done>
  <acceptance_criteria>
    User responds with `done`, `skip-accepted`, or `blocked: <reason>`. Decision recorded in `033.1-03-docs-carry-over-SUMMARY.md` so the audit trail captures the resolution.
  </acceptance_criteria>
  <resume-signal>Type "done", "skip-accepted", or "blocked: <reason>".</resume-signal>
</task>

</tasks>

<verification>
Phase-level proof:
```bash
# 1. ADR-003 timeline note present
grep "2026-04-23 via commit 090bdcc" .planning/ADR-003-drizzle-dev-only.md

# 2. RUNBOOK.md exists and covers .env.local
grep "NEXT_PUBLIC_APP_URL=http://localhost:3000" .planning/phases/033.1-audit-gate-closure/RUNBOOK.md

# 3. (User-side, on their machine — checkpoint Task 3 captures this)
grep -c '^NEXT_PUBLIC_APP_URL=' apps/web/.env.local   # expected: 1
```
</verification>

<success_criteria>
- ADR-003 documents BOTH the intended-state decision AND the actual historical timeline (when the claim became true)
- RUNBOOK.md gives any future operator the four most likely setup-time gotchas in one place
- User has either added `NEXT_PUBLIC_APP_URL` to their local `.env.local` or explicitly accepted the deferred risk via the checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/033.1-audit-gate-closure/033.1-03-docs-carry-over-SUMMARY.md` using the summary template. Record the user's checkpoint response (done/skip-accepted/blocked) in the SUMMARY so the audit trail is complete.
</output>
