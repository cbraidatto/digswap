---
phase: 12-release-pages
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - src/app/release/layout.tsx
  - src/app/release/[discogsId]/page.tsx
  - src/app/release/[discogsId]/_components/release-hero.tsx
  - src/app/release/[discogsId]/_components/youtube-embed.tsx
  - src/app/release/[discogsId]/_components/owners-section.tsx
  - src/app/release/[discogsId]/_components/reviews-section.tsx
autonomous: true
requirements:
  - REL-01
  - REL-02
  - REL-03
  - REL-04
  - REL-05
must_haves:
  truths:
    - "Visitor can load /release/[discogsId] without login and see release metadata"
    - "Page shows a clickable Discogs link to https://www.discogs.com/release/{discogsId}"
    - "Page embeds YouTube video from cached videoId or offers search trigger for logged-in users"
    - "Page lists up to 12 owners with avatar, username, and link to /perfil/[username]"
    - "Page shows all platform reviews for this release with pagination"
    - "Page has correct Open Graph meta tags for SEO and social sharing"
  artifacts:
    - path: "src/app/release/layout.tsx"
      provides: "Public route layout with optional auth shell"
      min_lines: 20
    - path: "src/app/release/[discogsId]/page.tsx"
      provides: "Main release page with generateMetadata"
      contains: "generateMetadata"
    - path: "src/app/release/[discogsId]/_components/release-hero.tsx"
      provides: "Cover art + title + metadata + Discogs link"
      contains: "discogs.com/release"
    - path: "src/app/release/[discogsId]/_components/youtube-embed.tsx"
      provides: "Client component for YouTube iframe + search trigger"
      contains: "youtube-nocookie.com"
    - path: "src/app/release/[discogsId]/_components/owners-section.tsx"
      provides: "Owners list with profile links"
      contains: "/perfil/"
    - path: "src/app/release/[discogsId]/_components/reviews-section.tsx"
      provides: "Reviews list using getReviewsForRelease"
      contains: "getReviewsForRelease"
  key_links:
    - from: "src/app/release/[discogsId]/page.tsx"
      to: "src/lib/release/queries.ts"
      via: "getReleaseByDiscogsId import"
      pattern: "getReleaseByDiscogsId"
    - from: "src/app/release/[discogsId]/_components/youtube-embed.tsx"
      to: "src/actions/release.ts"
      via: "searchYouTubeForRelease server action"
      pattern: "searchYouTubeForRelease"
    - from: "src/app/release/[discogsId]/_components/owners-section.tsx"
      to: "src/lib/release/queries.ts"
      via: "getOwnersByReleaseId import"
      pattern: "getOwnersByReleaseId"
---

<objective>
Build the public /release/[discogsId] route with all page components: hero section with Discogs link, YouTube embed, owners list, and reviews section.

Purpose: This is the core deliverable of Phase 12 -- a public, SEO-indexable page for every release that turns each record into an acquisition surface. Visitors discover who has the record and read community reviews without needing an account.
Output: Complete release page route at src/app/release/ with layout + page + 4 components
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-release-pages/12-CONTEXT.md
@.planning/phases/12-release-pages/12-RESEARCH.md
@.planning/phases/12-release-pages/12-01-SUMMARY.md

@src/app/perfil/layout.tsx
@src/app/perfil/[username]/page.tsx
@src/app/u/[username]/bounty/page.tsx
@src/lib/release/queries.ts
@src/actions/release.ts
@src/lib/community/queries.ts
@src/lib/collection/rarity.ts

<interfaces>
<!-- Contracts from Plan 01 that this plan consumes -->

From src/lib/release/queries.ts (created in Plan 01):
```typescript
export async function getReleaseByDiscogsId(discogsId: number): Promise<ReleaseRow | null>;

export interface ReleaseOwner {
  userId: string;
  username: string | null;
  avatarUrl: string | null;
  displayName: string | null;
  conditionGrade: string | null;
}

export async function getOwnersByReleaseId(releaseId: string, limit?: number): Promise<ReleaseOwner[]>;
export async function getOwnerCountByReleaseId(releaseId: string): Promise<number>;
```

From src/actions/release.ts (created in Plan 01):
```typescript
export async function searchYouTubeForRelease(releaseInternalId: string): Promise<{ videoId: string | null; error?: string }>;
```

From src/lib/community/queries.ts (existing):
```typescript
export type ReviewItem = {
  id: string;
  userId: string;
  username: string | null;
  avatarUrl: string | null;
  rating: number;
  title: string | null;
  body: string | null;
  isPressingSpecific: boolean;
  pressingDetails: string | null;
  createdAt: string;
};
export async function getReviewsForRelease(releaseId: string, cursor?: string, limit?: number): Promise<ReviewItem[]>;
export async function getReviewCountForRelease(releaseId: string): Promise<number>;
```

From src/lib/collection/rarity.ts (existing):
```typescript
export type RarityTier = "Ultra Rare" | "Rare" | "Common" | null;
export function getRarityTier(score: number | null): RarityTier;
export function getRarityBadgeVariant(tier: RarityTier): string;
```

From src/app/perfil/layout.tsx (pattern to copy exactly):
```typescript
// Public route layout -- NO redirect for unauthenticated users
// If user logged in: render AppShell with full nav
// If visitor: render minimal header with SIGN_IN / START_DIGGING links
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Release layout + page with generateMetadata + hero component</name>
  <files>src/app/release/layout.tsx, src/app/release/[discogsId]/page.tsx, src/app/release/[discogsId]/_components/release-hero.tsx</files>
  <action>
**src/app/release/layout.tsx:**
Copy the exact pattern from src/app/perfil/layout.tsx. This is a public route outside the (protected) group. The layout:
- Calls createClient().auth.getUser() to check auth state
- If user exists: fetches profile (displayName, avatarUrl) via Drizzle db, renders AppShell with ImportBanner
- If no user: renders minimal div with bg-background, a border-b header bar containing "DIGSWAP_v1.0.0" label + "SIGN_IN" and "START_DIGGING" links
- NO redirect for unauthenticated visitors -- this is the critical difference from protected routes

**src/app/release/[discogsId]/page.tsx:**
Server Component. This is the main release page.

1. `generateMetadata()` export:
   - Extract discogsId from params (await params per Next.js 15 pattern)
   - Call getReleaseByDiscogsId(Number(discogsId))
   - If no release: return `{ title: "Release Not Found - DigSwap" }`
   - Return metadata with:
     - title: `${release.title} by ${release.artist} - DigSwap`
     - description: `${release.title} by ${release.artist}${release.year ? ` (${release.year})` : ""}. See who owns this release and read reviews on DigSwap.`
     - openGraph.title: `${release.title} - ${release.artist}`
     - openGraph.description: `Discover who has this record in their collection on DigSwap.`
     - openGraph.images: if release.coverImageUrl, use it with width/height 300, alt = release.title. Use type "music.album".

2. Default export page component:
   - Await params, parse discogsId as number
   - Fetch release via getReleaseByDiscogsId. If not found, call notFound() from next/navigation.
   - Check auth state: createClient().auth.getUser() to determine if user is logged in (for YouTube search trigger)
   - Render page with Ghost Protocol aesthetic:
     - Section header: font-mono text-[10px] text-primary tracking-[0.2em] "RELEASE_PAGE"
     - ReleaseHero component (cover art + metadata + Discogs link)
     - YouTubeEmbed component (pass videoId from release, releaseId as internal UUID, isAuthenticated boolean)
     - OwnersSection component (pass releaseId internal UUID)
     - ReviewsSection component (pass releaseId internal UUID)
   - Page container: max-w-4xl mx-auto px-4 py-6 space-y-8

**src/app/release/[discogsId]/_components/release-hero.tsx:**
Server Component. Displays the release hero section.

Props: `{ release: { title, artist, year, genre, style, format, label, country, coverImageUrl, discogsId, rarityScore, discogsHave, discogsWant } }`

Layout:
- Flex row (md:flex-row, flex-col on mobile)
- Left: Cover art using next/image (300x300, aspect-square, rounded-lg, object-cover). Fallback: material-symbols "album" icon in centered placeholder div.
- Right: Text content:
  - Title: font-heading text-2xl font-bold text-on-surface
  - Artist: text-lg text-on-surface-variant
  - Metadata row (font-mono text-[11px] text-on-surface-variant): year | format | label | country -- only show non-null fields, separated by " / "
  - Genre chips: if genre array exists, map to small Badge components (variant=outline, text-[10px])
  - Rarity badge: use getRarityTier() and getRarityBadgeVariant() from src/lib/collection/rarity.ts
  - Discogs stats: font-mono text-[10px] showing "HAVE: {discogsHave} / WANT: {discogsWant}" (per REL-02 context)
  - Discogs link: `<a href="https://www.discogs.com/release/${release.discogsId}" target="_blank" rel="noopener noreferrer">` styled as font-mono text-[10px] text-primary hover:underline with material-symbols "open_in_new" icon. Text: "VIEW_ON_DISCOGS". Only render when discogsId is non-null.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Layout renders AppShell for logged-in users, minimal header for visitors. Page fetches release by discogsId, returns 404 if not found. generateMetadata produces correct OG tags. Hero shows cover art, metadata, rarity badge, and Discogs link.</done>
</task>

<task type="auto">
  <name>Task 2: YouTube embed + owners section + reviews section components</name>
  <files>src/app/release/[discogsId]/_components/youtube-embed.tsx, src/app/release/[discogsId]/_components/owners-section.tsx, src/app/release/[discogsId]/_components/reviews-section.tsx</files>
  <action>
**src/app/release/[discogsId]/_components/youtube-embed.tsx:**
Client Component ("use client"). Handles YouTube iframe or search trigger.

Props: `{ videoId: string | null; releaseId: string; isAuthenticated: boolean }`

Behavior:
- If videoId is set: render iframe embed. Use `https://www.youtube-nocookie.com/embed/${videoId}` for privacy-enhanced mode. Iframe attrs: width="100%", aspect-video container (aspect-ratio: 16/9), rounded-lg, allowFullScreen, allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture". Wrap in a section with header "YOUTUBE_PREVIEW" (font-mono text-[10px] text-primary tracking-[0.2em]).
- If videoId is null AND isAuthenticated: show a button "SEARCH_YOUTUBE" (font-mono text-[10px], bg-surface-container-low border border-outline-variant/20 rounded px-3 py-2 hover:border-primary/30). On click, call searchYouTubeForRelease(releaseId) via import from src/actions/release.ts. Show loading state with "SEARCHING..." text. On success, if videoId returned, update local state to render the iframe. On failure or null result, show "No video found for this release" in text-on-surface-variant.
- If videoId is null AND NOT isAuthenticated: render nothing (return null). Per locked decision, section hidden gracefully when no result.
- Use useState for local videoId state (initialized from prop), isPending for loading state.

**src/app/release/[discogsId]/_components/owners-section.tsx:**
Server Component. Shows platform users who own this release.

Props: `{ releaseId: string }`

Behavior:
- Call getOwnersByReleaseId(releaseId, 12) and getOwnerCountByReleaseId(releaseId) from src/lib/release/queries.ts
- If no owners: show empty state "No diggers on DigSwap own this release yet." (font-mono text-[11px] text-on-surface-variant)
- Section header: "OWNERS" (font-mono text-[10px] text-primary tracking-[0.2em]) with count badge showing total owner count
- Render owner cards in a grid (grid-cols-2 sm:grid-cols-3 md:grid-cols-4 gap-3). Each card per locked decision shows:
  - Avatar: 40x40 rounded, fallback to first letter of username in bg-surface-container-high
  - Username as Link to /perfil/[username] (font-mono text-[11px] text-on-surface hover:text-primary)
  - Display name below if different from username (text-[10px] text-on-surface-variant)
  - Condition grade badge if set (font-mono text-[9px])
- Same visual language as radar cards per locked decision -- use the same bg-surface-container-low border border-outline-variant/10 rounded pattern with hover:border-outline-variant/30 transition
- If ownerCount > 12, show "VER_TODOS ({ownerCount})" link at bottom (font-mono text-[10px] text-primary hover:underline). For now this link does not navigate anywhere functional -- it is a placeholder for a future paginated owners page. Style it as a non-functional span or link to the same page with #owners anchor.
- Per locked decision: NO inline REQUEST_TRADE button -- trade initiation stays on profile pages.

**src/app/release/[discogsId]/_components/reviews-section.tsx:**
Client Component ("use client"). Shows all platform reviews with load-more pagination.

Props: `{ releaseId: string; initialReviews: ReviewItem[]; initialCount: number }`

The parent page.tsx should pre-fetch the first page of reviews (limit 10) and the count, passing them as props to avoid a client-side fetch on initial load.

Behavior:
- Section header: "REVIEWS" (font-mono text-[10px] text-primary tracking-[0.2em]) with count showing initialCount
- If no reviews: show "No reviews yet. Be the first to review this release." (font-mono text-[11px] text-on-surface-variant)
- Render each review as a card (bg-surface-container-low border border-outline-variant/10 rounded-lg p-4):
  - Header row: avatar (32x32 rounded) + username (Link to /perfil/[username], font-mono text-[11px]) + date (font-mono text-[9px] text-on-surface-variant, formatted as relative or ISO date)
  - Star rating: render review.rating as filled/empty star material-symbols icons (font-size 14px, text-secondary for filled, text-on-surface-variant/30 for empty). 1-5 scale.
  - Review title if present (font-heading text-sm font-semibold)
  - Review body if present (text-sm text-on-surface-variant)
  - If isPressingSpecific, show a small badge "PRESSING_SPECIFIC" (font-mono text-[9px])
- "LOAD_MORE" button at bottom if reviews.length < initialCount. On click, call getReviewsForRelease via a server action wrapper (create a small getMoreReviews action in src/actions/release.ts that wraps getReviewsForRelease with the cursor from the last review's createdAt). Append results to local state.
- Use useState for reviews array, loading state.

Add the `getMoreReviews` server action to src/actions/release.ts:
```typescript
"use server";
export async function getMoreReviews(releaseId: string, cursor: string, limit = 10) {
  return getReviewsForRelease(releaseId, cursor, limit);
}
```

Update page.tsx to pre-fetch reviews:
- Import getReviewsForRelease and getReviewCountForRelease from src/lib/community/queries.ts
- Call both in the page component, pass results as initialReviews and initialCount to ReviewsSection
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>YouTube embed renders iframe for cached videos, offers search for authenticated users, hides for visitors. Owners section shows up to 12 owner cards with profile links. Reviews section shows paginated reviews with load-more. All three components render correctly within the release page layout.</done>
</task>

</tasks>

<verification>
- Visit /release/[validDiscogsId] in browser -- page loads with all four sections
- Visit /release/[invalidId] -- returns 404
- Visit while logged out -- minimal header, no YouTube search button, owners and reviews still visible
- View page source or inspect -- OG meta tags present with release title and cover image
- YouTube iframe uses youtube-nocookie.com domain
- Owner cards link to /perfil/[username]
- No REQUEST_TRADE button anywhere on the page
</verification>

<success_criteria>
1. /release/[discogsId] loads without authentication (public route)
2. generateMetadata produces correct OG tags (title, description, image)
3. Discogs link points to https://www.discogs.com/release/{discogsId}
4. YouTube embed shows cached video or search trigger (authenticated only)
5. Owners section shows up to 12 owners with profile links
6. Reviews section shows all reviews with load-more pagination
7. Ghost Protocol visual identity: mono font, tracking, material symbols, surface containers
</success_criteria>

<output>
After completion, create `.planning/phases/12-release-pages/12-02-SUMMARY.md`
</output>
