# 16-05 — Tests + human verification checkpoint

**Owner**: Split (Codex = entitlement/webhook tests, Claude = UI smoke tests)
**Phase**: 16 — Monetization
**Depends on**: 16-01, 16-02, 16-03, 16-04

## Goal

Unit test coverage for entitlement logic and webhook handler, plus a human checkpoint confirming the full freemium flow works end-to-end.

## Tasks (Codex)

### Task 1 — Entitlement unit tests

File: `apps/web/tests/unit/entitlements.test.ts`

Test cases:
- `canInitiateTrade`: free user under limit → allowed
- `canInitiateTrade`: free user at limit (5) → blocked, reason = "limit_reached"
- `canInitiateTrade`: premium user with 100 trades → allowed, reason = "no_limit"
- `getUserSubscription`: null subscription row → returns null (treated as free)
- `getUserSubscription`: `tradesMonthReset` > 30 days ago → resets counter to 0
- `incrementTradeCount`: free user → increments counter
- `incrementTradeCount`: premium user → no-op

### Task 2 — Webhook handler unit tests

File: `apps/web/tests/unit/api/stripe-webhook.test.ts`

Test cases:
- Invalid signature → 400 response
- `checkout.session.completed` → subscription row upserted, `profiles.subscription_tier` updated
- `customer.subscription.deleted` → plan reverted to "free"
- `invoice.payment_failed` → status set to "past_due"
- Unknown event type → 200 received (no crash)

Mock `stripe.webhooks.constructEvent` to return mock event payloads. Mock Supabase service-role client.

### Task 3 — `createTradeRequest` integration test

File: `apps/web/tests/unit/actions/trades.test.ts` (add cases):
- Free user under limit → trade created, counter incremented
- Free user at limit → returns `{ error: "TRADE_LIMIT_REACHED" }`, no trade inserted
- Premium user → trade created, counter NOT incremented

## Tasks (Claude)

### Task 4 — Pricing page smoke test (Playwright)

File: `apps/web/tests/e2e/pricing.spec.ts`

- `/pricing` loads and shows both plan cards
- Toggle switches between monthly/annual price display
- Signed-out user: "Get started free" links to `/signup`
- Signed-in free user: "Upgrade to Premium" button is visible

### Task 5 — Human verification checkpoint

**Manual QA checklist** (record in this file after execution):

- [ ] Create test Stripe subscription via Stripe test mode checkout
- [ ] Confirm webhook received + subscription row updated in Supabase
- [ ] Confirm `/settings/billing` shows "PREMIUM" plan state
- [ ] Confirm premium badge appears on profile
- [ ] Cancel via Stripe Customer Portal → plan reverts to free
- [ ] Confirm quota banner reappears on `/trades`
- [ ] Create 5 trades as free user → 6th attempt shows `TradeLimitModal`
- [ ] Wait for month reset (or manually trigger by backfilling `tradesMonthReset`) → trades reset to 0

## Verification

- [ ] All unit tests pass: `pnpm --dir apps/web exec vitest run tests/unit/entitlements.test.ts`
- [ ] All unit tests pass: `pnpm --dir apps/web exec vitest run tests/unit/api/stripe-webhook.test.ts`
- [ ] Playwright pricing test passes
- [ ] Human checkpoint items checked off
