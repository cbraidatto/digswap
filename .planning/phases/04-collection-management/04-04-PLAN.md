---
phase: 04-collection-management
plan: 04
type: execute
wave: 3
depends_on: ["04-02", "04-03"]
files_modified:
  - tests/unit/lib/collection/rarity.test.ts
  - tests/unit/lib/collection/filters.test.ts
  - tests/unit/components/collection/collection-grid.test.tsx
  - tests/unit/components/collection/add-record-dialog.test.tsx
  - tests/integration/collection/add-record.test.ts
  - tests/integration/collection/condition.test.ts
  - tests/integration/collection/public-profile.test.ts
  - tests/integration/collection/sort.test.ts
autonomous: false
requirements:
  - COLL-01
  - COLL-02
  - COLL-03
  - COLL-04
  - COLL-05
  - COLL-06

must_haves:
  truths:
    - "All 8 test files have real implementations replacing test.todo() stubs"
    - "Unit tests for rarity tier mapping cover all threshold boundaries"
    - "Unit tests for filters cover decade mapping and schema validation"
    - "Integration tests verify server action behavior with mocked dependencies"
    - "Component tests verify card rendering with rarity badges and grid layout"
    - "Human verifies full collection management flow end-to-end"
  artifacts:
    - path: "tests/unit/lib/collection/rarity.test.ts"
      provides: "Rarity tier mapping tests"
      min_lines: 30
    - path: "tests/unit/lib/collection/filters.test.ts"
      provides: "Filter schema and decade mapping tests"
      min_lines: 25
    - path: "tests/unit/components/collection/collection-grid.test.tsx"
      provides: "Collection grid rendering tests"
      min_lines: 30
    - path: "tests/integration/collection/add-record.test.ts"
      provides: "Add record server action integration tests"
      min_lines: 40
  key_links:
    - from: "tests/unit/lib/collection/rarity.test.ts"
      to: "src/lib/collection/rarity.ts"
      via: "import getRarityTier, getRarityBadgeVariant"
      pattern: "getRarityTier"
    - from: "tests/integration/collection/add-record.test.ts"
      to: "src/actions/collection.ts"
      via: "import addRecordToCollection, searchDiscogs"
      pattern: "addRecordToCollection"
---

<objective>
Replace all Wave 0 test.todo() stubs with real test implementations and run a human verification checkpoint to confirm the full collection management flow works end-to-end.

Purpose: Ensures all COLL-01 through COLL-06 requirements have automated test coverage and the visual/functional experience is verified by a human.
Output: 8 test files with real implementations, all passing. Human-verified collection flow.
</objective>

<execution_context>
@C:\Users\INTEL\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\INTEL\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-collection-management/04-CONTEXT.md
@.planning/phases/04-collection-management/04-01-SUMMARY.md
@.planning/phases/04-collection-management/04-02-SUMMARY.md
@.planning/phases/04-collection-management/04-03-SUMMARY.md

<interfaces>
<!-- Key contracts to test against -->

From src/lib/collection/rarity.ts:
```typescript
export type RarityTier = "Ultra Rare" | "Rare" | "Common" | null;
export function getRarityTier(score: number | null): RarityTier;
export function getRarityBadgeVariant(tier: RarityTier): "destructive" | "default" | "secondary" | "outline";
```

From src/lib/collection/filters.ts:
```typescript
export const CONDITION_GRADES = ["Mint", "VG+", "VG", "G+", "G", "F", "P"] as const;
export const DECADES = [{ label: "50s", startYear: 1950 }, ...] as const;
export const SORT_OPTIONS = [{ value: "rarity", label: "Rarity" }, ...] as const;
export function getDecadeRange(decade: string): { start: number; end: number } | null;
export const collectionFilterSchema: z.ZodObject<...>;
```

From src/actions/collection.ts:
```typescript
export async function searchDiscogs(query: string): Promise<SearchResult[]>;
export async function addRecordToCollection(discogsId: number): Promise<{ success: true } | { error: string }>;
export async function updateConditionGrade(collectionItemId: string, grade: ConditionGrade): Promise<{ success: true } | { error: string }>;
```

Test patterns from Phase 3 (established):
- vi.mock() for full module isolation
- No real DB or API calls
- Mock Supabase client with .from().select().eq().single() chains
- Mock Discogs client with database().search() chains
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement all unit and integration test files</name>
  <files>
    tests/unit/lib/collection/rarity.test.ts,
    tests/unit/lib/collection/filters.test.ts,
    tests/unit/components/collection/collection-grid.test.tsx,
    tests/unit/components/collection/add-record-dialog.test.tsx,
    tests/integration/collection/add-record.test.ts,
    tests/integration/collection/condition.test.ts,
    tests/integration/collection/public-profile.test.ts,
    tests/integration/collection/sort.test.ts
  </files>
  <read_first>
    tests/unit/lib/collection/rarity.test.ts,
    tests/unit/lib/collection/filters.test.ts,
    tests/unit/components/collection/collection-grid.test.tsx,
    tests/unit/components/collection/add-record-dialog.test.tsx,
    tests/integration/collection/add-record.test.ts,
    tests/integration/collection/condition.test.ts,
    tests/integration/collection/public-profile.test.ts,
    tests/integration/collection/sort.test.ts,
    src/lib/collection/rarity.ts,
    src/lib/collection/filters.ts,
    src/lib/collection/queries.ts,
    src/actions/collection.ts,
    src/app/(protected)/(profile)/perfil/_components/collection-grid.tsx,
    src/app/(protected)/(profile)/perfil/_components/collection-card.tsx,
    src/app/(protected)/(profile)/perfil/_components/add-record-dialog.tsx,
    tests/unit/lib/discogs/import-worker.test.ts,
    tests/integration/discogs/disconnect.test.ts
  </read_first>
  <action>
    Replace all `test.todo()` stubs with real test implementations. Follow the Phase 3 test patterns: `vi.mock()` for module isolation, no real DB or API calls.

    **1. `tests/unit/lib/collection/rarity.test.ts`** (COLL-02):

    Direct function tests -- no mocking needed:
    ```typescript
    import { getRarityTier, getRarityBadgeVariant, type RarityTier } from "@/lib/collection/rarity";

    describe("getRarityTier", () => {
      test("returns 'Ultra Rare' for score >= 2.0", () => {
        expect(getRarityTier(2.0)).toBe("Ultra Rare");
        expect(getRarityTier(5.5)).toBe("Ultra Rare");
        expect(getRarityTier(100)).toBe("Ultra Rare");
      });
      test("returns 'Rare' for score >= 0.5 and < 2.0", () => {
        expect(getRarityTier(0.5)).toBe("Rare");
        expect(getRarityTier(1.0)).toBe("Rare");
        expect(getRarityTier(1.99)).toBe("Rare");
      });
      test("returns 'Common' for score < 0.5", () => {
        expect(getRarityTier(0.0)).toBe("Common");
        expect(getRarityTier(0.1)).toBe("Common");
        expect(getRarityTier(0.49)).toBe("Common");
      });
      test("returns null for null score", () => {
        expect(getRarityTier(null)).toBeNull();
      });
      test("returns null for undefined score", () => {
        expect(getRarityTier(undefined as unknown as null)).toBeNull();
      });
    });

    describe("getRarityBadgeVariant", () => {
      test("returns 'destructive' for Ultra Rare", () => {
        expect(getRarityBadgeVariant("Ultra Rare")).toBe("destructive");
      });
      test("returns 'default' for Rare", () => {
        expect(getRarityBadgeVariant("Rare")).toBe("default");
      });
      test("returns 'secondary' for Common", () => {
        expect(getRarityBadgeVariant("Common")).toBe("secondary");
      });
      test("returns 'outline' for null", () => {
        expect(getRarityBadgeVariant(null)).toBe("outline");
      });
    });
    ```

    **2. `tests/unit/lib/collection/filters.test.ts`** (COLL-04):

    Direct function + schema tests:
    ```typescript
    import { getDecadeRange, collectionFilterSchema, DECADES, CONDITION_GRADES } from "@/lib/collection/filters";

    describe("getDecadeRange", () => {
      test("returns {start: 1980, end: 1990} for '80s'", () => {
        expect(getDecadeRange("80s")).toEqual({ start: 1980, end: 1990 });
      });
      test("returns {start: 1950, end: 1960} for '50s'", () => {
        expect(getDecadeRange("50s")).toEqual({ start: 1950, end: 1960 });
      });
      test("returns null for invalid decade string", () => {
        expect(getDecadeRange("invalid")).toBeNull();
        expect(getDecadeRange("30s")).toBeNull();
      });
    });

    describe("collectionFilterSchema", () => {
      test("provides default sort='rarity' and page=1 when empty", () => {
        const result = collectionFilterSchema.parse({});
        expect(result.sort).toBe("rarity");
        expect(result.page).toBe(1);
      });
      test("validates sort enum rejects invalid values", () => {
        expect(() => collectionFilterSchema.parse({ sort: "invalid" })).toThrow();
      });
      test("coerces page string to number", () => {
        const result = collectionFilterSchema.parse({ page: "3" });
        expect(result.page).toBe(3);
      });
      test("passes through genre, decade, format as optional strings", () => {
        const result = collectionFilterSchema.parse({ genre: "Jazz", decade: "80s", format: "LP" });
        expect(result.genre).toBe("Jazz");
        expect(result.decade).toBe("80s");
        expect(result.format).toBe("LP");
      });
    });

    describe("CONDITION_GRADES", () => {
      test("contains exactly 7 grades in Discogs order", () => {
        expect(CONDITION_GRADES).toEqual(["Mint", "VG+", "VG", "G+", "G", "F", "P"]);
      });
    });
    ```

    **3. `tests/unit/components/collection/collection-grid.test.tsx`** (COLL-01):

    Mock the CollectionCard import, test grid rendering:
    - Mock `@/app/(protected)/(profile)/perfil/_components/collection-card` to render a simple div with data-testid
    - Test: renders correct number of cards for given items array
    - Test: renders empty state when items is empty (contains "No records found" or similar text)
    - Test: grid has responsive classes `grid-cols-2 md:grid-cols-3 lg:grid-cols-4`

    **4. `tests/unit/components/collection/add-record-dialog.test.tsx`** (COLL-03):

    Mock `@/actions/collection` server actions, test component behavior:
    - Mock `searchDiscogs` to return fake results
    - Mock `addRecordToCollection` to return `{ success: true }`
    - Mock `next/navigation` `useRouter` to provide a mock `refresh`
    - Test: renders search input
    - Test: calls searchDiscogs after typing (with debounce -- may need `vi.advanceTimersByTime`)
    - Test: displays search results
    - Test: shows loading state during search

    **5. `tests/integration/collection/add-record.test.ts`** (COLL-03):

    Mock `@/lib/supabase/server`, `@/lib/supabase/admin`, `@/lib/discogs/client`:
    - Mock `createClient` to return auth user
    - Mock `createAdminClient` with chainable `.from().select().eq().maybeSingle()` and `.from().insert()`
    - Mock `createDiscogsClient` with `.database().getRelease()` returning fake release data
    - Test: inserts new release and collection item for unknown discogsId
    - Test: reuses existing release for known discogsId (admin.from("releases").select().eq().maybeSingle() returns data)
    - Test: returns error for duplicate collection item
    - Test: throws for unauthenticated user (createClient returns null user)

    **6. `tests/integration/collection/condition.test.ts`** (COLL-06):

    Same mocking pattern:
    - Test: updates condition grade on own collection item (admin.from("collection_items").update().eq().eq() succeeds)
    - Test: rejects invalid grade value (not in CONDITION_GRADES)
    - Test: rejects update on another user's item (the .eq("user_id", user.id) filter means update returns 0 rows -- action returns error)

    **7. `tests/integration/collection/public-profile.test.ts`** (COLL-01):

    Mock `@/lib/db` and test the query logic:
    - Test: returns collection data for valid username (mock db.select returns profile row, then collection items)
    - Test: handles non-existent username (mock db.select returns empty array)

    **8. `tests/integration/collection/sort.test.ts`** (COLL-05):

    Test the getCollectionPage query helper directly with mocked db:
    - Test: sort=rarity produces `desc(COALESCE(rarityScore, -1))` ordering (verify order of returned mock data)
    - Test: sort=date produces `desc(createdAt)` ordering
    - Test: sort=alpha produces `asc(title)` ordering
    - Test: null rarity scores sort last when sort=rarity

    For sort tests, it may be more practical to test the `getCollectionPage` function with a mocked `db` that captures the `orderBy` argument, or test the end result ordering with pre-seeded mock data.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/lib/collection/ tests/unit/components/collection/ tests/integration/collection/ --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - tests/unit/lib/collection/rarity.test.ts does NOT contain `test.todo`
    - tests/unit/lib/collection/rarity.test.ts contains `expect(getRarityTier(2.0)).toBe("Ultra Rare")`
    - tests/unit/lib/collection/filters.test.ts does NOT contain `test.todo`
    - tests/unit/lib/collection/filters.test.ts contains `expect(getDecadeRange("80s")).toEqual`
    - tests/unit/components/collection/collection-grid.test.tsx does NOT contain `test.todo`
    - tests/unit/components/collection/add-record-dialog.test.tsx does NOT contain `test.todo`
    - tests/integration/collection/add-record.test.ts does NOT contain `test.todo`
    - tests/integration/collection/add-record.test.ts contains `vi.mock`
    - tests/integration/collection/condition.test.ts does NOT contain `test.todo`
    - tests/integration/collection/condition.test.ts contains `CONDITION_GRADES` or `updateConditionGrade`
    - tests/integration/collection/public-profile.test.ts does NOT contain `test.todo`
    - tests/integration/collection/sort.test.ts does NOT contain `test.todo`
    - `npx vitest run tests/unit/lib/collection/ tests/unit/components/collection/ tests/integration/collection/` exits with code 0 (all tests pass)
  </acceptance_criteria>
  <done>
    All 8 test files have real implementations. No test.todo() stubs remain. All tests pass. COLL-01 through COLL-06 have automated test coverage.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of collection management flow</name>
  <files>none -- verification only</files>
  <action>
    Human verifies the complete collection management system built across Plans 01-04.
    Start dev server with `npm run dev`, then walk through all verification steps below.
    This checkpoint confirms visual correctness, functional behavior, and public/private access controls.

    What was built:
    1. Public profile page at /perfil/[username] accessible without login
    2. Own profile page at /perfil with collection grid, filter chips, sort options, pagination
    3. Rarity badges (Common/Rare/Ultra Rare) on each collection card
    4. FAB button to add records via Discogs search
    5. Condition grade editor via popover on owned cards
    6. Filter by genre, decade, format with URL-based state
    7. Sort by rarity (default), date added, A-Z

    Steps to verify:
    1. Start dev server: `npm run dev`
    2. Log in to your account
    3. Navigate to /perfil -- verify:
       - Collection displays as card grid (2-4 columns depending on screen width)
       - Cards show cover art, title, artist, rarity badge
       - Filter chips row visible below stats section: Genero, Decada, Formato, Ordenar
       - Click each filter dropdown -- verify options populate
       - Select a genre filter -- URL updates with ?genre=... and grid filters
       - Select sort "A-Z" -- verify alphabetical ordering
       - FAB (round + button) visible in bottom-right above the nav bar
    4. Click the FAB -- verify:
       - Search dialog opens
       - Type a record name (e.g., "Kind of Blue Miles Davis")
       - After ~300ms debounce, search results appear with thumbnails
       - Click a result to add it
       - Toast confirms "Record added" and grid refreshes showing the new record
    5. On a card you own -- verify:
       - Condition grade editor visible (small button showing grade or "Grade")
       - Click it -- popover opens with 7 grade options
       - Select "VG+" -- popover closes, grade updates
    6. Open an incognito/private window (not logged in)
       - Navigate to /perfil/[your-username]
       - Verify: collection is visible, no FAB, no condition editor
       - Verify: filter and sort work
    7. Navigate to /perfil/nonexistent-user -- verify: 404 page shown
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose 2>&1 | tail -5</automated>
  </verify>
  <done>
    Human confirms: collection grid renders correctly, filters and sort work, FAB opens search dialog and adds records, condition editor updates grades, public profile is accessible without login, and 404 shows for invalid usernames. Type "approved" or describe issues.
  </done>
</task>

</tasks>

<verification>
- `npx vitest run tests/unit/lib/collection/ tests/unit/components/collection/ tests/integration/collection/ --reporter=verbose` -- all tests pass
- `npx vitest run` -- full suite passes (no regressions from Phase 1-3 tests)
- Human verification confirms visual and functional correctness of the entire Phase 4 feature set
</verification>

<success_criteria>
1. All 8 test files contain real implementations (no test.todo remaining)
2. All tests pass with `npx vitest run`
3. Human confirms: collection grid renders, filters work, FAB adds records, condition editor works
4. Human confirms: public profile accessible without login, 404 for invalid usernames
5. No regressions in Phase 1-3 test suites
</success_criteria>

<output>
After completion, create `.planning/phases/04-collection-management/04-04-SUMMARY.md`
</output>
