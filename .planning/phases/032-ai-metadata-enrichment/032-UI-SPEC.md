---
phase: 32
slug: ai-metadata-enrichment
status: draft
shadcn_initialized: false
preset: none
created: 2026-04-15
---

# Phase 32 — UI Design Contract

> Visual and interaction contract for AI Metadata Enrichment in the Electron desktop app. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (Electron desktop — no shadcn) |
| Preset | not applicable |
| Component library | none (custom React components with Tailwind utility classes) |
| Icon library | Inline SVG (established desktop pattern — no external icon library) |
| Font | System font stack via Tailwind defaults (`-webkit-font-smoothing: antialiased`) |

**Note:** The desktop app does not use shadcn/ui. All components are hand-built with Tailwind CSS utility classes and inline hex colors. This phase follows the same pattern.

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps (sparkle icon margin-right), inline badge padding |
| sm | 8px | Compact row padding (`py-2`), button gaps, inline edit vertical padding (`py-1`) |
| md | 16px | Default element spacing (`px-4`), section gaps (`gap-4`) |
| lg | 24px | Section padding (`p-6`, `gap-6`) |
| xl | 32px | Not used in this phase |
| 2xl | 48px | Not used in this phase |
| 3xl | 64px | Not used in this phase |

No new spacing exceptions declared in this phase. Existing codebase uses `py-1.5` (6px) in `LibraryAlbumView.tsx` track rows and `SettingsScreen.tsx` buttons; these are pre-existing inherited values, not tokens introduced by this phase. New components in this phase use only the 4-multiple scale above.

---

## Typography

| Role | Size | Weight | Line Height | Usage in this phase |
|------|------|--------|-------------|---------------------|
| Body | 14px (`text-sm`) | 400 (normal) | 1.5 | Track metadata cells, progress text, badge labels |
| Label | 12px (`text-xs`) | 600 (`font-semibold`) | 1.5 | Column headers, section labels, progress counters, confidence tooltips |
| Heading | 20px (`text-xl`) | 600 (`font-semibold`) | 1.2 | Empty state heading ("Minha Biblioteca" — existing, not new) |
| Mono | 12px (`text-xs font-mono`) | 400 (normal) | 1.5 | File format, bitrate, duration columns, API key display |

**Constraint:** Maximum 2 weights used: 400 (normal) and 600 (semibold). No bold (700) in the desktop app.

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `#0d0d0d` | App background, input backgrounds, overlay backgrounds |
| Secondary (30%) | `#111008` | Header bars, sticky headers, card backgrounds, active tab backgrounds |
| Accent (10%) | `#c8914a` | See reserved-for list below |
| Destructive | `#e85c5c` | Error text, error counters only |

### Accent (`#c8914a`) reserved for:

1. **"Enriquecer IA" CTA button** — primary action background
2. **Active tab indicator** — tab bar underline (existing)
3. **Progress bar fill** — enrichment progress bar (matches scan progress bar)
4. **Scanning/progress label** — uppercase tracking label during enrichment
5. **Sparkle icon on AI-inferred fields** — the AI badge indicator color

### Extended palette (established in codebase, not new):

| Color | Hex | Role |
|-------|-----|------|
| Primary text | `#e8dcc8` | High-confidence metadata, headings, primary content |
| Secondary text | `#7a6e5f` | Album names, file counts, descriptions, non-critical labels |
| Tertiary text | `#4a4035` | Column headers, null placeholders (`--`), format/bitrate/duration mono text |
| Low-confidence text | `#8b7355` | Folder-inferred metadata (italic, existing pattern) |
| AI-confidence text | `#c8914a` | AI-inferred metadata (with sparkle icon, new for this phase) |
| Border | `#2a2218` | Table borders, card borders, input borders |
| Border light | `#2a2218/50` | Row separators (50% opacity variant) |
| Hover surface | `#111008/50` | Row hover state |
| Active surface | `#1a1508` | Active toggle background, selected state |
| Input hover | `#221f14` | Button hover fill (from LoginScreen pattern) |
| Success | `#7ac87a` | Success messages (magic link sent — existing, reuse for "enrichment complete") |
| Error | `#e85c5c` | Error text, error count badges |
| Error surface | `#241419/90` | Error summary card background |
| Error border | `#6b3940` | Error summary card border |

---

## Focal Point

**Default library state:** The "Enriquecer IA" button in the header bar is the primary focal point. It uses the accent color (`#c8914a`) background against the dark header (`#111008`), drawing the eye as the only warm-filled element in the header row.

**Enrichment state:** The progress bar fill (`#c8914a` on `#1a1508` track) becomes the visual anchor. The centered layout with the animated fill creates a single point of attention during the enrichment process.

---

## Component Inventory

### New Components

| Component | Location | Description |
|-----------|----------|-------------|
| Enrich button | `LibraryScreen.tsx` header bar | "Enriquecer IA" CTA next to Re-scan buttons |
| Enrichment progress | `LibraryScreen.tsx` | Progress bar + counter overlay during AI inference |
| AI sparkle badge | `LibraryListView.tsx`, `LibraryAlbumView.tsx` | Inline SVG sparkle icon before AI-inferred field values |
| Inline edit input | `LibraryListView.tsx`, `LibraryAlbumView.tsx` | Text input replacing cell content on click, with save-on-blur/Enter |
| API key prompt | `LibraryScreen.tsx` or inline dialog | First-use prompt when Gemini API key is not configured |
| API key settings | `SettingsScreen.tsx` | Persistent API key management section |

### Modified Components

| Component | Change |
|-----------|--------|
| `InferredCell` in `LibraryListView.tsx` | Add `confidence === "ai"` branch with sparkle icon + click-to-edit |
| `LibraryAlbumView.tsx` | Add AI confidence rendering for album headers and track titles |
| `LibraryScreen.tsx` | Add enrichment button, progress state, enrich-complete refresh |
| `SettingsScreen.tsx` | Add Gemini API Key section |

---

## Interaction Contracts

### I-01: "Enriquecer IA" Button

- **Location:** Library header bar, right side, between "Re-scan Completo" and view toggle buttons
- **Appearance:** `bg-[#c8914a] text-[#0d0d0d] px-4 py-1.5 rounded text-xs font-semibold` (inherits existing `py-1.5` button pattern from SettingsScreen.tsx — not a new spacing token)
- **Label:** "Enriquecer IA" (Portuguese, consistent with "Re-scan Completo")
- **Disabled state:** `opacity-50 cursor-not-allowed` when: enrichment in progress, OR no qualifying tracks, OR no API key configured
- **Tooltip when disabled (no key):** "Configure a chave API do Gemini em Settings"
- **Tooltip when disabled (no qualifying):** "Todas as faixas ja possuem metadados completos"
- **Click behavior:** If API key missing, show inline key prompt. If key exists, start enrichment batch.

### I-02: Enrichment Progress

- **Replaces:** Library content area during enrichment (same pattern as scanning state)
- **Layout:** Centered vertically, same structure as scanning state
- **Elements:**
  1. Label: `text-xs font-semibold uppercase tracking-[0.18em] text-[#c8914a]` reading "Enriquecendo..."
  2. Progress bar: `w-full max-w-md h-2 rounded-full bg-[#1a1508]` with `bg-[#c8914a]` fill
  3. Counter: `text-sm font-mono text-[#e8dcc8]` reading "{processed} / {total} faixas"
  4. Error counter (if errors > 0): `text-xs text-[#e85c5c]` reading "{N} erros"
- **Completion:** Auto-transitions back to library view with refreshed tracks. Brief success flash: "Enriquecimento completo — {N} faixas atualizadas" in `text-[#7ac87a] text-sm` for 3 seconds.

### I-03: AI Sparkle Badge (InferredCell extension)

- **Indicator:** Inline SVG sparkle/star icon, 12x12px (`w-3 h-3`), `opacity-60`, `text-[#c8914a]`, positioned before the text value with `mr-1`
- **Text color:** `text-[#c8914a]` (not italic — italic is reserved for `low` confidence)
- **Title tooltip:** "Inferido por IA — clique para editar"
- **Cursor:** `cursor-pointer` on the entire cell
- **Click:** Opens inline edit mode for that field

### I-04: Inline Edit Mode

- **Trigger:** Click on any AI-inferred cell (sparkle badge cells)
- **Appearance:** Replace cell `<span>` with `<input>` of matching width
- **Input styling:** `bg-[#0d0d0d] border border-[#c8914a]/50 rounded px-2 py-1 text-sm text-[#e8dcc8] outline-none focus:border-[#c8914a]`
- **No layout shift:** Input height matches cell height. Use `min-w-0` and cell width constraints.
- **Save:** On Enter key or blur (focus loss)
- **Cancel:** On Escape key — revert to original value
- **After save:** Sparkle badge disappears. Text reverts to `text-[#e8dcc8]` (high confidence appearance). The field is now user-edited.
- **After save feedback:** No toast. The visual change (sparkle removal) is sufficient feedback.

### I-05: API Key Prompt (First Use)

- **Trigger:** User clicks "Enriquecer IA" with no Gemini API key stored
- **Style:** Inline card below the header bar (not modal). Matches error-summary card pattern.
- **Card:** `rounded border border-[#2a2218] bg-[#111008] p-4 w-full max-w-md mx-auto mt-8`
- **Heading:** `text-sm font-semibold text-[#e8dcc8]` reading "Chave API do Gemini"
- **Description:** `text-xs text-[#7a6e5f] mt-1` reading "Cole sua chave API do Google AI Studio para enriquecer metadados com IA."
- **Input:** `bg-[#0d0d0d] border border-[#2a2218] rounded px-3 py-2 text-sm font-mono text-[#e8dcc8] placeholder-[#4a4035] w-full mt-3` with placeholder "AIza..."
- **Save button:** `bg-[#c8914a] text-[#0d0d0d] px-4 py-2 rounded text-sm font-semibold mt-3` reading "Salvar e Enriquecer"
- **Dismiss link:** `text-xs text-[#4a4035] hover:text-[#7a6e5f] mt-2` reading "Manter sem IA"
- **After save:** Key stored in safeStorage vault, enrichment begins immediately

### I-06: Settings — Gemini API Key Section

- **Location:** `SettingsScreen.tsx`, new section after "Update Channel" and before "Account"
- **Pattern:** Matches existing SettingsScreen section layout exactly
- **Label:** `text-[#e8dcc8] text-sm font-medium` reading "Gemini API Key"
- **Display when set:** Masked key display `text-[#7a6e5f] text-xs font-mono` showing "AIza...{last4}" in `bg-[#111008] border border-[#2a2218] rounded px-3 py-2`
- **"Alterar chave" button:** `text-xs font-medium text-[#c8914a] hover:text-[#e8a85a]` reading "Alterar chave"
- **"Remover chave" button:** `text-xs font-medium text-red-400 hover:text-red-300` reading "Remover chave"
- **"Remover chave" confirmation:** On click, button text changes to "Confirmar remocao?" in `text-red-300` for 3 seconds. A second click within the 3-second window executes removal. If no second click, reverts to "Remover chave". No modal dialog.
- **Description:** `text-[#4a4035] text-xs` reading "Obtenha em ai.google.dev/aistudio — o plano gratuito permite 500 solicitacoes/dia."
- **Display when not set:** Same layout but value shows `text-[#4a4035] text-xs` reading "Nao configurada" and only a "Configurar chave API" button in accent color

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA | "Enriquecer IA" |
| CTA tooltip (no key) | "Configure a chave API do Gemini em Settings" |
| CTA tooltip (no tracks) | "Todas as faixas ja possuem metadados completos" |
| Progress label | "Enriquecendo..." |
| Progress counter | "{processed} / {total} faixas" |
| Progress error | "{N} erros" |
| Completion success | "Enriquecimento completo — {N} faixas atualizadas" |
| AI badge tooltip | "Inferido por IA — clique para editar" |
| Empty state (no qualifying tracks) | Button disabled with tooltip — no separate empty state screen |
| API key prompt heading | "Chave API do Gemini" |
| API key prompt body | "Cole sua chave API do Google AI Studio para enriquecer metadados com IA." |
| API key prompt CTA | "Salvar e Enriquecer" |
| API key prompt dismiss | "Manter sem IA" |
| API key settings label | "Gemini API Key" |
| API key settings not set | "Nao configurada" |
| API key settings help | "Obtenha em ai.google.dev/aistudio — o plano gratuito permite 500 solicitacoes/dia." |
| API key settings change | "Alterar chave" |
| API key settings remove | "Remover chave" |
| API key settings remove confirm | "Confirmar remocao?" |
| API key settings configure | "Configurar chave API" |
| Error: invalid key | "Chave API invalida. Verifique e tente novamente." |
| Error: rate limited | "Limite de solicitacoes atingido. Tente novamente em alguns minutos." |
| Error: network | "Falha de conexao com a API do Gemini. Verifique sua internet." |

---

## State Inventory

### Screen States (LibraryScreen)

| State | Trigger | Visual |
|-------|---------|--------|
| `library` (default) | Tracks loaded, no enrichment active | Library list/album view with "Enriquecer IA" button enabled |
| `enriching` | User clicks "Enriquecer IA" | Progress overlay replaces content (same pattern as `scanning`) |
| `enrich-complete` | Enrichment finishes | Brief success message, then auto-return to `library` with refreshed data |
| `api-key-prompt` | User clicks "Enriquecer IA" with no key | Inline card prompting for API key |

### Cell States (InferredCell)

| Confidence | Visual Treatment |
|------------|------------------|
| `high` | `text-[#e8dcc8]` — normal white-cream text, no badge, no italic |
| `low` | `text-[#8b7355] italic` — muted brown italic, tooltip "Inferido do caminho do arquivo" |
| `ai` | `text-[#c8914a]` — amber/accent text, sparkle icon prefix, tooltip "Inferido por IA — clique para editar", `cursor-pointer` |
| `null` value | `text-[#4a4035]` — dark placeholder showing "--" |
| Inline editing | `<input>` replaces `<span>`, `border-[#c8914a]/50 focus:border-[#c8914a]`, save on Enter/blur, cancel on Escape |

### Button States (Enrich CTA)

| State | Appearance |
|-------|------------|
| Default | `bg-[#c8914a] text-[#0d0d0d] text-xs font-semibold px-4 py-1.5 rounded` (inherits existing button pattern) |
| Hover | `hover:brightness-110` |
| Disabled (no key) | `opacity-50 cursor-not-allowed`, tooltip about configuring key |
| Disabled (no qualifying) | `opacity-50 cursor-not-allowed`, tooltip about all tracks complete |
| Disabled (enriching) | `opacity-50 cursor-not-allowed`, progress shown in content area |

### Remove Key Confirmation States

| State | Appearance |
|-------|------------|
| Default | `text-xs font-medium text-red-400 hover:text-red-300` reading "Remover chave" |
| Confirming | `text-xs font-medium text-red-300` reading "Confirmar remocao?" — reverts after 3 seconds if no second click |
| After removal | Section switches to "not set" display with "Configurar chave API" button |

---

## Accessibility

| Requirement | Implementation |
|-------------|----------------|
| Sparkle badge meaning | `title` attribute on AI-inferred cells ("Inferido por IA — clique para editar") |
| Inline edit keyboard | Enter to save, Escape to cancel, Tab to move to next editable field |
| Progress announcements | Progress counter text updates are visible; no `aria-live` region needed (desktop app, not screen reader target) |
| Disabled button explanation | `title` attribute explains why button is disabled |
| Focus management | After saving inline edit, focus returns to the cell that was being edited |
| Color contrast | `#c8914a` on `#0d0d0d` = ratio 5.4:1 (WCAG AA pass). `#e8dcc8` on `#0d0d0d` = ratio 13.2:1 (AAA pass). `#8b7355` on `#0d0d0d` = ratio 3.8:1 (AA large text pass). |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| N/A | N/A | Desktop app does not use shadcn or any component registry |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
