---
phase: 22-dependency-security
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/desktop/package.json
  - apps/web/package.json
  - packages/trade-domain/package.json
  - pnpm-lock.yaml
autonomous: true
requirements:
  - SEC-08
must_haves:
  truths:
    - "pnpm audit reports zero HIGH or CRITICAL vulnerabilities"
    - "All three workspaces (apps/desktop, apps/web, packages/trade-domain) use patched vite/vitest versions"
    - "pnpm install resolves cleanly with no peer dependency errors"
  artifacts:
    - path: "apps/desktop/package.json"
      provides: "Patched vite (>=7.3.2) and @vitejs/plugin-react"
      contains: "vite"
    - path: "apps/web/package.json"
      provides: "Patched @vitejs/plugin-react (>=6.0.1 resolving vite >=8.0.5) and vitest"
      contains: "vitest"
    - path: "packages/trade-domain/package.json"
      provides: "Patched vitest (>=3.2.x resolving vite >=7.3.2)"
      contains: "vitest"
    - path: "pnpm-lock.yaml"
      provides: "Resolved lockfile with patched transitive vite versions"
  key_links:
    - from: "pnpm-lock.yaml"
      to: "all package.json files"
      via: "pnpm install resolution"
      pattern: "vite"
---

<objective>
Update vite, vitest, and @vitejs/plugin-react across all three affected workspaces to resolve 9 known vulnerabilities (6 high, 3 moderate) in the vite package.

Purpose: Achieve SEC-08 (zero HIGH/CRITICAL audit vulnerabilities) as a deploy readiness gate.
Output: Updated package.json files and lockfile with zero audit findings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update vite ecosystem dependencies across all workspaces</name>
  <files>
    apps/desktop/package.json,
    apps/web/package.json,
    packages/trade-domain/package.json,
    pnpm-lock.yaml
  </files>
  <read_first>
    apps/desktop/package.json,
    apps/web/package.json,
    packages/trade-domain/package.json
  </read_first>
  <action>
Update vulnerable vite ecosystem packages in all three affected workspaces:

1. **apps/desktop** — `vite` is currently `^7.3.1` (needs >=7.3.2). `@vitejs/plugin-react` is `^5.1.4`.
   Run: `pnpm --filter @digswap/desktop update vite @vitejs/plugin-react`

2. **apps/web** — `@vitejs/plugin-react` is `^6.0.1` (pulls vite 8.x, needs >=8.0.5). `vitest` is `^4.1.1`.
   Run: `pnpm --filter @digswap/web update @vitejs/plugin-react vitest`
   If @vitejs/plugin-react update alone does not resolve the transitive vite vulnerability, also run:
   `pnpm --filter @digswap/web update vite`

3. **packages/trade-domain** — `vitest` is `^3.2.1` (pulls vite 7.x, needs >=7.3.2).
   Run: `pnpm --filter @digswap/trade-domain update vitest`

After updating all workspaces, run `pnpm install` from the monorepo root to regenerate the lockfile.

If `pnpm update` does not bump the transitive vite to patched versions, manually set specific minimum versions in the package.json devDependencies:
- apps/desktop: `"vite": "^7.3.2"`
- If needed, add `pnpm.overrides` in root package.json to force vite resolution.

Verify no peer dependency warnings or install errors.
  </action>
  <verify>
    <automated>cd "C:/Users/INTEL/Desktop/Get Shit DOne" && pnpm audit --audit-level high 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm audit --audit-level high` returns "0 vulnerabilities" or exits with code 0
    - `grep -o '"vite": "[^"]*"' apps/desktop/package.json` shows version >= 7.3.2
    - `pnpm ls vite --filter @digswap/desktop --depth 0` shows >= 7.3.2
    - `pnpm ls vite --filter @digswap/web --depth 1` shows no vite < 8.0.5
    - `pnpm ls vite --filter @digswap/trade-domain --depth 1` shows no vite < 7.3.2
    - `pnpm install` exits 0 with no unresolved peer warnings
  </acceptance_criteria>
  <done>All 9 vite vulnerabilities (GHSA-v2wj-q39q-566r, GHSA-p9ff-h696-f583, GHSA-4w7w-66w2-5vf9) resolved across desktop, web, and trade-domain workspaces. pnpm audit reports zero HIGH/CRITICAL findings.</done>
</task>

</tasks>

<verification>
1. `pnpm audit` — zero HIGH or CRITICAL vulnerabilities
2. `pnpm install` — clean install, no errors
3. `pnpm --filter @digswap/web run build` — build still succeeds (no breaking changes from updates)
4. `pnpm --filter @digswap/trade-domain run test` — vitest still runs (no breaking changes)
</verification>

<success_criteria>
- SEC-08 satisfied: zero HIGH/CRITICAL vulnerabilities in pnpm audit
- All three workspaces have patched vite versions
- Lockfile updated and committed
- No regressions in build or test
</success_criteria>

<output>
After completion, create `.planning/phases/22-dependency-security/22-01-SUMMARY.md`
</output>
