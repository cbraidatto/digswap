---
phase: 02-ui-shell-navigation
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/app/(protected)/(feed)/feed/page.tsx
  - src/app/(protected)/(profile)/perfil/page.tsx
  - src/app/(protected)/(explore)/explorar/page.tsx
  - src/app/(protected)/(community)/comunidade/page.tsx
  - tests/unit/components/shell/bottom-bar.test.tsx
  - tests/unit/components/shell/app-header.test.tsx
  - tests/unit/components/shell/empty-state.test.tsx
  - tests/e2e/navigation.spec.ts
autonomous: true
requirements:
  - NAV-01
  - NAV-02
  - NAV-03

must_haves:
  truths:
    - "Navigating to /feed renders the Feed empty state with 'Your feed is warming up'"
    - "Navigating to /explorar renders the Explorar empty state with 'Discover what is out there'"
    - "Navigating to /comunidade renders the Comunidade empty state with 'Find your people'"
    - "Navigating to /perfil renders the user profile placeholder with display name and avatar"
    - "Deep-linking to /feed highlights the Feed tab in the bottom bar"
    - "Unit tests verify bottom bar renders 4 tabs with correct labels"
    - "Unit tests verify active tab detection via pathname"
  artifacts:
    - path: "src/app/(protected)/(feed)/feed/page.tsx"
      provides: "Feed tab empty state page"
      contains: "Your feed is warming up"
    - path: "src/app/(protected)/(profile)/perfil/page.tsx"
      provides: "Perfil tab user profile placeholder"
      contains: "Account settings"
    - path: "src/app/(protected)/(explore)/explorar/page.tsx"
      provides: "Explorar tab empty state page"
      contains: "Discover what"
    - path: "src/app/(protected)/(community)/comunidade/page.tsx"
      provides: "Comunidade tab empty state page"
      contains: "Find your people"
    - path: "tests/unit/components/shell/bottom-bar.test.tsx"
      provides: "Unit tests for BottomBar and BottomBarItem"
      contains: "describe"
    - path: "tests/e2e/navigation.spec.ts"
      provides: "E2E tests for tab navigation and deep links"
      contains: "test.describe"
  key_links:
    - from: "src/app/(protected)/(feed)/feed/page.tsx"
      to: "src/components/shell/empty-state.tsx"
      via: "EmptyState component import"
      pattern: "EmptyState"
    - from: "src/app/(protected)/(profile)/perfil/page.tsx"
      to: "src/lib/db/schema/users.ts"
      via: "Drizzle query for current user profile"
      pattern: "profiles\\.displayName"
---

<objective>
Create the 4 tab page files (Feed, Perfil, Explorar, Comunidade) in their respective route groups with styled content per UI-SPEC, plus unit and E2E tests verifying all NAV requirements.

Purpose: Deliver the visible tab content that users see when navigating the app, and prove through automated tests that NAV-01 (4 tabs), NAV-02 (active tab indication), and NAV-03 (deep link support) all work correctly.
Output: 4 tab pages with empty states/profile placeholder, plus passing test suites.
</objective>

<execution_context>
@C:\Users\INTEL\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\INTEL\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ui-shell-navigation/02-CONTEXT.md
@.planning/phases/02-ui-shell-navigation/02-RESEARCH.md
@.planning/phases/02-ui-shell-navigation/02-UI-SPEC.md
@.planning/phases/02-ui-shell-navigation/02-01-SUMMARY.md

<interfaces>
<!-- Contracts from Plan 01 that this plan consumes -->

From src/components/shell/empty-state.tsx (created in Plan 01):
```typescript
import type { LucideIcon } from "lucide-react";

interface EmptyStateProps {
  icon: LucideIcon;
  heading: string;
  body: string;
}

export function EmptyState({ icon, heading, body }: EmptyStateProps): JSX.Element;
```

From src/components/shell/bottom-bar.tsx (created in Plan 01):
```typescript
// TABS array used internally:
const TABS = [
  { href: "/feed", label: "Feed", icon: Disc3 },
  { href: "/perfil", label: "Perfil", icon: User },
  { href: "/explorar", label: "Explorar", icon: Search },
  { href: "/comunidade", label: "Comunidade", icon: Users },
] as const;

export function BottomBar(): JSX.Element;
```

From src/components/shell/bottom-bar-item.tsx (created in Plan 01):
```typescript
interface BottomBarItemProps {
  href: string;
  label: string;
  icon: LucideIcon;
  isActive: boolean;
}
export function BottomBarItem(props: BottomBarItemProps): JSX.Element;
```

From src/components/shell/app-header.tsx (created in Plan 01):
```typescript
interface AppHeaderProps {
  displayName: string | null;
  avatarUrl: string | null;
}
export function AppHeader(props: AppHeaderProps): JSX.Element;
```

From src/components/ui/avatar.tsx (existing):
```typescript
export { Avatar, AvatarFallback, AvatarImage };
```

From src/lib/db/schema/users.ts (existing):
```typescript
export const profiles = pgTable("profiles", {
  id: uuid("id").primaryKey(),
  displayName: varchar("display_name", { length: 50 }),
  avatarUrl: text("avatar_url"),
  // ...
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 4 tab pages in route groups with styled content</name>
  <files>
    src/app/(protected)/(feed)/feed/page.tsx
    src/app/(protected)/(profile)/perfil/page.tsx
    src/app/(protected)/(explore)/explorar/page.tsx
    src/app/(protected)/(community)/comunidade/page.tsx
  </files>
  <read_first>
    src/components/shell/empty-state.tsx
    src/components/ui/avatar.tsx
    src/lib/supabase/server.ts
    src/lib/db/index.ts
    src/lib/db/schema/users.ts
    .planning/phases/02-ui-shell-navigation/02-UI-SPEC.md
    .planning/phases/02-ui-shell-navigation/02-CONTEXT.md
  </read_first>
  <action>
    Create 4 directories and page files. Each route group `(groupName)/` wraps the actual URL segment folder. Per D-13, structure is:
    - `(protected)/(feed)/feed/page.tsx` -> URL: `/feed`
    - `(protected)/(profile)/perfil/page.tsx` -> URL: `/perfil`
    - `(protected)/(explore)/explorar/page.tsx` -> URL: `/explorar`
    - `(protected)/(community)/comunidade/page.tsx` -> URL: `/comunidade`

    **File 1: `src/app/(protected)/(feed)/feed/page.tsx`**

    Server component. Import `EmptyState` from `@/components/shell/empty-state` and `Disc3` from `lucide-react`.

    ```typescript
    import { EmptyState } from "@/components/shell/empty-state";
    import { Disc3 } from "lucide-react";

    export default function FeedPage() {
      return (
        <EmptyState
          icon={Disc3}
          heading="Your feed is warming up"
          body="Drops when you follow other diggers"
        />
      );
    }
    ```

    Per D-08: Exact copy text. Per D-09: EmptyState component handles dark-warm styling.

    **File 2: `src/app/(protected)/(explore)/explorar/page.tsx`**

    Server component. Import `EmptyState` and `Search` from `lucide-react`.

    ```typescript
    import { EmptyState } from "@/components/shell/empty-state";
    import { Search } from "lucide-react";

    export default function ExplorarPage() {
      return (
        <EmptyState
          icon={Search}
          heading="Discover what's out there"
          body="Search across every collection on the platform"
        />
      );
    }
    ```

    **File 3: `src/app/(protected)/(community)/comunidade/page.tsx`**

    Server component. Import `EmptyState` and `Users` from `lucide-react`.

    ```typescript
    import { EmptyState } from "@/components/shell/empty-state";
    import { Users } from "lucide-react";

    export default function ComunidadePage() {
      return (
        <EmptyState
          icon={Users}
          heading="Find your people"
          body="Connect with diggers who share your taste"
        />
      );
    }
    ```

    **File 4: `src/app/(protected)/(profile)/perfil/page.tsx`**

    Server component. This is NOT an empty state -- it shows the current user's profile placeholder per D-10 and UI-SPEC Perfil Tab Placeholder.

    ```typescript
    import { redirect } from "next/navigation";
    import Link from "next/link";
    import { createClient } from "@/lib/supabase/server";
    import { db } from "@/lib/db";
    import { profiles } from "@/lib/db/schema/users";
    import { eq } from "drizzle-orm";
    import { Avatar, AvatarImage, AvatarFallback } from "@/components/ui/avatar";

    export default async function PerfilPage() {
      const supabase = await createClient();
      const { data: { user } } = await supabase.auth.getUser();

      if (!user) {
        redirect("/signin");
      }

      const [profile] = await db
        .select({
          displayName: profiles.displayName,
          avatarUrl: profiles.avatarUrl,
        })
        .from(profiles)
        .where(eq(profiles.id, user.id))
        .limit(1);

      const displayName = profile?.displayName ?? "Digger";
      const avatarUrl = profile?.avatarUrl ?? null;
      const initials = displayName.charAt(0).toUpperCase();

      return (
        <div className="flex flex-col items-center pt-12">
          <Avatar size="lg" className="size-20">
            {avatarUrl ? (
              <AvatarImage src={avatarUrl} alt={displayName} />
            ) : null}
            <AvatarFallback className="bg-primary text-primary-foreground text-2xl">
              {initials}
            </AvatarFallback>
          </Avatar>
          <h1 className="mt-4 font-heading text-xl font-semibold text-foreground">
            {displayName}
          </h1>
          <Link
            href="/settings"
            className="mt-2 text-base text-muted-foreground hover:underline"
          >
            Account settings
          </Link>
        </div>
      );
    }
    ```

    Per UI-SPEC Perfil Tab Placeholder:
    - Avatar: 80px diameter -- use `size="lg"` base Avatar (40px) but override with `className="size-20"` for 80px
    - Avatar fallback: `bg-primary text-primary-foreground` (amber background, dark text)
    - Display name: Fraunces 20px semibold = `font-heading text-xl font-semibold text-foreground`
    - Gap avatar-to-name: 16px = `mt-4`
    - Settings link: DM Sans 16px regular, muted-foreground, underline on hover
    - Settings link text: "Account settings"
    - Layout: `flex flex-col items-center pt-12` (48px = pt-12, approximately 2xl spacing)
  </action>
  <verify>
    <automated>npx biome check src/app/\(protected\)/\(feed\)/feed/page.tsx src/app/\(protected\)/\(profile\)/perfil/page.tsx src/app/\(protected\)/\(explore\)/explorar/page.tsx src/app/\(protected\)/\(community\)/comunidade/page.tsx --skip-errors 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/app/(protected)/(feed)/feed/page.tsx exists
    - src/app/(protected)/(feed)/feed/page.tsx contains `Your feed is warming up` (exact D-08 copy)
    - src/app/(protected)/(feed)/feed/page.tsx contains `Drops when you follow other diggers` (exact D-08 copy)
    - src/app/(protected)/(feed)/feed/page.tsx contains `EmptyState`
    - src/app/(protected)/(feed)/feed/page.tsx contains `Disc3`
    - src/app/(protected)/(explore)/explorar/page.tsx exists
    - src/app/(protected)/(explore)/explorar/page.tsx contains `Discover what` (D-08 copy)
    - src/app/(protected)/(explore)/explorar/page.tsx contains `Search` import from lucide-react
    - src/app/(protected)/(community)/comunidade/page.tsx exists
    - src/app/(protected)/(community)/comunidade/page.tsx contains `Find your people` (D-08 copy)
    - src/app/(protected)/(community)/comunidade/page.tsx contains `Users` import from lucide-react
    - src/app/(protected)/(profile)/perfil/page.tsx exists
    - src/app/(protected)/(profile)/perfil/page.tsx contains `Account settings` (settings link per D-10)
    - src/app/(protected)/(profile)/perfil/page.tsx contains `profiles.displayName`
    - src/app/(protected)/(profile)/perfil/page.tsx contains `Avatar`
    - src/app/(protected)/(profile)/perfil/page.tsx contains `bg-primary text-primary-foreground` (amber avatar fallback per UI-SPEC)
    - src/app/(protected)/(profile)/perfil/page.tsx contains `font-heading text-xl font-semibold` (Fraunces heading per UI-SPEC)
    - src/app/(protected)/(profile)/perfil/page.tsx contains `href="/settings"` (link to settings per D-10)
  </acceptance_criteria>
  <done>
    4 tab pages created in correct route group structure. Feed, Explorar, and Comunidade show styled empty states with exact D-08 copy text and correct Lucide icons. Perfil shows user profile placeholder with 80px avatar (amber fallback), display name in Fraunces, and "Account settings" link.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unit tests for shell components and E2E navigation tests</name>
  <files>
    tests/unit/components/shell/bottom-bar.test.tsx
    tests/unit/components/shell/app-header.test.tsx
    tests/unit/components/shell/empty-state.test.tsx
    tests/e2e/navigation.spec.ts
  </files>
  <read_first>
    src/components/shell/bottom-bar.tsx
    src/components/shell/bottom-bar-item.tsx
    src/components/shell/app-header.tsx
    src/components/shell/user-avatar-menu.tsx
    src/components/shell/empty-state.tsx
    vitest.config.ts
    playwright.config.ts
    tests/unit/lib/rate-limit.test.ts
    tests/e2e/auth-flow.spec.ts
  </read_first>
  <behavior>
    Unit tests (Vitest + jsdom):
    - BottomBar renders 4 tab links with correct labels: "Feed", "Perfil", "Explorar", "Comunidade"
    - BottomBar renders 4 tab links with correct hrefs: "/feed", "/perfil", "/explorar", "/comunidade"
    - BottomBarItem renders with aria-current="page" when isActive is true
    - BottomBarItem renders without aria-current when isActive is false
    - BottomBarItem applies text-primary class when active
    - BottomBarItem applies text-muted-foreground class when inactive
    - AppHeader renders "VinylDig" wordmark text
    - AppHeader renders with font-heading class
    - EmptyState renders heading and body text
    - EmptyState renders the icon element

    E2E tests (Playwright):
    - Navigation test: authenticated user sees 4 tabs on /feed page
    - Navigation test: active tab highlighted when on /feed
    - Navigation test: clicking Explorar tab navigates to /explorar
    - Navigation test: deep-linking to /explorar highlights Explorar tab
    - Navigation test: header shows "VinylDig" wordmark
  </behavior>
  <action>
    **Unit Tests Setup:**

    The Vitest config at `vitest.config.ts` is already configured with jsdom environment and `@` path alias. Tests go in `tests/unit/components/shell/`.

    Need to mock `next/navigation` (usePathname, useRouter) and `next/link` since these are Next.js runtime APIs not available in jsdom. Follow the pattern from existing test files.

    **File 1: `tests/unit/components/shell/bottom-bar.test.tsx`**

    ```typescript
    import { describe, it, expect, vi } from "vitest";
    import { render, screen } from "@testing-library/react";
    ```

    Mock `next/navigation`:
    ```typescript
    const mockPathname = vi.fn(() => "/feed");
    vi.mock("next/navigation", () => ({
      usePathname: () => mockPathname(),
    }));
    ```

    Mock `next/link` to render a plain `<a>` tag:
    ```typescript
    vi.mock("next/link", () => ({
      default: ({ href, children, ...props }: any) => <a href={href} {...props}>{children}</a>,
    }));
    ```

    Tests:
    - `it("renders 4 tab links with correct labels")` -- render BottomBar, use `screen.getByText("Feed")`, `screen.getByText("Perfil")`, `screen.getByText("Explorar")`, `screen.getByText("Comunidade")`, all should be in document.
    - `it("renders tab links with correct hrefs")` -- render BottomBar, find links by role "link", verify href attributes include `/feed`, `/perfil`, `/explorar`, `/comunidade`.
    - `it("marks active tab with aria-current=page")` -- mockPathname returns "/feed", render BottomBar, find the Feed link, expect `aria-current` to be "page". Other tabs should NOT have `aria-current`.
    - `it("applies active styling to current tab")` -- mockPathname returns "/explorar", render BottomBar, find the Explorar link, expect it to have `text-primary` in className (or check for the class).
    - `it("has nav element with aria-label")` -- render BottomBar, `screen.getByRole("navigation")` should have `aria-label` of "Main navigation".
    - `it("detects active tab for deep links using startsWith")` -- mockPathname returns "/feed/some-post/123", render BottomBar, Feed tab should have `aria-current="page"`.

    **File 2: `tests/unit/components/shell/app-header.test.tsx`**

    Mock `next/navigation` (useRouter) and `next/link`. Import AppHeader.

    Tests:
    - `it("renders VinylDig wordmark")` -- render AppHeader with displayName="Test" and avatarUrl=null, expect `screen.getByText("VinylDig")` to be in document.
    - `it("renders wordmark with font-heading class")` -- render AppHeader, the VinylDig text element should have `font-heading` in className.

    Note: UserAvatarMenu inside AppHeader uses DropdownMenu which uses Base UI. These need mocking or shallow rendering. If Base UI components cause issues in jsdom, mock `@/components/shell/user-avatar-menu` to render a simple placeholder. Keep tests focused on what AppHeader itself renders.

    **File 3: `tests/unit/components/shell/empty-state.test.tsx`**

    Import EmptyState, import a Lucide icon (e.g., `Disc3`).

    Tests:
    - `it("renders heading text")` -- render EmptyState with heading="Test Heading", body="Test body", icon=Disc3. Expect `screen.getByText("Test Heading")` in document.
    - `it("renders body text")` -- expect `screen.getByText("Test body")` in document.
    - `it("renders the icon")` -- the Lucide icon renders as an SVG. Check that an SVG element exists in the rendered output.

    **File 4: `tests/e2e/navigation.spec.ts`**

    Follow the pattern established in `tests/e2e/auth-flow.spec.ts`. Import from `@playwright/test`.

    These E2E tests require a running app with a logged-in user. Structure:

    ```typescript
    import { test, expect } from "@playwright/test";

    test.describe("Navigation Shell", () => {
      // NOTE: These tests require an authenticated session.
      // If auth setup is not automated, mark as fixme or skip.
      // The test structure below documents the expected behavior for NAV-01, NAV-02, NAV-03.

      test("bottom bar displays 4 tabs with correct labels", async ({ page }) => {
        // Navigate to /feed (requires auth -- may redirect to /signin)
        await page.goto("/feed");
        // Check for 4 tab labels
        const nav = page.locator('nav[aria-label="Main navigation"]');
        await expect(nav).toBeVisible();
        await expect(nav.getByText("Feed")).toBeVisible();
        await expect(nav.getByText("Perfil")).toBeVisible();
        await expect(nav.getByText("Explorar")).toBeVisible();
        await expect(nav.getByText("Comunidade")).toBeVisible();
      });

      test("active tab is visually indicated on /feed", async ({ page }) => {
        await page.goto("/feed");
        const feedLink = page.locator('nav[aria-label="Main navigation"] a[href="/feed"]');
        await expect(feedLink).toHaveAttribute("aria-current", "page");
      });

      test("deep link to /explorar highlights Explorar tab", async ({ page }) => {
        await page.goto("/explorar");
        const explorarLink = page.locator('nav[aria-label="Main navigation"] a[href="/explorar"]');
        await expect(explorarLink).toHaveAttribute("aria-current", "page");
        // Feed should NOT be active
        const feedLink = page.locator('nav[aria-label="Main navigation"] a[href="/feed"]');
        await expect(feedLink).not.toHaveAttribute("aria-current", "page");
      });

      test("header displays VinylDig wordmark", async ({ page }) => {
        await page.goto("/feed");
        await expect(page.getByText("VinylDig")).toBeVisible();
      });

      test("clicking tab navigates to correct route", async ({ page }) => {
        await page.goto("/feed");
        await page.locator('nav[aria-label="Main navigation"]').getByText("Explorar").click();
        await expect(page).toHaveURL(/\/explorar/);
      });
    });
    ```

    If the existing Playwright config does not have an auth setup helper (storageState), add a comment at the top of the test file: `// TODO: Requires authenticated storageState -- run with seeded test user`

    Install `@testing-library/react` if not already installed. Check first:
    ```bash
    npm ls @testing-library/react 2>/dev/null
    ```
    If not installed: `npm install -D @testing-library/react @testing-library/jest-dom`
  </action>
  <verify>
    <automated>npx vitest run tests/unit/components/shell/ --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - tests/unit/components/shell/bottom-bar.test.tsx exists and contains `describe`
    - tests/unit/components/shell/bottom-bar.test.tsx contains test for `"Feed"` label
    - tests/unit/components/shell/bottom-bar.test.tsx contains test for `"Comunidade"` label
    - tests/unit/components/shell/bottom-bar.test.tsx contains test for `aria-current`
    - tests/unit/components/shell/bottom-bar.test.tsx contains test for deep link (`startsWith` or `/feed/`)
    - tests/unit/components/shell/app-header.test.tsx exists and contains `VinylDig`
    - tests/unit/components/shell/empty-state.test.tsx exists and contains `EmptyState`
    - tests/e2e/navigation.spec.ts exists and contains `Main navigation`
    - tests/e2e/navigation.spec.ts contains tests for `Feed`, `Perfil`, `Explorar`, `Comunidade`
    - tests/e2e/navigation.spec.ts contains `aria-current` assertion
    - `npx vitest run tests/unit/components/shell/` exits with code 0 (all unit tests pass)
  </acceptance_criteria>
  <done>
    Unit tests pass for BottomBar (4 tabs, labels, hrefs, active detection with startsWith, aria-current, deep link support), AppHeader (wordmark rendering), and EmptyState (heading, body, icon). E2E test file created with Playwright specs for tab visibility, active tab indication, deep link highlighting, and tab navigation. Unit test suite green.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `npx vitest run tests/unit/components/shell/` -- all unit tests pass (0 failures)
2. `npx biome check src/app/(protected)/(feed)/ src/app/(protected)/(profile)/ src/app/(protected)/(explore)/ src/app/(protected)/(community)/` -- no lint errors
3. All 4 tab page files exist with correct empty state copy from D-08
4. Perfil page fetches user profile and shows display name + avatar + settings link
5. Route group structure matches D-13: `(feed)/feed/`, `(profile)/perfil/`, `(explore)/explorar/`, `(community)/comunidade/`
</verification>

<success_criteria>
- 4 tab pages created in correct Next.js route group structure
- Feed/Explorar/Comunidade show styled empty states with exact D-08 copy
- Perfil shows profile placeholder with avatar, display name, and settings link per D-10
- Unit tests verify NAV-01 (4 tabs rendered), NAV-02 (active tab detection), NAV-03 (deep link support)
- E2E test file documents expected navigation behavior
- All unit tests pass with `npx vitest run`
</success_criteria>

<output>
After completion, create `.planning/phases/02-ui-shell-navigation/02-02-SUMMARY.md`
</output>
