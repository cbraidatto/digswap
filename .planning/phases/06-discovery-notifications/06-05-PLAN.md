---
phase: 06-discovery-notifications
plan: 05
type: execute
wave: 4
depends_on: ["06-02", "06-03", "06-04"]
files_modified:
  - tests/unit/components/shell/notification-bell.test.tsx
autonomous: false
requirements:
  - DISC2-01
  - DISC2-02
  - DISC2-03
  - DISC2-04
  - NOTF-01
  - NOTF-02
  - NOTF-04

must_haves:
  truths:
    - "User can search by record name or artist and see which platform users own that record"
    - "User can browse collections filtered by genre and decade on the Explorar tab"
    - "User receives an in-app notification when a platform user has a record from their wantlist"
    - "User can configure which notification types they receive per event type"
    - "Platform suggests records based on collection taste and similar diggers' collections"
  artifacts:
    - path: "tests/unit/components/shell/notification-bell.test.tsx"
      provides: "NotificationBell component tests"
  key_links:
    - from: "src/actions/collection.ts"
      to: "src/lib/notifications/match.ts"
      via: "checkWantlistMatches call"
      pattern: "checkWantlistMatches"
    - from: "src/components/shell/notification-bell.tsx"
      to: "src/lib/supabase/client.ts"
      via: "Realtime subscription"
      pattern: "postgres_changes"
---

<objective>
Run all tests, add NotificationBell component test, and perform human verification of the complete Discovery + Notifications feature set across all surfaces.

Purpose: Final quality gate. Ensures all Phase 6 features work end-to-end and meet acceptance criteria before marking the phase complete.
Output: 1 test file + human verification
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-discovery-notifications/06-CONTEXT.md
@.planning/phases/06-discovery-notifications/06-UI-SPEC.md
@.planning/phases/06-discovery-notifications/06-01-SUMMARY.md
@.planning/phases/06-discovery-notifications/06-02-SUMMARY.md
@.planning/phases/06-discovery-notifications/06-03-SUMMARY.md
@.planning/phases/06-discovery-notifications/06-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add NotificationBell component test and run full test suite</name>
  <files>
    tests/unit/components/shell/notification-bell.test.tsx
  </files>
  <read_first>
    src/components/shell/notification-bell.tsx,
    src/components/shell/notification-row.tsx,
    tests/unit/components/shell
  </read_first>
  <action>
    Create `tests/unit/components/shell/notification-bell.test.tsx`:
    - Mock `@/lib/supabase/client` to return a fake Supabase client with `.channel()` that returns a chainable `.on().subscribe()` pattern and `.removeChannel()` that is a no-op
    - Mock `@/actions/notifications` to return controlled data:
      - `getUnreadCountAction` returns 3
      - `getNotificationsAction` returns array of 2 test notifications
      - `markAllRead` resolves
      - `markNotificationRead` resolves
    - Mock `next/navigation` useRouter and useSearchParams

    Tests:
    - Test: renders bell icon with material-symbols-outlined notifications
    - Test: shows unread badge with count from getUnreadCountAction
    - Test: badge shows "9+" when count > 9
    - Test: no badge when count is 0
    - Test: bell has aria-label="Notifications"
    - Test: creates Supabase Realtime channel on mount with correct filter
    - Test: cleans up channel on unmount (removeChannel called)

    After creating the test, run the full test suite:
    ```
    npx vitest run --reporter=verbose
    ```

    Verify all Phase 6 tests pass:
    - tests/unit/discovery/record-search.test.ts
    - tests/unit/discovery/genre-browse.test.ts
    - tests/unit/discovery/taste-match.test.ts
    - tests/unit/notifications/wantlist-match.test.ts
    - tests/unit/notifications/email.test.ts
    - tests/unit/notifications/preferences.test.ts
    - tests/unit/components/shell/notification-bell.test.tsx
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - tests/unit/components/shell/notification-bell.test.tsx exists
    - tests/unit/components/shell/notification-bell.test.tsx contains `vi.mock("@/lib/supabase/client")`
    - tests/unit/components/shell/notification-bell.test.tsx contains `vi.mock("@/actions/notifications")`
    - tests/unit/components/shell/notification-bell.test.tsx contains test for `aria-label="Notifications"`
    - tests/unit/components/shell/notification-bell.test.tsx contains test for `9+` badge overflow
    - npx vitest run exits 0 (all tests pass)
  </acceptance_criteria>
  <done>
    NotificationBell component test covers rendering, badge display, and Realtime subscription lifecycle. Full test suite passes including all 7 Phase 6 test files.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of complete Discovery + Notifications feature set</name>
  <files>none</files>
  <action>
    Present the user with the verification checklist below. Wait for user to test all surfaces and confirm functionality or report issues.

    IMPORTANT: Before testing, the user must run this SQL in Supabase Dashboard -> SQL Editor:
    ```sql
    ALTER PUBLICATION supabase_realtime ADD TABLE notifications;
    ```
    Also ensure RESEND_API_KEY is set in .env.local (or skip email verification).
  </action>
  <verify>User confirms "approved" or provides issues to fix</verify>
  <done>User has verified all Phase 6 surfaces work correctly end-to-end</done>
  <what-built>
    Complete Discovery + Notifications feature set for Phase 6:
    1. Record search on /explorar RECORDS tab (search by title/artist, see owners with condition grades)
    2. Genre/decade browse on /explorar RECORDS tab (filter chips, cross-user grid sorted by rarity)
    3. Taste-match suggestions ("SUGGESTED_FOR_YOU" section below browse)
    4. Notification bell in header with unread badge and dropdown panel
    5. /notifications page with paginated notification list
    6. Wantlist match trigger in addRecord and import pipeline
    7. Notification preferences in /settings with per-type toggles
    8. Email notifications via Resend for wantlist matches
  </what-built>
  <how-to-verify>
    1. **Record Search (DISC2-01):**
       - Visit /explorar
       - Click "RECORDS" tab
       - Type a record name or artist in the search input (at least 2 chars)
       - Verify: results appear after ~300ms debounce
       - Verify: each result shows title, artist, rarity badge, and owner count
       - Verify: owners list shows usernames with condition grades
       - Verify: typing less than 2 chars clears results

    2. **Genre/Decade Browse (DISC2-02):**
       - On RECORDS tab, click a genre chip (e.g., "Electronic")
       - Verify: grid of records filtered by that genre appears
       - Click a decade chip (e.g., "90s")
       - Verify: results narrow to genre + decade combination
       - Click the active genre chip to deselect
       - Verify: only decade filter remains active

    3. **Taste Match Suggestions (DISC2-04):**
       - On RECORDS tab, scroll below search/browse area
       - Verify: "SUGGESTED_FOR_YOU" section appears (may be empty if no collection/follows)
       - If empty: verify "[NO_SUGGESTIONS_YET]" empty state shows

    4. **Tab Switching:**
       - Click "DIGGERS" tab
       - Verify: username search from Phase 5 appears
       - Click "RECORDS" tab
       - Verify: record search appears
       - Verify: search state is preserved independently per tab

    5. **Notification Bell (NOTF-01):**
       - Verify: bell icon visible in header next to avatar
       - Click bell
       - Verify: dropdown opens with up to 5 recent notifications (or empty state)
       - Verify: "Mark all read" and "View all notifications" links at bottom
       - Click "View all notifications"
       - Verify: navigates to /notifications page with paginated list

    6. **Notification Preferences (NOTF-04):**
       - Visit /settings
       - Scroll to "Notification Preferences" section
       - Verify: "Wantlist Match" has functional In-app and Email toggles
       - Verify: "Trade Request" shows disabled toggles with [PHASE_9] badge
       - Verify: "Ranking Change" and "New Badge Earned" show [PHASE_8] badges
       - Toggle a Wantlist Match preference off and refresh
       - Verify: toggle state persists

    7. **Wantlist Match Trigger (DISC2-03):**
       - (Requires two test users or manual DB setup)
       - User A has a record on their wantlist
       - User B adds that same record to their collection
       - Verify: User A sees a notification in their bell dropdown
       - Verify: notification says "Wantlist match found!" with record details
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
npx vitest run --reporter=verbose && npx tsc --noEmit
All tests pass. TypeScript compiles. Human verification confirms end-to-end functionality.
</verification>

<success_criteria>
- All Phase 6 test files pass (7 test files total)
- Human verifies record search, browse, suggestions, bell, preferences, and match trigger
- No TypeScript compilation errors
- No regressions in existing Phase 1-5 tests
</success_criteria>

<output>
After completion, create `.planning/phases/06-discovery-notifications/06-05-SUMMARY.md`
</output>
