---
phase: 06-discovery-notifications
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/app/(protected)/(explore)/explorar/page.tsx
  - src/app/(protected)/(explore)/explorar/_components/records-tab.tsx
  - src/app/(protected)/(explore)/explorar/_components/record-search.tsx
  - src/app/(protected)/(explore)/explorar/_components/record-search-card.tsx
  - src/app/(protected)/(explore)/explorar/_components/owners-list.tsx
  - src/app/(protected)/(explore)/explorar/_components/browse-filters.tsx
  - src/app/(protected)/(explore)/explorar/_components/browse-grid.tsx
  - src/app/(protected)/(explore)/explorar/_components/suggested-section.tsx
autonomous: true
requirements:
  - DISC2-01
  - DISC2-02
  - DISC2-04

must_haves:
  truths:
    - "User can search by record name or artist on the RECORDS tab and see results with owners"
    - "User can browse records filtered by genre and decade using filter chips"
    - "User sees suggested records based on their collection taste and followed users"
    - "Tab switching between DIGGERS and RECORDS preserves independent search state"
  artifacts:
    - path: "src/app/(protected)/(explore)/explorar/page.tsx"
      provides: "Explorar page with DIGGERS/RECORDS tab layout"
      contains: "DIGGERS"
    - path: "src/app/(protected)/(explore)/explorar/_components/records-tab.tsx"
      provides: "RECORDS tab container with search, browse, and suggestions"
    - path: "src/app/(protected)/(explore)/explorar/_components/record-search.tsx"
      provides: "Debounced record search input with results"
    - path: "src/app/(protected)/(explore)/explorar/_components/browse-filters.tsx"
      provides: "Genre and decade filter chips"
    - path: "src/app/(protected)/(explore)/explorar/_components/browse-grid.tsx"
      provides: "Cross-user record browse grid"
    - path: "src/app/(protected)/(explore)/explorar/_components/suggested-section.tsx"
      provides: "SUGGESTED_FOR_YOU section"
    - path: "src/app/(protected)/(explore)/explorar/_components/owners-list.tsx"
      provides: "Compact owner list for record search cards"
  key_links:
    - from: "src/app/(protected)/(explore)/explorar/_components/record-search.tsx"
      to: "src/actions/discovery.ts"
      via: "import searchRecordsAction"
      pattern: "import.*searchRecordsAction.*from.*actions/discovery"
    - from: "src/app/(protected)/(explore)/explorar/_components/browse-grid.tsx"
      to: "src/actions/discovery.ts"
      via: "import browseRecordsAction"
      pattern: "import.*browseRecordsAction.*from.*actions/discovery"
    - from: "src/app/(protected)/(explore)/explorar/_components/suggested-section.tsx"
      to: "src/actions/discovery.ts"
      via: "import getSuggestionsAction"
      pattern: "import.*getSuggestionsAction.*from.*actions/discovery"
---

<objective>
Rewrite the `/explorar` page from its current placeholder state to a fully functional discovery surface with DIGGERS/RECORDS tab bar, record search with owner listings, genre/decade browse filters, and taste-match suggestions.

Purpose: This is the primary discovery surface where users find who has the records they want (core value proposition). The DIGGERS tab preserves Phase 5's username search; the new RECORDS tab delivers DISC2-01, DISC2-02, and DISC2-04.
Output: 8 component files (1 rewrite + 7 new)
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-discovery-notifications/06-CONTEXT.md
@.planning/phases/06-discovery-notifications/06-RESEARCH.md
@.planning/phases/06-discovery-notifications/06-UI-SPEC.md
@.planning/phases/06-discovery-notifications/06-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->

From src/actions/discovery.ts (created in Plan 01):
```typescript
"use server"
export async function searchRecordsAction(term: string): Promise<RecordSearchResult[]>;
export async function browseRecordsAction(genre: string | null, decade: string | null, page?: number): Promise<BrowseResult[]>;
export async function getSuggestionsAction(): Promise<SuggestedRecord[]>;
```

From src/lib/collection/rarity.ts:
```typescript
export type RarityTier = "Ultra Rare" | "Rare" | "Common" | null;
export function getRarityTier(score: number | null): RarityTier;
export function getRarityBadgeVariant(tier: RarityTier): "destructive" | "default" | "secondary" | "outline";
```

From src/lib/collection/filters.ts:
```typescript
export const DECADES = [
  { label: "50s", startYear: 1950 }, ..., { label: "20s", startYear: 2020 }
] as const;
```

From src/lib/discogs/taxonomy.ts:
```typescript
export const DISCOGS_GENRES: string[]; // ["Blues","Brass & Military","Children's","Classical","Electronic",...]
```

From existing search-section.tsx (DIGGERS tab, unchanged):
```typescript
export function SearchSection(): JSX.Element; // Client component, self-contained
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite explorar page with tab layout and build RECORDS tab components</name>
  <files>
    src/app/(protected)/(explore)/explorar/page.tsx,
    src/app/(protected)/(explore)/explorar/_components/records-tab.tsx,
    src/app/(protected)/(explore)/explorar/_components/record-search.tsx,
    src/app/(protected)/(explore)/explorar/_components/record-search-card.tsx,
    src/app/(protected)/(explore)/explorar/_components/owners-list.tsx,
    src/app/(protected)/(explore)/explorar/_components/browse-filters.tsx,
    src/app/(protected)/(explore)/explorar/_components/browse-grid.tsx,
    src/app/(protected)/(explore)/explorar/_components/suggested-section.tsx
  </files>
  <read_first>
    src/app/(protected)/(explore)/explorar/page.tsx,
    src/app/(protected)/(explore)/explorar/_components/search-section.tsx,
    src/lib/collection/rarity.ts,
    src/lib/collection/filters.ts,
    src/lib/discogs/taxonomy.ts,
    src/app/globals.css,
    .planning/phases/06-discovery-notifications/06-UI-SPEC.md
  </read_first>
  <action>
    **Rewrite `src/app/(protected)/(explore)/explorar/page.tsx`** (per D-03):
    - Remove ALL existing hardcoded data (DIRECTORIES, SEARCH_RESULTS, the entire sidebar/results layout)
    - Make it a client component (`"use client"`)
    - Add state: `const [activeTab, setActiveTab] = useState<"diggers" | "records">("diggers")` (default DIGGERS per UI-SPEC interaction contract: "Page load: Default to DIGGERS tab")
    - Support URL param: read `?tab=records` from `useSearchParams()` to deep-link to RECORDS tab
    - Render tab bar at top:
      ```
      <div className="w-full border-b border-outline-variant/10">
        <div className="max-w-4xl mx-auto flex gap-6">
          <button onClick={() => setActiveTab("diggers")}
            className={`px-4 py-2 font-mono text-[10px] uppercase tracking-widest transition-colors
              ${activeTab === "diggers" ? "text-primary border-b-2 border-primary font-semibold" : "text-on-surface-variant hover:text-on-surface font-normal"}`}
            role="tab" aria-selected={activeTab === "diggers"}>
            DIGGERS
          </button>
          <button onClick={() => setActiveTab("records")} ...same pattern...>
            RECORDS
          </button>
        </div>
      </div>
      ```
    - Tab bar: `role="tablist"` on container
    - Content area with `role="tabpanel"`:
      - DIGGERS tab: `<section className="w-full bg-surface-container-low p-8 md:p-12"><div className="max-w-4xl mx-auto"><SearchSection /></div></section>` (exactly preserves Phase 5 hero layout)
      - RECORDS tab: `<RecordsTab />`
    - Import `SearchSection` from `./_components/search-section"` (existing)
    - Import `RecordsTab` from `./_components/records-tab"`

    **Create `records-tab.tsx`** (client component):
    - Container: `<div className="w-full p-8 md:p-12"><div className="max-w-4xl mx-auto space-y-8">`
    - Render in order: `<RecordSearch />`, `<BrowseFilters>` + `<BrowseGrid>`, `<SuggestedSection />`
    - BrowseFilters manages filter state (selectedGenre, selectedDecade) and passes them to BrowseGrid
    - Uses local state for genre/decade, passes setter callbacks to BrowseFilters

    **Create `record-search.tsx`** (client component, per D-04, D-05):
    - Follow exact same debounce pattern from `search-section.tsx`: `useState` for query, `useRef` for timeout, `useCallback` for handler, `useTransition` for pending
    - Section header: `<div className="font-mono text-[10px] text-on-surface-variant uppercase tracking-[0.2em] mb-4">search_records</div>`
    - Search input: `<div className="relative flex items-center bg-surface-container-lowest p-4 border-l-4 border-primary">`
      - Prompt: `<span className="text-primary font-mono text-xl mr-4">&gt;</span>`
      - Input: `<input type="text" role="searchbox" aria-label="Search records by name or artist" placeholder="Search by record or artist..." className="w-full bg-transparent border-none focus:ring-0 text-on-surface font-mono text-lg placeholder:text-on-surface-variant/40 outline-none" />`
      - Cursor: `<span className="w-3 h-8 bg-primary blink ml-2" />`
    - Minimum 2 chars to trigger (per D-04)
    - 300ms debounce (per D-04)
    - On search: call `searchRecordsAction(value)` from `@/actions/discovery`
    - Loading state: `<div className="font-mono text-[10px] text-on-surface-variant text-center py-8">searching...</div>`
    - Results header: `<div className="font-mono text-[10px] text-on-surface-variant mt-4 mb-4"><span className="text-primary">{N}</span> results found</div>`
    - Each result: render `<RecordSearchCard>` component
    - Empty results: `<div className="font-mono text-sm text-on-surface-variant text-center py-8">no records found matching "{query}"</div>`

    **Create `record-search-card.tsx`** (per D-05, UI-SPEC):
    - Props: `{ release: RecordSearchResult }` (the type from Plan 01's searchRecords return)
    - Layout: `<div className="bg-surface-container-low rounded-lg p-4 flex gap-4 hover:bg-surface-container transition-colors cursor-pointer group">`
    - Album art placeholder: `<div className="w-12 h-12 bg-surface-container-high rounded flex-shrink-0 flex items-center justify-center"><span className="material-symbols-outlined text-on-surface-variant/40 text-xl">album</span></div>`
    - If `coverImageUrl` exists, show `<img>` instead
    - Title: `<span className="font-heading font-semibold text-on-surface group-hover:text-primary transition-colors truncate text-sm">{title}</span>`
    - Rarity badge: use `getRarityTier(rarityScore)` and render `<span className="text-[10px] font-mono px-1.5 py-0.5 rounded border {colorClasses}">[{TIER}]</span>` with tier-appropriate colors:
      - Ultra Rare: `text-tertiary border-tertiary`
      - Rare: `text-secondary border-secondary`
      - Common: `text-on-surface-variant border-outline-variant`
    - Metadata: `<div className="flex flex-wrap items-center gap-3 text-[10px] text-on-surface-variant font-mono">{artist} . {label} . {year} . {format}</div>`
    - Owner count: `<span className="text-[10px] font-mono text-on-surface-variant">{ownerCount} {ownerCount === 1 ? "owner" : "owners"}</span>`
    - Below card: `<OwnersList owners={release.owners} />`

    **Create `owners-list.tsx`** (per D-05, UI-SPEC):
    - Props: `{ owners: Owner[] }`
    - If empty: `<div className="font-mono text-[10px] text-on-surface-variant py-2">[NO_OWNERS_IN_NETWORK]</div>`
    - Show first 3 owners, if more show "+{N} more" expand button
    - Each row: `<div className="flex items-center gap-2 h-8">`
      - Avatar: `<div className="w-6 h-6 rounded bg-surface-container-high flex items-center justify-center flex-shrink-0">` with initial letter or img
      - Username: `<Link href={/perfil/${username}} className="font-mono text-[10px] text-on-surface hover:text-primary transition-colors">{username}</Link>`
      - Condition badge: `<span className="font-mono text-[10px] text-on-surface-variant border border-outline-variant/20 px-1 rounded">{conditionGrade}</span>` (or nothing if null)
    - Row gap: `gap-1` (4px per UI-SPEC)

    **Create `browse-filters.tsx`** (per D-06, UI-SPEC):
    - Props: `{ selectedGenre: string | null, selectedDecade: string | null, onGenreChange: (g: string | null) => void, onDecadeChange: (d: string | null) => void }`
    - Import `DISCOGS_GENRES` from `@/lib/discogs/taxonomy` (use the first ~10 main genres: Electronic, Jazz, Hip Hop, Rock, Soul, Funk / Soul, Latin, Classical, Blues, Pop -- filter from the full list)
    - Import `DECADES` from `@/lib/collection/filters`
    - Genre row: label "Genre:" in `font-mono text-[10px] uppercase tracking-[0.2em] text-outline`, then chips
    - Decade row: label "Decade:" same style, then chips
    - Row gap: `gap-4` (16px between genre and decade rows)
    - Each chip: `<button className="px-4 py-1 rounded-full font-mono text-[10px] transition-colors {stateClasses}" aria-pressed={isActive}>`
      - Inactive: `bg-surface-container-low text-on-surface-variant border border-outline-variant/20 hover:bg-surface-container hover:text-on-surface`
      - Active: `bg-primary/10 text-primary border border-primary`
    - Click active chip deselects (toggles off)
    - Chip gap: `gap-2` (8px)
    - Layout: horizontal flex wrap

    **Create `browse-grid.tsx`** (per D-07, UI-SPEC):
    - Props: `{ genre: string | null, decade: string | null }`
    - If neither genre nor decade selected, render nothing (per D-08: browse empty state only shows after filter applied)
    - On genre/decade change: call `browseRecordsAction(genre, decade)` via `useTransition`
    - Loading: 4x skeleton cards using `.skeleton-shimmer` class
    - Results: grid layout `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4`
    - Each card: similar to record-search-card but without owners list. Shows: album art, title, artist, rarity badge, owner count badge
    - Empty state (after filter): `<div className="font-mono text-[10px] text-on-surface-variant text-center py-8">[NO_RECORDS_FOUND]</div>` with sub-text `<div className="font-mono text-sm text-on-surface-variant">No records match the selected filters. Try a different combination.</div>`

    **Create `suggested-section.tsx`** (per D-02, UI-SPEC):
    - On mount: call `getSuggestionsAction()` via `useEffect` + `useTransition`
    - Section header: `<div className="font-mono text-[10px] uppercase tracking-[0.2em] text-outline">SUGGESTED_FOR_YOU</div>`
    - Grid: `grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-4`
    - Max 8 cards
    - Each card: same style as browse-grid cards
    - Loading: 4x skeleton cards
    - Empty state: `<div className="font-mono text-[10px] text-on-surface-variant">[NO_SUGGESTIONS_YET]</div>` with body `Follow more diggers or import your collection to get personalized suggestions.`
  </action>
  <verify>
    <automated>npx tsc --noEmit --project tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - src/app/(protected)/(explore)/explorar/page.tsx contains `"use client"` directive
    - src/app/(protected)/(explore)/explorar/page.tsx contains `role="tablist"`
    - src/app/(protected)/(explore)/explorar/page.tsx contains `DIGGERS` and `RECORDS` tab labels
    - src/app/(protected)/(explore)/explorar/page.tsx imports `SearchSection` from `./_components/search-section`
    - src/app/(protected)/(explore)/explorar/page.tsx imports `RecordsTab` from `./_components/records-tab`
    - src/app/(protected)/(explore)/explorar/page.tsx does NOT contain `SEARCH_RESULTS` or `DIRECTORIES` (hardcoded data removed)
    - records-tab.tsx contains `RecordSearch` and `BrowseFilters` and `SuggestedSection`
    - record-search.tsx contains `searchRecordsAction` import
    - record-search.tsx contains `Search by record or artist...` placeholder text
    - record-search.tsx contains `300` (debounce timeout)
    - record-search-card.tsx contains `getRarityTier` import
    - record-search-card.tsx contains `text-tertiary border-tertiary` (Ultra Rare colors)
    - owners-list.tsx contains `[NO_OWNERS_IN_NETWORK]`
    - browse-filters.tsx contains `aria-pressed`
    - browse-filters.tsx contains `DISCOGS_GENRES` or genre chip values
    - browse-filters.tsx contains `DECADES` import
    - browse-grid.tsx contains `browseRecordsAction` import
    - browse-grid.tsx contains `[NO_RECORDS_FOUND]`
    - suggested-section.tsx contains `getSuggestionsAction` import
    - suggested-section.tsx contains `SUGGESTED_FOR_YOU`
    - suggested-section.tsx contains `[NO_SUGGESTIONS_YET]`
    - TypeScript compilation succeeds (tsc --noEmit exits 0 or only has pre-existing errors)
  </acceptance_criteria>
  <done>
    Explorar page rewritten with DIGGERS/RECORDS tabs. RECORDS tab has working record search with debounce, genre/decade filter chips, browse grid, and taste-match suggestions section. All hardcoded placeholder data removed. Phase 5 SearchSection preserved unchanged in DIGGERS tab.
  </done>
</task>

</tasks>

<verification>
npx tsc --noEmit && npx vitest run --reporter=dot
TypeScript compiles without errors. Existing tests still pass.
</verification>

<success_criteria>
- Explorar page has DIGGERS/RECORDS tab bar with proper ARIA roles
- DIGGERS tab renders existing SearchSection unchanged
- RECORDS tab renders record search, browse filters, browse grid, and suggested section
- Record search uses 300ms debounce with minimum 2 character trigger
- Filter chips support single selection per dimension with toggle-off
- All components use Ghost Protocol design tokens per UI-SPEC
- No hardcoded placeholder data remains on the page
</success_criteria>

<output>
After completion, create `.planning/phases/06-discovery-notifications/06-02-SUMMARY.md`
</output>
