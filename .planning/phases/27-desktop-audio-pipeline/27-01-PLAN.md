---
phase: 27-desktop-audio-pipeline
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - apps/desktop/src/main/audio/ffmpeg-pipeline.ts
  - apps/desktop/src/main/audio/ffmpeg-pipeline.test.ts
autonomous: true
requirements: [TRD-07, TRD-08, TRD-09]

must_haves:
  truths:
    - "FFmpeg pipeline rejects files shorter than 2 minutes with a clear error"
    - "extractSpecs returns format, bitrate, sample_rate, duration for a valid audio file"
    - "generatePreview calls FFmpeg with -c copy (no transcoding) and a random offset between 10%–80% of duration"
    - "SHA-256 of source file is computed via Node.js crypto and returned alongside specs"
    - "All behavior is test-driven — tests pass before implementation is considered complete"
  artifacts:
    - path: "apps/desktop/src/main/audio/ffmpeg-pipeline.ts"
      provides: "extractSpecs, generatePreview, computeFileSha256 — main process audio utilities"
      exports: ["extractSpecs", "generatePreview", "computeFileSha256", "AudioSpecs", "PreviewResult"]
    - path: "apps/desktop/src/main/audio/ffmpeg-pipeline.test.ts"
      provides: "Vitest unit tests for all pipeline behaviors"
      min_lines: 80
  key_links:
    - from: "apps/desktop/src/main/audio/ffmpeg-pipeline.ts"
      to: "ffmpeg-static"
      via: "import ffmpegPath from 'ffmpeg-static'"
      pattern: "ffmpeg-static"
    - from: "apps/desktop/src/main/audio/ffmpeg-pipeline.ts"
      to: "music-metadata"
      via: "import * as mm from 'music-metadata'"
      pattern: "music-metadata"
---

<objective>
TDD plan: FFmpeg pipeline for audio spec extraction, SHA-256 computation, and raw 2-minute preview generation.

Purpose: Desktop app must extract audio metadata and cut previews without transcoding before multi-item P2P transfer. This is the core audio processing layer that all upstream trade flow depends on.
Output: apps/desktop/src/main/audio/ffmpeg-pipeline.ts with full test coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@apps/desktop/src/main/webrtc/chunked-transfer.ts
@apps/desktop/src/shared/ipc-types.ts
@packages/trade-domain/src/constants.ts

<interfaces>
<!-- Existing chunked-transfer.ts exports (executor must not duplicate) -->
From apps/desktop/src/main/webrtc/chunked-transfer.ts:
```typescript
export async function computeFileSha256(filePath: string): Promise<string>
// NOTE: This private function in chunked-transfer.ts must be re-exported from
// the new pipeline module for unified access. Do NOT duplicate logic — import it.
// But since it's not exported, replicate it cleanly in ffmpeg-pipeline.ts.
```

From apps/desktop/package.json (current deps — executor adds ffmpeg-static and music-metadata):
```json
{
  "dependencies": {
    "@digswap/trade-domain": "workspace:*",
    "@supabase/supabase-js": "^2.101.0",
    "electron-store": "^8.2.0",
    "peerjs": "^1.5.4"
  }
}
```
</interfaces>
</context>

<feature>
  <name>FFmpeg Audio Pipeline</name>
  <files>
    apps/desktop/src/main/audio/ffmpeg-pipeline.ts,
    apps/desktop/src/main/audio/ffmpeg-pipeline.test.ts
  </files>
  <behavior>
    Exported types:
    - AudioSpecs { format: string; bitrate: number; sampleRate: number; duration: number; }
    - PreviewResult { previewPath: string; offsetSeconds: number; durationSeconds: number; }

    extractSpecs(filePath: string): Promise<AudioSpecs>
    - Test 1: Returns correct format/bitrate/sampleRate/duration for a 3min FLAC fixture
    - Test 2: Falls back to music-metadata when FFmpeg probe fails (mock ffprobe error)
    - Test 3: Throws AudioPipelineError('FILE_TOO_SHORT') when duration < 120s
    - Test 4: Throws AudioPipelineError('PROBE_FAILED') when both FFmpeg and music-metadata fail

    generatePreview(filePath: string, specs: AudioSpecs, outputDir: string): Promise<PreviewResult>
    - Test 5: Calls FFmpeg with -ss <offset> -t 120 -c copy (no transcoding)
    - Test 6: offset is between 10% and 80% of duration (computed as Math.random() * 0.7 + 0.1)
    - Test 7: Output filename is derived from input basename + '_preview' + original extension
    - Test 8: Throws AudioPipelineError('FFMPEG_FAILED') when spawn exits with non-zero code
    - Test 9: Rejects when specs.duration < 120 (guard even if called directly)

    computeFileSha256(filePath: string): Promise<string>
    - Test 10: Returns lowercase hex SHA-256 string for a known fixture
    - Test 11: Rejects with ENOENT when file does not exist

    AudioPipelineError class:
    - Test 12: instanceof Error, has code property ('FILE_TOO_SHORT' | 'PROBE_FAILED' | 'FFMPEG_FAILED')
  </behavior>
  <implementation>
    1. Add deps to apps/desktop/package.json: "ffmpeg-static": "^5.2.0", "music-metadata": "^10.0.0"
       Add devDeps: "@types/ffmpeg-static": "^3.0.0", "vitest": "^3.0.0"
    2. Run pnpm install from workspace root
    3. Create apps/desktop/src/main/audio/ffmpeg-pipeline.ts:
       - import ffmpegPath from 'ffmpeg-static' (returns absolute binary path bundled in node_modules)
       - import { parseFile } from 'music-metadata' for fallback
       - extractSpecs: spawn ffmpeg -v quiet -print_format json -show_streams -show_format <filePath>
         to get probe data. Parse stdout JSON. Map codec_name→format, bit_rate→bitrate,
         sample_rate→sampleRate, duration→duration. If spawn fails or parse fails, fall through
         to music-metadata fallback. After obtaining duration, throw FILE_TOO_SHORT if < 120.
       - generatePreview: compute offset = Math.floor((Math.random() * 0.7 + 0.1) * specs.duration).
         Spawn ffmpeg -ss <offset> -t 120 -c copy <outputPath>. outputPath is
         path.join(outputDir, basename_noext + '_preview' + ext). Return PreviewResult.
       - computeFileSha256: streaming Node.js crypto hash, identical to chunked-transfer.ts pattern.
       - Export AudioPipelineError with code discriminant.
    4. Create vitest.config.ts in apps/desktop if absent (executor checks first).
  </implementation>
</feature>

<verification>
<automated>cd "C:\Users\INTEL\Desktop\Get Shit DOne" && pnpm --filter @digswap/desktop exec vitest run src/main/audio/ffmpeg-pipeline.test.ts</automated>
</verification>

<success_criteria>
All 12 tests pass. No TypeScript errors (tsc --noEmit). ffmpeg-static and music-metadata present in apps/desktop/node_modules.
</success_criteria>

<output>
After completion, create `.planning/phases/27-desktop-audio-pipeline/27-01-SUMMARY.md`
</output>
