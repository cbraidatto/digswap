---
phase: 07-community-reviews
plan: 05
type: execute
wave: 4
depends_on: ["07-04"]
files_modified:
  - tests/unit/community/create-group.test.ts
  - tests/unit/community/membership.test.ts
  - tests/unit/community/group-post.test.ts
  - tests/unit/community/group-feed.test.ts
  - tests/unit/community/visibility.test.ts
  - tests/unit/community/review.test.ts
autonomous: false
requirements: [COMM-01, COMM-02, COMM-03, COMM-04, COMM-05, REV-01, REV-02, REV-03]

must_haves:
  truths:
    - "All community test scaffolds are implemented with real assertions"
    - "Tests mock DB and auth correctly following project patterns"
    - "Human has verified the full community + reviews flow works"
  artifacts:
    - path: "tests/unit/community/create-group.test.ts"
      provides: "Group creation tests"
      contains: "createGroupAction"
    - path: "tests/unit/community/review.test.ts"
      provides: "Review CRUD tests"
      contains: "createReviewAction"
  key_links:
    - from: "tests/unit/community/create-group.test.ts"
      to: "src/actions/community.ts"
      via: "import and mock"
      pattern: "createGroupAction"
---

<objective>
Implement all Wave 0 test scaffolds with real assertions (replacing it.todo() placeholders) and run the full test suite. Then perform human verification of the complete community + reviews flow.

Purpose: Validates all 8 requirements (COMM-01 through COMM-05, REV-01 through REV-03) with automated tests and manual verification.
Output: Green test suite + human-verified community flow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-community-reviews/07-CONTEXT.md
@.planning/phases/07-community-reviews/07-VALIDATION.md
@.planning/phases/07-community-reviews/07-01-SUMMARY.md
@.planning/phases/07-community-reviews/07-02-SUMMARY.md
@.planning/phases/07-community-reviews/07-03-SUMMARY.md
@.planning/phases/07-community-reviews/07-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement all community test suites</name>
  <files>
    tests/unit/community/create-group.test.ts,
    tests/unit/community/membership.test.ts,
    tests/unit/community/group-post.test.ts,
    tests/unit/community/group-feed.test.ts,
    tests/unit/community/visibility.test.ts,
    tests/unit/community/review.test.ts
  </files>
  <read_first>
    tests/unit/community/create-group.test.ts,
    tests/unit/community/membership.test.ts,
    tests/unit/community/group-post.test.ts,
    tests/unit/community/group-feed.test.ts,
    tests/unit/community/visibility.test.ts,
    tests/unit/community/review.test.ts,
    tests/unit/community/slugify.test.ts,
    src/actions/community.ts,
    src/lib/community/queries.ts,
    src/lib/community/slugify.ts,
    tests/unit/discovery/search-records.test.ts,
    tests/unit/social/follow.test.ts
  </read_first>
  <action>
    Implement all 6 test files by replacing `it.todo()` placeholders with real test implementations. Follow the existing test patterns from `tests/unit/discovery/` and `tests/unit/social/` for mocking DB, auth, and server actions.

    **Mocking pattern (from existing tests):**
    ```typescript
    vi.mock("@/lib/supabase/server", () => ({
      createClient: vi.fn(() => ({
        auth: {
          getUser: vi.fn(() => ({ data: { user: { id: "test-user-id" } } })),
        },
      })),
    }));

    vi.mock("@/lib/db", () => ({
      db: {
        select: vi.fn(() => ({ from: vi.fn(() => ({ where: vi.fn(() => []) })) })),
        insert: vi.fn(() => ({ values: vi.fn(() => ({ returning: vi.fn(() => [{ id: "new-id" }]) })) })),
        delete: vi.fn(() => ({ where: vi.fn(() => {}) })),
        update: vi.fn(() => ({ set: vi.fn(() => ({ where: vi.fn(() => {}) })) })),
      },
    }));
    ```

    **`tests/unit/community/create-group.test.ts`** -- COMM-01:
    - Test: createGroupAction creates group with valid name, description, category, visibility
    - Test: createGroupAction generates slug from name (mock slugify response)
    - Test: createGroupAction rejects empty name (expects error)
    - Test: createGroupAction rejects name > 80 chars (expects error)
    - Test: createGroupAction inserts creator as admin member
    - Test: createGroupAction sets visibility to "private" when specified
    - Mock the DB insert to return `{ slug: "test-group" }`

    **`tests/unit/community/membership.test.ts`** -- COMM-02:
    - Test: joinGroupAction inserts membership row
    - Test: joinGroupAction increments member_count
    - Test: joinGroupAction returns error when already member
    - Test: leaveGroupAction deletes membership row
    - Test: leaveGroupAction decrements member_count
    - Test: leaveGroupAction returns error when not member
    - Test: leaveGroupAction prevents sole admin from leaving

    **`tests/unit/community/group-post.test.ts`** -- COMM-03:
    - Test: createPostAction inserts post with content
    - Test: createPostAction inserts post with linked release_id
    - Test: createPostAction rejects empty content
    - Test: createPostAction requires group membership (mock non-member)
    - Test: createPostAction calls logActivity with actionType "group_post"

    **`tests/unit/community/group-feed.test.ts`** -- COMM-04:
    - Test: getGroupPosts returns posts ordered by createdAt desc
    - Test: getGroupPosts applies cursor-based pagination (filter lt createdAt)
    - Test: getGroupPosts joins with profiles for username
    - Test: getGroupPosts joins with releases for linked record info
    - Test: getGroupPosts joins with reviews for review rating

    **`tests/unit/community/visibility.test.ts`** -- COMM-05:
    - Test: private group appears in getMemberGroups results
    - Test: getGroupPosts returns empty for non-member on private group
    - Test: generateInviteAction creates invite token
    - Test: generateInviteAction requires admin role
    - Test: acceptInviteAction adds member to group
    - Test: inviteUserAction sends notification to target user

    **`tests/unit/community/review.test.ts`** -- REV-01, REV-02, REV-03:
    - Test: createReviewAction inserts review with rating 1-5
    - Test: createReviewAction rejects rating 0 (validation error)
    - Test: createReviewAction rejects rating 6 (validation error)
    - Test: createReviewAction creates group_post with reviewId
    - Test: createReviewAction handles pressing-specific review (isPressingSpecific: true, pressingDetails: "Original 1973 SP")
    - Test: createReviewAction handles general release review (isPressingSpecific: false)
    - Test: getReviewsForRelease returns reviews filtered by releaseId
    - Test: getReviewsForRelease returns reviews ordered by createdAt desc
    - Test: getReviewCountForRelease returns correct count

    Each test file uses `describe()` blocks, `beforeEach()` for mock setup, and `afterEach(() => vi.restoreAllMocks())`. Each test verifies the mock was called with expected arguments using `expect(mockFn).toHaveBeenCalledWith(expect.objectContaining({...}))`.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/community/ --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - tests/unit/community/create-group.test.ts does NOT contain `it.todo`
    - tests/unit/community/membership.test.ts does NOT contain `it.todo`
    - tests/unit/community/group-post.test.ts does NOT contain `it.todo`
    - tests/unit/community/group-feed.test.ts does NOT contain `it.todo`
    - tests/unit/community/visibility.test.ts does NOT contain `it.todo`
    - tests/unit/community/review.test.ts does NOT contain `it.todo`
    - tests/unit/community/create-group.test.ts contains `createGroupAction`
    - tests/unit/community/review.test.ts contains `createReviewAction`
    - tests/unit/community/review.test.ts contains `getReviewsForRelease`
    - npx vitest run tests/unit/community/ exits 0
  </acceptance_criteria>
  <done>All 7 community test files pass (slugify + 6 server action/query test files). Zero it.todo() placeholders remain. All 8 requirements covered by automated tests.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of complete community + reviews flow</name>
  <files>N/A</files>
  <action>
    Human verifies the complete community + reviews system works end-to-end by testing all 10 verification steps in the how-to-verify section. This covers all 8 requirements: group creation (COMM-01), join/leave (COMM-02), posting (COMM-03), group feed (COMM-04), private groups (COMM-05), pressing review (REV-01), general review (REV-02), review browsing (REV-03).
  </action>
  <verify>Human confirms all 10 verification steps pass</verify>
  <done>Human has approved the complete community + reviews flow</done>
  <what-built>Complete community + reviews system across 4 plans: group discovery hub (/comunidade), group creation (/comunidade/new), group detail with posts and reviews (/comunidade/[slug]), private group invites (/join/[token]), review browsing on /explorar record cards, group posts in /feed.</what-built>
  <how-to-verify>
    1. Navigate to `/comunidade` -- verify genre groups appear as dotted rows, member groups section shows (or shows empty state), genre filter chips work, [+ CREATE_GROUP] button visible
    2. Click [+ CREATE_GROUP] -- fill out form with name "Test Group", description, category "Jazz", visibility "Public" -- submit and verify redirect to /comunidade/test-group
    3. On the group detail page -- verify header shows group name, category, member count, [JOINED] indicator (you are auto-joined as creator)
    4. Write a post in the composer -- type text, click [Post] -- verify post appears at top of feed
    5. Write a post with linked record -- click [+ link record], search for a record, select it, type review text, click [Post] -- verify post shows linked record with RARITY score
    6. Click [Write review] in composer -- select 4 stars, link a record, write review text, click [Post Review] -- verify review card appears with star rating and elevated style
    7. Navigate to `/explorar` -- search for the record you reviewed -- verify `reviews: 1` trigger appears on the card -- click to expand and verify your review shows inline
    8. Navigate to `/feed` -- verify your group posts appear with "posted in TEST GROUP" subtitle (only visible in global feed or if you follow yourself conceptually -- test with a second browser session if possible)
    9. Go back to `/comunidade` -- verify your new group appears in the MEMBER_GROUPS section
    10. Create a private group -- verify [PRIVATE] badge appears, verify non-members see [INVITE_ONLY]
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
</task>

</tasks>

<verification>
npx vitest run tests/unit/community/ --reporter=verbose
npx vitest run --reporter=verbose
</verification>

<success_criteria>
- All community tests pass (7 test files, 0 failures)
- Full test suite passes (npx vitest run)
- Human has verified the community + reviews flow end-to-end
- All 8 requirements (COMM-01 through COMM-05, REV-01 through REV-03) are verified
</success_criteria>

<output>
After completion, create `.planning/phases/07-community-reviews/07-05-SUMMARY.md`
</output>
