---
phase: 07-community-reviews
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/app/(protected)/(community)/comunidade/page.tsx
  - src/app/(protected)/(community)/comunidade/_components/group-card.tsx
  - src/app/(protected)/(community)/comunidade/_components/genre-group-row.tsx
  - src/app/(protected)/(community)/comunidade/_components/group-discovery-hub.tsx
  - src/app/(protected)/(community)/comunidade/_components/group-filter-chips.tsx
  - src/app/(protected)/(community)/comunidade/new/page.tsx
  - src/app/(protected)/(community)/comunidade/new/_components/group-create-form.tsx
autonomous: true
requirements: [COMM-01, COMM-02, COMM-05]

must_haves:
  truths:
    - "User sees genre groups sorted by member count on /comunidade"
    - "User sees member-created groups paginated on /comunidade"
    - "User can filter groups by genre via filter chips"
    - "User can navigate to /comunidade/new and create a group"
    - "Private groups show [PRIVATE] badge but no join button"
  artifacts:
    - path: "src/app/(protected)/(community)/comunidade/page.tsx"
      provides: "Group discovery hub page (replaces placeholder)"
      contains: "COMMUNITY_HUB"
    - path: "src/app/(protected)/(community)/comunidade/_components/group-discovery-hub.tsx"
      provides: "Client component for genre filter + member group pagination"
      contains: "GroupDiscoveryHub"
    - path: "src/app/(protected)/(community)/comunidade/new/_components/group-create-form.tsx"
      provides: "Group creation form with validation"
      contains: "GroupCreateForm"
  key_links:
    - from: "src/app/(protected)/(community)/comunidade/page.tsx"
      to: "src/actions/community.ts"
      via: "server action imports for initial data load"
      pattern: "import.*from.*actions/community"
    - from: "src/app/(protected)/(community)/comunidade/new/_components/group-create-form.tsx"
      to: "src/actions/community.ts"
      via: "createGroupAction call"
      pattern: "createGroupAction"
---

<objective>
Build the `/comunidade` group discovery hub page and `/comunidade/new` group creation form. This transforms the placeholder Swaps/Discussions/Crews page into a functional community hub with genre groups, member groups, genre filter chips, and a create group form.

Purpose: Delivers COMM-01 (group creation), partial COMM-02 (group discovery for joining), and COMM-05 (visibility display). Users can discover and create groups.
Output: Functional /comunidade page and /comunidade/new form.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-community-reviews/07-CONTEXT.md
@.planning/phases/07-community-reviews/07-UI-SPEC.md
@.planning/phases/07-community-reviews/07-01-SUMMARY.md

<interfaces>
<!-- Contracts from Plan 01 that this plan consumes -->

From src/actions/community.ts:
```typescript
export async function createGroupAction(data: { name: string; description?: string; category?: string; visibility: "public" | "private" }): Promise<{ slug: string }>;
export async function loadGenreGroupsAction(genreFilter?: string): Promise<GenreGroup[]>;
export async function loadMemberGroupsAction(genreFilter?: string, cursor?: string): Promise<MemberGroup[]>;
```

From src/lib/community/queries.ts:
```typescript
export type GenreGroup = { id: string; name: string; slug: string; category: string | null; memberCount: number };
export type MemberGroup = { id: string; name: string; slug: string; category: string | null; visibility: string; memberCount: number; createdAt: string; creatorUsername: string | null };
```

From src/lib/discogs/taxonomy.ts:
```typescript
export const DISCOGS_GENRES = ["Blues","Brass & Military","Children's","Classical","Electronic","Folk, World, & Country","Funk / Soul","Hip Hop","Jazz","Latin","Non-Music","Pop","Reggae","Rock","Stage & Screen"] as const;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: /comunidade group discovery hub page</name>
  <files>
    src/app/(protected)/(community)/comunidade/page.tsx,
    src/app/(protected)/(community)/comunidade/_components/genre-group-row.tsx,
    src/app/(protected)/(community)/comunidade/_components/group-card.tsx,
    src/app/(protected)/(community)/comunidade/_components/group-discovery-hub.tsx,
    src/app/(protected)/(community)/comunidade/_components/group-filter-chips.tsx
  </files>
  <read_first>
    src/app/(protected)/(community)/comunidade/page.tsx,
    src/actions/community.ts,
    src/lib/community/queries.ts,
    src/lib/discogs/taxonomy.ts,
    .planning/phases/07-community-reviews/07-UI-SPEC.md,
    .planning/phases/07-community-reviews/07-CONTEXT.md,
    src/app/(protected)/(explore)/explorar/page.tsx
  </read_first>
  <action>
    **Rewrite `src/app/(protected)/(community)/comunidade/page.tsx`** as a server component (per D-01):

    - Import `loadGenreGroupsAction` and `loadMemberGroupsAction` from `@/actions/community`
    - Fetch initial genre groups (no filter) and member groups (page 1) server-side
    - Render page header: terminal label `COMMUNITY_HUB` (JetBrains Mono, 10px, uppercase, `tracking-[0.2em]`, `text-outline`) and subtitle `// {N} active groups` (JetBrains Mono, 10px, `text-on-surface-variant`) where N is total group count (genreGroups.length + initial memberGroups total estimated)
    - Render `[+ CREATE_GROUP]` button as a Next.js `<Link href="/comunidade/new">` styled: `font-mono text-[10px] border border-primary text-primary px-3 py-1 rounded hover:bg-primary/10`
    - Header layout: `flex items-center justify-between` wrapped in `max-w-4xl mx-auto px-4 md:px-8 py-8`
    - Render `<GroupFilterChips>` client component below header
    - Render `<GroupDiscoveryHub>` client component with `initialGenreGroups` and `initialMemberGroups` props
    - Page structure: header -> filter chips -> genre groups section -> member groups section

    **Create `src/app/(protected)/(community)/comunidade/_components/genre-group-row.tsx`** (server component):

    - Props: `{ name: string; slug: string; memberCount: number }`
    - Render as a `<Link href={"/comunidade/" + slug}>` styled as a monospace dotted row
    - Format: `{Name}` left-aligned, dots filling middle, `{N} members` right-aligned
    - Implementation: use flexbox with `flex-1` spacer of dots or CSS technique: name span + flex-grow dotted fill + count span
    - Use `font-mono text-[10px] text-on-surface` for name, `text-on-surface-variant` for member count
    - Hover: `hover:text-primary` on the name portion
    - Format member count: use `Intl.NumberFormat` for compact display (1.2k, 2.1k, etc.) or simple `toLocaleString()`

    **Create `src/app/(protected)/(community)/comunidade/_components/group-card.tsx`** (server component):

    - Props: `{ name: string; slug: string; category: string | null; visibility: string; memberCount: number; creatorUsername: string | null }`
    - Background: `bg-surface-container-low`, border: `border border-outline-variant/10`, rounded, `p-4`
    - Hover: `hover:bg-surface-container` transition
    - Group name: `font-heading font-semibold text-sm text-on-surface`
    - Member count: `font-mono text-[10px] text-on-surface-variant`
    - If visibility === "private": render `[PRIVATE]` badge with `font-mono text-[10px] font-semibold bg-surface-container-high text-on-surface-variant border border-outline-variant/20 px-2 py-0.5 rounded`
    - Creator credit: `Created by @{creatorUsername}` with username as `<Link href={"/perfil/" + creatorUsername}>` and `hover:text-primary`
    - Entire card wrapped in `<Link href={"/comunidade/" + slug}>`

    **Create `src/app/(protected)/(community)/comunidade/_components/group-filter-chips.tsx`** (client component, `"use client"`):

    - Props: `{ genres: string[]; activeGenre: string | null; onGenreChange: (genre: string | null) => void }`
    - Render `Genre:` label (font-mono text-[10px] text-on-surface-variant) followed by chip row
    - `All` chip first (default, no filter)
    - Each genre as a `<button>` with `aria-pressed` reflecting active state
    - Active chip: `bg-primary/10 text-primary border-primary`
    - Inactive chip: `bg-surface-container-low text-on-surface-variant border border-outline-variant/20 hover:text-on-surface`
    - All chips: `font-mono text-[10px] px-2 py-1 rounded`
    - Clicking active chip deselects (returns to null/All)
    - Chip row: `flex flex-wrap gap-2`

    **Create `src/app/(protected)/(community)/comunidade/_components/group-discovery-hub.tsx`** (client component, `"use client"`):

    - Props: `{ initialGenreGroups: GenreGroup[]; initialMemberGroups: MemberGroup[] }`
    - State: `activeGenre` (string | null, default null), `genreGroups` (GenreGroup[]), `memberGroups` (MemberGroup[]), `showAllGenres` (boolean, default false), `hasMoreMembers` (boolean), `cursor` (string | null)
    - When `activeGenre` changes: call `loadGenreGroupsAction(activeGenre)` and `loadMemberGroupsAction(activeGenre)` to refresh both sections
    - **Genre groups section:**
      - Section header: `GENRE_GROUPS` (font-mono text-[10px] uppercase tracking-[0.2em] text-outline)
      - Show top 6 genre groups by member count
      - `[show all genre groups]` link below — clicking sets `showAllGenres = true` to reveal all 15
      - Each genre group rendered via `<GenreGroupRow>`
    - **Member groups section:**
      - Section header: `MEMBER_GROUPS` (same terminal label style)
      - Render each member group via `<GroupCard>`
      - `[load more]` button at bottom, calls `loadMemberGroupsAction(activeGenre, cursor)` and appends results
      - When no member groups: empty state with `[NO_GROUPS_YET]` label and `No community groups yet. Be the first to create one.` body
    - Render `<GroupFilterChips>` with genres from DISCOGS_GENRES, passing activeGenre and onGenreChange handler
  </action>
  <verify>
    <automated>npx tsc --noEmit --project tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - src/app/(protected)/(community)/comunidade/page.tsx contains `COMMUNITY_HUB`
    - src/app/(protected)/(community)/comunidade/page.tsx contains `CREATE_GROUP`
    - src/app/(protected)/(community)/comunidade/page.tsx contains `loadGenreGroupsAction`
    - src/app/(protected)/(community)/comunidade/_components/genre-group-row.tsx contains `export function GenreGroupRow`
    - src/app/(protected)/(community)/comunidade/_components/genre-group-row.tsx contains `members`
    - src/app/(protected)/(community)/comunidade/_components/group-card.tsx contains `PRIVATE`
    - src/app/(protected)/(community)/comunidade/_components/group-card.tsx contains `Created by`
    - src/app/(protected)/(community)/comunidade/_components/group-discovery-hub.tsx contains `"use client"`
    - src/app/(protected)/(community)/comunidade/_components/group-discovery-hub.tsx contains `GENRE_GROUPS`
    - src/app/(protected)/(community)/comunidade/_components/group-discovery-hub.tsx contains `MEMBER_GROUPS`
    - src/app/(protected)/(community)/comunidade/_components/group-discovery-hub.tsx contains `NO_GROUPS_YET`
    - src/app/(protected)/(community)/comunidade/_components/group-filter-chips.tsx contains `aria-pressed`
    - TypeScript compilation passes with zero errors
  </acceptance_criteria>
  <done>/comunidade is a functional group discovery hub showing genre groups (dotted rows), member groups (cards with [PRIVATE] badges), genre filter chips, and a [+ CREATE_GROUP] link. The old Swaps/Discussions/Crews placeholder is entirely replaced.</done>
</task>

<task type="auto">
  <name>Task 2: /comunidade/new group creation form</name>
  <files>
    src/app/(protected)/(community)/comunidade/new/page.tsx,
    src/app/(protected)/(community)/comunidade/new/_components/group-create-form.tsx
  </files>
  <read_first>
    src/actions/community.ts,
    .planning/phases/07-community-reviews/07-UI-SPEC.md,
    src/app/(protected)/(community)/comunidade/page.tsx
  </read_first>
  <action>
    **Create `src/app/(protected)/(community)/comunidade/new/page.tsx`** (server component):

    - Simple wrapper page that renders `<GroupCreateForm />`
    - Page heading: `NEW_GROUP` terminal label (font-mono text-[10px] uppercase tracking-[0.2em] text-outline)
    - Centered layout: `max-w-[640px] mx-auto px-4 md:px-8 py-8`

    **Create `src/app/(protected)/(community)/comunidade/new/_components/group-create-form.tsx`** (client component, `"use client"`):

    - Use React Hook Form + Zod for validation
    - Zod schema:
      ```typescript
      const groupSchema = z.object({
        name: z.string().min(1, "GROUP_NAME_REQUIRED").max(80, "NAME_TOO_LONG (max 80 chars)"),
        description: z.string().optional(),
        category: z.string().optional(),
        visibility: z.enum(["public", "private"]),
      });
      ```
    - Form fields (per UI-SPEC):
      - `GROUP_NAME` label (font-mono text-[10px] uppercase tracking-[0.2em] text-outline), `<Input>` component, character counter right-aligned below (font-mono text-[10px] text-on-surface-variant showing `{length}/80`)
      - `DESCRIPTION (optional)` label, `<Textarea>` with `min-h-[80px] resize-none`
      - `CATEGORY` label with hint `(genre, era, region, style...)`, `<Input>` component (free-form text)
      - `VISIBILITY` label, shadcn `<Select>` with options `Public (anyone can join)` and `Private (invite only)`, default "public"
    - All input fields styled: `bg-surface-container-low border border-outline-variant/20 focus:border-primary rounded px-3 py-2 text-sm font-sans`
    - Form gap between fields: `space-y-6` (24px = lg)
    - Inline validation errors: font-mono text-[10px] text-destructive below each field
    - `[Cancel]` button: `<Link href="/comunidade">` styled `font-mono text-[10px] text-on-surface-variant hover:text-on-surface px-4 py-2`
    - `[Create Group]` button: `font-mono text-[10px] bg-primary text-primary-foreground px-4 py-2 rounded`, disabled while submitting
    - Button row: `flex items-center justify-between mt-8`
    - On submit: call `createGroupAction(data)`, on success `toast("Group created.")` via sonner + `router.push("/comunidade/" + result.slug)`, on error show toast error
    - Import `createGroupAction` from `@/actions/community`, `toast` from `sonner`, `useRouter` from `next/navigation`
  </action>
  <verify>
    <automated>npx tsc --noEmit --project tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - src/app/(protected)/(community)/comunidade/new/page.tsx contains `NEW_GROUP`
    - src/app/(protected)/(community)/comunidade/new/_components/group-create-form.tsx contains `"use client"`
    - src/app/(protected)/(community)/comunidade/new/_components/group-create-form.tsx contains `createGroupAction`
    - src/app/(protected)/(community)/comunidade/new/_components/group-create-form.tsx contains `GROUP_NAME_REQUIRED`
    - src/app/(protected)/(community)/comunidade/new/_components/group-create-form.tsx contains `visibility`
    - src/app/(protected)/(community)/comunidade/new/_components/group-create-form.tsx contains `Group created`
    - src/app/(protected)/(community)/comunidade/new/_components/group-create-form.tsx contains `max 80 chars`
    - TypeScript compilation passes with zero errors
  </acceptance_criteria>
  <done>/comunidade/new renders a group creation form with validated name (1-80 chars), optional description, optional category, visibility selector (public/private), and submits via createGroupAction. Success redirects to the new group page.</done>
</task>

</tasks>

<verification>
npx tsc --noEmit --project tsconfig.json 2>&1 | head -30
</verification>

<success_criteria>
- /comunidade shows genre groups as dotted rows and member groups as cards
- Genre filter chips narrow both sections
- [PRIVATE] badge appears on private groups
- /comunidade/new form validates and creates groups via server action
- The old Swaps/Discussions/Crews placeholder is completely gone
</success_criteria>

<output>
After completion, create `.planning/phases/07-community-reviews/07-02-SUMMARY.md`
</output>
